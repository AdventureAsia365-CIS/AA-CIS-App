# AA-464 [A4] Nối dây suggest_ramp_transition() — Implementation Notes

## STEP 0 (06/09/2026)

- **Premise confirmed still live**: `services/acp_produce/trust_ramp.py::suggest_ramp_transition()`
  (pure function, no DB, takes `current_mode`/`engagement_ok`/`weeks_active`) has **0 real
  callers** — `grep -rn "suggest_ramp_transition"` across `api/`, `services/`, `frontend/` only
  turns up the function's own definition, its own docstring self-references, and
  `tests/unit/test_aa365_trust_ramp.py`. `admin_a4.py`'s own module docstring (decision #2, from
  AA-437) independently confirms the same finding: "no `suggest_ramp_transition()` automation, no
  `engagement_ok`/`weeks_active` formula (STEP0 confirmed neither is computed anywhere)".
- **`confirm_ramp_transition()` also has 0 real callers today** — a real, additional finding not
  called out by AA-464's own issue text. `services/acp_produce/packets.py::
  packet_pieces_review_complete()`'s docstring says it exists to gate
  `admin_produce.py::approve_gate_c()` "before it calls `trust_ramp.confirm_ramp_transition()`" —
  but `api/routers/admin_produce.py` **does not exist anywhere in this repo**
  (`find . -iname "*admin_produce*"` → nothing). That was aspirational documentation from AA-365,
  never built. `confirm_ramp_transition()` is otherwise sound (AA-365's own follow-up round
  unlocked `packets.ALLOWED_PUBLISH_MODES_UNTIL_F6` to all 3 ramp states, live-tested) — this
  build gives it its first real caller, via the new approve endpoint below.
- **The `aamc/delivery.py` reference file AA-365 ported this from does not exist in this
  checkout** (`docs/AI-gent-for automation works/aa-marketing-v2/aamc/delivery.py`) — it's a
  separate, uncommitted reference repo. `docs/implementation-notes/AA-365.md` (this repo, already
  committed) has the full port record instead; read that rather than trying to find the original.
- **No architecture change needed to `suggest_ramp_transition()` itself.** The issue's STEP0
  ask ("có cần cập nhật gì sau các thay đổi kiến trúc gần đây — T7 viết lại, distinctiveness mới
  — không?") — checked: T7 rewrite (quarter/slot planning) and B4 distinctiveness scoring
  (AA-445-02) are both upstream of content SELECTION/PLANNING, entirely unrelated to Trust Ramp's
  subject (production/delivery trust — whether a tenant's *packets* can auto-publish). The `RAMP`
  list (`propose_only`/`approve_to_publish`/`veto_window_auto`) and the pure escalation rule
  (`engagement_ok and weeks_active >= 2`) are untouched by anything shipped since AA-365
  (14/08/2026). No update needed — confirmed by reading the function fresh, not assumed.
- **`engagement_ok`/`weeks_active` were never computed anywhere** — confirmed, this is the actual
  gap AA-464 asks to close. Two DIFFERENT "engagement" concepts already exist in this codebase and
  must not be confused:
  1. `services/acp_shared/content_metrics.py` (AA-448 round 6) — manual reach/engagement entry
     PER PIECE, rolled up into `tour_atoms.weight` for quarter-plan scoring. This is real, live
     data (`acp_shared.content_metric_snapshot`, migration 112).
  2. Trust Ramp's `engagement_ok` (aamc G3 concept) — a simple yes/no gate on whether a tenant's
     packets should be trusted with more autonomy. Never had its own metric source; the aamc
     prototype file that would define it isn't in this repo (see above).
  Rather than inventing a third, brand-new metric, this build defines Trust Ramp's
  `engagement_ok` as a real, live-data-backed proxy computed FROM (1) — see Decisions below —
  since it's the only genuine "engagement" signal that exists in this schema today.
- **packets schema** (migrations 094 + 106): `tenant_id` TEXT, `year`/`month`/`week` (`UNIQUE
  (tenant_id, year, month, week)` — one row per tenant per real calendar week, confirmed via
  migration 106 line 96), `publish_mode`, `status`, `created_at`, `delivered_at`. Since the
  uniqueness constraint already guarantees 1 row = 1 distinct week, `weeks_active` is a plain
  `COUNT(*) GROUP BY tenant_id` — no `DISTINCT` needed.
- **`acp_shared.content_metric_snapshot` schema** (migration 112): `tenant_id` UUID (FK
  `shared.tenants`), `piece_id` TEXT, `reach`/`engagement`/`clicks` INTEGER (nullable),
  `entered_at`. `content_metrics.py::_piece_score()`'s own "unknown stays unknown" rule (never
  divide by a fake reach of 1) is reused verbatim — a tenant with 0 reach-bearing snapshots gets
  `engagement_ok=False` (fail-closed), never a fabricated `True`.
- **AWS confirmed live this session** (`aws sts get-caller-identity --profile aa365-admin` →
  account 005097885195; ECS `aa-cis-dev-api` desired=1/running=1, task def `:221`; RDS
  `aa-cis-dev-db` `available`) — no cis-start needed, live-verify possible without asking Nghiep to
  start anything.
- **Environment note (not part of AA-464's own scope, but affected how this build was executed)**:
  this session's originally-assigned isolated worktree (`.claude/worktrees/agent-af4556496a2c5b3cb`)
  was gone by the time work resumed post-rate-limit — the shared checkout's default cwd had
  drifted to another parallel task's branch (`pqnghiep1354/aa-492-...`), matching the known
  "parallel sessions share git workdir" hazard (`memory/feedback_parallel_sessions_share_git_workdir.md`).
  Recovered by creating a fresh, explicitly-isolated worktree
  (`.claude/worktrees/agent-aa464`, branch `pqnghiep1354/aa-464-a4-noi-day-suggest-ramp-transition`,
  based on `origin/main` @ `2b7eb13`) rather than switching the shared checkout's branch under a
  possibly-still-running sibling session.

## Decisions

- **On-demand computation, not a scheduled job** — per the issue's own recommendation ("Đề xuất
  on-demand trước — đơn giản hơn, không cần thêm hạ tầng scheduler"). `GET /admin/a4/trust-ramp`
  now computes `engagement_ok`/`weeks_active`/`suggested_mode` fresh on every call, no caching, no
  new EventBridge schedule, no new table. Matches this repo's existing precedent
  (`suggest_trip_reallocation()`, AA-448 round 6 — same "never writes, recomputed live" shape).
  Nghiep is not available to re-confirm before build (background/async task); going with the
  issue's own explicit recommendation rather than blocking on a round-trip, and flagging this
  choice clearly in the Linear comment so it can be revisited.
- **`weeks_active` = `COUNT(*) FROM acp_deliver.packets GROUP BY tenant_id`** — real production
  cadence (how many real weekly packets exist for this tenant), not calendar time since account
  creation. Reuses `packets`' own `UNIQUE(tenant_id, year, month, week)` guarantee (migration 106)
  directly — no new column.
- **`engagement_ok` = tenant-wide average `engagement/reach` (from the LATEST snapshot per piece,
  same "DISTINCT ON piece_id ORDER BY entered_at DESC" shape `content_metrics.py`'s own
  `rollup_atom_weights()` already uses) >= `ENGAGEMENT_RATE_BASELINE`** (0.05,
  `services/acp_planning/constants.py` — reused as-is, not a new magic number), gated on
  `>= CONFIDENCE_ATOM_MIN_POSTS` (3) real reach-bearing snapshots for that tenant. Below the
  confidence gate: `engagement_ok=False` — "no data yet" is NOT the same as "engagement is good",
  same fail-closed law `_piece_score()` already establishes for the underlying data.
  New functions added to `trust_ramp.py` (not `content_metrics.py`) since these are Trust-Ramp-
  specific derived signals, consumed only here — `content_metrics.py`'s own public surface
  (`record_metric_snapshot`/`rollup_atom_weights`) is unchanged, no shared helper extracted since
  the two modules use the same raw data for genuinely different purposes (atom weight vs. ramp
  eligibility) with different aggregation scopes (per-atom vs. tenant-wide).
- **Suggestion surfaced per PACKET, not collapsed to one tenant-level flag** — matches
  `admin_a4.py`'s own existing decision #3 (AA-437: "no per-tenant single ramp level ... this
  returns every packet with its own level, never collapsed") and the current UI's per-packet-row
  table. `GET /admin/a4/trust-ramp` gains `engagement_ok`/`weeks_active`/`suggested_mode`/
  `eligible` per row; `eligible = (suggested_mode != publish_mode)`. The issue's own UI text says
  "Tenant X đủ điều kiện lên bậc Y" (tenant-level phrasing) but the existing data model is
  per-packet — kept the existing per-packet shape rather than inventing a tenant-collapse (which
  AA-437 explicitly decided against), and render the badge per eligible packet row, grouped
  visually by tenant exactly as the page already groups them.
- **Two new mutating endpoints**, both requiring an explicit admin click (per ADR-2026-038 §0.2 —
  never auto-transition):
  - `POST /admin/a4/trust-ramp/{packet_id}/approve` — re-computes the suggestion fresh (not
    trusting a client-supplied `suggested_mode`, same "never stale" principle
    `confirm_trip_reallocation()` already uses), then calls the EXISTING
    `trust_ramp.confirm_ramp_transition()` unchanged — this is that function's first real caller.
    BOFU hard-block and the 3-state validation inside `confirm_ramp_transition()` apply exactly as
    before; a blocked attempt still gets a `publish_mode_transition` audit_log row (already built
    into `confirm_ramp_transition()`) and this endpoint surfaces it as 409, not a silent no-op.
  - `POST /admin/a4/trust-ramp/{packet_id}/skip` — new. Re-computes the suggestion fresh, writes
    ONE `acp_shared.audit_log` row (`action='ramp_suggestion_skipped'`, same table every other
    real gate/approval decision in this repo already uses — no new logging shape), does NOT touch
    `packets.publish_mode`. Closes the issue's #4 ask ("log mỗi lần admin duyệt/bỏ qua") for the
    "bỏ qua" (skip/dismiss) half, which had no existing mechanism at all.
  Both actions reuse the exact `x-admin-user-id` → `actor="admin:<id>"` convention
  `admin_a4.py::force_unpublish()` (AA-455) already established — no new identity mechanism.
- **`GET /admin/a4/trust-ramp` itself does NOT write to `audit_log` on every page view** — logging
  every read as a "suggestion given" event would be a new precedent this repo's read endpoints
  don't follow anywhere (every other A4 read endpoint uses `structlog.logger.info` for
  observability, `audit_log` is reserved for actual gate/approval DECISIONS). The issue's #4 ask
  ("ghi log mỗi lần gợi ý được đưa ra") is read as "log the decision made about a surfaced
  suggestion" (approve/skip, both now logged) rather than "log every time an admin's browser
  happens to fetch the page" — flagged explicitly in the Linear comment as an interpretation
  choice in case Nghiep meant literal view-logging.
- **No new migration.** Reuses `acp_deliver.packets` (094/106), `acp_shared.content_metric_snapshot`
  (112), and `acp_shared.audit_log` (030) — all already live. Latest migration in this repo remains
  144 (`route_versioning.sql`).

## Changed

- `services/acp_produce/trust_ramp.py`: added `compute_tenant_ramp_signals()` (weeks_active +
  engagement_ok per tenant_id, 2 read-only queries) and `compute_ramp_suggestion()` (per-packet:
  fetches the packet, computes signals, calls the existing pure `suggest_ramp_transition()`,
  returns a dict). `suggest_ramp_transition()`/`confirm_ramp_transition()` themselves: UNCHANGED.
- `api/routers/admin_a4.py`: `get_trust_ramp()` extended to include the 4 new fields per row (still
  a pure read, no writes). Two new endpoints: `approve_ramp_suggestion()`, `skip_ramp_suggestion()`.
- `frontend/app/admin/a4-oversight/page.tsx`: `TrustRampRow` interface gains the 4 new fields;
  `TrustRampSection` renders an "Approve"/"Skip" badge+button pair on eligible rows, section
  description updated (was: "no auto-suggested next level... not wired into any live process
  yet").
- `tests/unit/test_aa464_trust_ramp_suggestion.py` (new): unit tests for the 2 new trust_ramp.py
  functions (mocked db, matching `test_aa365_trust_ramp.py`'s own convention) + the 2 new router
  endpoints.

## Tradeoffs

- Computing `engagement_ok` tenant-wide (not per-packet/per-week) means a tenant with one
  excellent week and several mediocre ones gets one blended signal, same coarseness the aamc
  prototype's own G3 gate had (a single tenant-level `engagement_ok` bool, never per-period) — not
  a regression introduced here, this build stays at the same granularity the ported design
  already had.
- On-demand computation means opening the Trust Ramp panel triggers 1 extra read query per
  distinct tenant on the page (weeks_active) + 1 more (engagement_ok) — at current real data
  volumes (few tenants, few packets) this is negligible; flagged in case tenant count grows large
  enough to warrant caching later (no premature optimization added here).

## Live Verify (pre-merge, 06/09/2026)

Real HTTP-level verify of the new endpoints needs a deploy (PR #317 not merged yet) — see "Still
needs post-deploy verification" below. What WAS verified pre-merge, against the real live dev DB
via the S3-mediated ECS exec pattern (`aa365-admin`, account 005097885195, ECS `aa-cis-dev-api`
task def `:221` running, RDS `available` — confirmed via `aws sts get-caller-identity` +
`aws ecs describe-services` before touching anything):

- **Real schema confirmed matches code assumptions exactly**: `acp_deliver.packets` columns
  (`packet_id uuid, tenant_id text, year integer, week smallint, publish_mode text, status text,
  created_at/delivered_at timestamptz, month smallint`) and `acp_shared.content_metric_snapshot`
  columns (`snapshot_id uuid, tenant_id uuid, piece_id text, reach/engagement/clicks integer,
  entered_by text, entered_at/created_at timestamptz`) — read via
  `information_schema.columns`, not assumed.
- **Real tenant found with real packets**: `tenant_id=00000000-0000-0000-0000-000000000001`
  (`shared.tenants` confirms this is `aa_internal`/"Adventure Asia Internal") has 4 real
  `acp_deliver.packets` rows, all `publish_mode='propose_only'`.
- **Negative path (real, as-found data)**: `compute_tenant_weeks_active()` returned `4`,
  matching a raw `GROUP BY tenant_id` count exactly. `acp_shared.content_metric_snapshot` had
  **0 rows in the entire database** at the time of this check — `compute_tenant_engagement_ok()`
  correctly returned `False` (fail-closed, 0 real data points), and all 4 real packets correctly
  came back `eligible=False` (`suggested_mode == current publish_mode`).
- **Positive path, real data, cleaned up after**: aa_internal had 135 real `acp_deliver.pieces`
  rows (10 for this tenant) to attach to. Inserted 3 TEMPORARY real
  `content_metric_snapshot` rows (`entered_by='aa464-live-verify-temp'`, `reach=100
  engagement=20` → 20%, well above the 5% `ENGAGEMENT_RATE_BASELINE`) against 3 real, distinct,
  pre-existing `piece_id`s for this tenant. Re-ran the signal computations:
  `engagement_ok` flipped to `True` (from exactly 3 real reach-bearing snapshots — the
  `CONFIDENCE_ATOM_MIN_POSTS` gate, not mocked), and the real packet
  `6cadcbaa-ddaf-4ff4-88ef-740a06054733` came back `eligible=True`,
  `suggested_mode='approve_to_publish'` — the exact mechanism `suggest_ramp_transition()` was
  always supposed to drive, now proven end-to-end against real rows for the first time.
  **Cleaned up immediately after**: deleted all 3 temporary snapshot rows in the same script run
  (try/finally), re-checked count — `content_metric_snapshot` back to 0 rows, confirmed the DB
  was left exactly as found. Scratch verify scripts deleted from the S3 scripts bucket afterward
  (`aws s3 rm`, confirmed gone via a follow-up listing).
- **`acp_shared.audit_log` action-name collision check**: real, current action distribution is
  `trip_reallocation_suggestion: 4`, `agency.onboard: 1` — confirms `publish_mode_transition`
  and the new `ramp_suggestion_skipped` do not already exist under some other meaning (0 rows),
  and matches the issue's own "transition log currently 0 rows" observation exactly.
- **Deliberately NOT done**: did not call `confirm_ramp_transition()` (the approve action)
  against this real packet — that function is pre-existing, unchanged code already live-tested
  by AA-365 itself (not new logic from this issue), and calling it here would mutate
  `aa_internal`'s real `publish_mode` with no clean way to prove the DB back to its exact prior
  state (unlike the snapshot rows, which are freely insertable/deletable test data). The NEW
  code this PR actually adds — `GET /trust-ramp`'s new fields and the 2 new endpoints
  themselves — was verified at the query/logic level above (identical SQL + Python, run for
  real), not yet via an actual HTTP call through a deployed task, since PR #317 isn't merged.

**Still needs post-deploy verification** (flagged clearly, per this repo's own established
pattern of separating pre-merge function-level verify from post-deploy HTTP verify): a real
`GET /admin/a4/trust-ramp` HTTP call showing the new fields in the actual JSON response, and a
real `POST .../approve` + `POST .../skip` HTTP round-trip against a genuinely eligible packet
(the same temporary-snapshot-then-cleanup technique used above can be reused post-deploy to
manufacture a real eligible packet for that test).

## Should know

- `suggest_ramp_transition()` and `confirm_ramp_transition()` are BOTH unchanged — this build is
  entirely new callers + new signal-computation, no edits to the ramp logic itself.
- The BOFU hard-block (`packet_has_bofu_piece()`) is untouched and still applies inside
  `confirm_ramp_transition()` — a BOFU-carrying packet can be "eligible" per the suggestion (the
  suggestion logic doesn't know about BOFU) but will still be rejected (409) if an admin tries to
  approve it into `veto_window_auto`, exactly as it already would for any other caller.
- `admin_produce.py`/`approve_gate_c()` referenced in `packets.py`'s docstring does not exist —
  don't go looking for it if touching this area again; that was AA-365's own aspirational note,
  never built, and this issue's new endpoints are the first real callers of `confirm_ramp_transition()`.
