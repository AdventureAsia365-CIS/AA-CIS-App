## AA-82: Migration brand_brief_extensions

### Decisions
- Decision: All columns from the AA-82 spec (brand_type, core_idea, customer_segment, customer_mindset, voice_examples JSONB, source_docx_s3_key) were already added by migration 018_brand_identity_columns.sql (AA-85 session). Migration 019 only creates tenant_brand_rule_versions.
- Decision: source_type CHECK constraint uses ('manual','docx_parse') as specified.
- Decision: tenant_id is VARCHAR(50) REFERENCES shared.tenant_brand_rules(tenant_id) per spec — not UUID, matching tenant_brand_rules PK type.

### Changed
- 019 is shorter than originally planned — no ADD COLUMN statements needed.

### Tradeoffs
- Creating tenant_brand_rule_versions now (before docx_parse pipeline exists) is forward-compatible. The table just stays empty until AA-83/docx upload feature lands.

### Should know
- Do not re-apply 018 — it is already in dev DB (verified AA-85 session, 19/05/2026).
- tenant_brand_rule_versions.snapshot JSONB stores the full brand rules row at version time — suitable for diff/audit UI.
