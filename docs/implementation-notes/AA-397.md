# AA-397/AA-398 (AA-CIS-App side) — acc3 làm Bedrock satellite chính

## Bước 1 — Invoke-model test thật qua acc3 (STOPPED here — không đi tiếp)

### Decisions
- Test chạy đúng chain thật sẽ dùng trong production: qua ECS exec (S3-mediated,
  container `api` thật, cùng identity `aa-cis-dev-ecs-task-role` mà pipeline code
  sẽ dùng) — không chạy từ terminal cá nhân/role khác, để loại trừ sai khác môi
  trường giữa test và production.
- Dùng `ACC3_ROLE_ARN`/`ACC3_EXTERNAL_ID` lấy từ Terraform ĐÃ APPLY thật (không
  đoán): `accounts/acc3-bedrock/main.tf` + `accounts/aa365/bedrock_satellite_assume.tf`
  trong repo AA-CIS-Infra (PR #22, #24, cả 2 đã merge main). Verify sống trước khi
  chạy test: `aws iam get-role-policy --role-name aa-cis-dev-ecs-task-role
  --policy-name aa-cis-dev-ecs-assume-bedrock-invoker --profile aa365-admin` —
  xác nhận statement `AssumeSatelliteBedrockInvokerAcc3` đã có thật trên AWS
  (không chỉ trong code đã merge — AA-CIS-Infra's docs/implementation-notes/AA-397.md
  từng ghi rõ statement này "CHƯA apply" ở một thời điểm, nên không thể tin
  Linear status "Done" hay chỉ đọc code — phải verify live).
- Script self-daemonize (double-fork + setsid, ghi log ra `/tmp/aa397_result.log`
  trong container) — interactive ECS exec session chết giữa chừng
  (`Cannot perform start session: EOF`) trước khi in hết output, kể cả khi script
  đã chạy xong và ghi log đầy đủ (đây là giới hạn của SSM session streaming qua
  harness, không phải lỗi script). Đọc lại log bằng cách upload lên S3 rồi
  download về, không tin trực tiếp output `cat` qua session (bị cắt giữa chừng ở
  lần đầu).

### Kết quả — nguyên văn cả 2 model

**Sonnet 4.6 (acc3, `global.anthropic.claude-sonnet-4-6`): SUCCESS (1124ms)**
```json
{
  "model": "claude-sonnet-4-6",
  "id": "msg_bdrk_01SyqpfHNjmscz81f8DeYadH",
  "content": [{"type": "text", "text": "OK"}],
  "stop_reason": "end_turn",
  "usage": {"input_tokens": 12, "output_tokens": 4}
}
```

**Haiku 4.5 (acc3, `global.anthropic.claude-haiku-4-5-20251001-v1:0`): FAILED**
```
AccessDeniedException: Model access is denied due to IAM user or service role is
not authorized to perform the required AWS Marketplace actions
(aws-marketplace:ViewSubscriptions, aws-marketplace:Subscribe) to enable access
to this model. Refer to the Amazon Bedrock documentation for further details.
Your AWS Marketplace subscription for this model cannot be completed at this
time. If you recently fixed this issue, try again after 2 minutes.
```
(HTTP 403, RequestId `6b7d9435-a2fa-4804-9d80-45a415318453`, 2026-08-12T05:55:34Z)

### Should know — STOP, không code Bước 2
- IAM/trust/AssumeRole chain hoạt động đúng 100% (AssumeRole + Sonnet invoke đều
  pass) — vấn đề KHÔNG phải ở Terraform hay ở role, mà ở Bedrock model access
  cấp account: acc3 chưa có Marketplace subscription cho riêng model Haiku 4.5.
  Sonnet 4.6 đã có access (khớp STEP 0 discovery nói cross-region inference
  profile "trùng ID với acc1" nhưng KHÔNG đồng nghĩa cùng access — account acc3
  là account riêng, model access (AWS Marketplace subscription) không tự động
  kế thừa hay đi kèm profile tồn tại).
- Theo đúng điều kiện Nghiep đặt ra trước khi bắt đầu: AccessDeniedException ở
  BẤT KỲ model nào → dừng ngay, KHÔNG code routing (Bước 2-5), KHÔNG sửa 7 điểm
  gọi (Bước 3), KHÔNG đổi models.py (Bước 4), KHÔNG viết migration (Bước 5),
  KHÔNG tạo Linear issue/branch/PR (Bước 7). Toàn bộ các bước đó đang PENDING
  chờ quyết định của Nghiep.
- Cần Nghiep tự vào AWS Console (Bedrock → Model access) trên account 786888028788
  (acc3), request access cho Anthropic Claude Haiku 4.5, đợi được duyệt (thường
  vài phút với model access tự động, nhưng thông báo "try again after 2 minutes"
  gợi ý có thể cần thao tác Marketplace subscription thủ công lần đầu — khác hành
  vi model access thông thường).
- Nếu Nghiep chỉ muốn dùng Sonnet 4.6 qua acc3 (bỏ Haiku tier trên acc3, giữ Haiku
  vẫn đi qua acc1 hoặc route khác) — đó là một hướng khác với scope gốc AA-397
  (chain 6 tầng T1→T1.5a(acc3)→T1.5b(acc1)→T2→T2.5a(acc3)→T2.5b(acc1)→T3 giả định
  CẢ 2 model có access acc3). Cần Nghiep quyết định hướng nào trước khi code tiếp,
  không tự chọn.

### Tradeoffs
- Không xoá script/log test khỏi `s3://aa-cis-bronze-005097885195/scripts/`
  (`aa397_acc3_invoke_test.py`, `aa397_result.log`) — không chứa secret, chi phí
  lưu trữ không đáng kể, giữ lại làm bằng chứng nếu cần đối chiếu sau.

## Bước 1 (retest) — sau khi Nghiep enable Marketplace subscription

- Root cause xác nhận đúng như điều tra: acc3 chưa từng subscribe bất kỳ sản phẩm
  Marketplace nào (service-linked role `AWSServiceRoleForAWSMarketplace` không
  tồn tại trước đó) — không phải IAM (AdministratorAccess gốc, `Action:"*"`,
  không exclude, không boundary/inline/group) và không phải SCP (acc3 đứng độc
  lập, `AWSOrganizationsNotInUseException`).
- Nghiep tự subscribe qua Console: "Claude Haiku 4.5 (Amazon Bedrock Edition)"
  Active trên acc3, Agreement `agmt-2fg3q7k1rhilq9tr6cclhpkmp`, service start
  2026-08-12 12:58 UTC+07.
- Retest lại **cả 2 model** (không giả định chỉ Haiku cần test lại) qua đúng
  chain production (ECS task role → AssumeRole `AA3-Bedrock-Invoker` → invoke
  Bedrock us-west-1), cùng script đã dùng lần đầu:

**Sonnet 4.6 (acc3): SUCCESS (967ms)**
```json
{"model": "claude-sonnet-4-6", "content": [{"type": "text", "text": "OK"}], "stop_reason": "end_turn", "usage": {"input_tokens": 12, "output_tokens": 4}}
```

**Haiku 4.5 (acc3): SUCCESS (851ms)**
```json
{"model": "claude-haiku-4-5-20251001", "content": [{"type": "text", "text": "OK"}], "stop_reason": "end_turn", "usage": {"input_tokens": 12, "output_tokens": 4}}
```

- Cả 2 model pass → điều kiện đủ để tiến sang Bước 2-8 (routing chain thật).
  Tiếp tục ghi incremental bên dưới khi code các bước đó.

## Bước 2 — bedrock_satellite.py + client.py (chain 6 tầng)

### Decisions
- Session cache đổi từ 1 biến global (`_cached_session`) sang dict theo account
  (`_cached_sessions`/`_cached_session_expiry`, key "acc1"/"acc3") — mỗi account
  AssumeRole/refresh độc lập, không đụng nhau. `_SATELLITE_ACCOUNTS` +
  `_INFERENCE_PROFILES` là 2 dict cấu hình trung tâm — thêm account thứ 3 sau
  này chỉ cần thêm 1 entry mỗi dict, không sửa logic `_get_satellite_session`/
  `invoke_claude`.
- `account` param mặc định `"acc1"` ở MỌI hàm public (`_get_satellite_session`,
  `get_satellite_client`, `invoke_claude`) — giữ nguyên hành vi cũ 100% cho bất
  kỳ caller nào chưa được cập nhật tham số này (quan trọng nhất:
  `v1_atoms.py:428`, xem Bước 3 dưới).
- Thêm log phân loại `AccessDeniedException` (`satellite_access_denied`) khác
  `satellite_invoke_failed` (lỗi khác) trong `invoke_claude()`'s except block —
  CHỈ thêm log, không đổi luồng try/except gốc (vẫn luôn raise
  `BedrockUnavailable` bất kể loại lỗi). Mục đích: đọc CloudWatch sau này phân
  biệt được ngay "model access chưa enable" (như Bước 1 vừa gặp, tự hết sau khi
  Marketplace subscription active) khỏi lỗi thật khác.
- `client.py`'s `generate()`: chain mở rộng T1→T1.5(acc1)→T2→T2.5(acc1)→T3
  thành T1→T1.5a(acc3)→T1.5b(acc1)→T2→T2.5a(acc3)→T2.5b(acc1)→T3 — acc3 thử
  trước, acc1 lùi xuống fallback-của-fallback. Native T1/T2 (acc2) không đổi.

### Should know
- IAM policy trên acc3's `AA3-Bedrock-Invoker` (accounts/aa365/
  bedrock_satellite_assume.tf, AA-CIS-Infra) CHỈ có `bedrock:InvokeModel` +
  `InvokeModelWithResponseStream` — KHÔNG có Batch permissions
  (`CreateModelInvocationJob`/`PassRole`), quyết định cố ý ghi rõ trong
  AA-CIS-Infra's AA-397.md Bước 1. Điều này chặn trực tiếp việc đổi
  `v1_atoms.py:428`'s `get_satellite_client("bedrock")` (Batch control-plane,
  AA-302) sang account="acc3" — xem Bước 3.

## Bước 3 — 7 điểm gọi trực tiếp

### Decisions
- Đọc nguyên văn cả 7 dòng trước khi sửa (không tin số dòng cũ) — phát hiện
  1/7 điểm liệt kê ban đầu (`v1_atoms.py:428`) KHÔNG phải `invoke_claude()` mà
  là `get_satellite_client("bedrock")`, dùng cho Batch control-plane (AA-302's
  atom-decompose job), không phải interactive invoke. 6/7 còn lại đúng là
  `invoke_claude()` thật: research.py, generation.py, repair.py, faq.py,
  adapt.py, s1_from_atom.py — cả 6 đã thêm `account="acc3"`.
- **KHÔNG sửa `v1_atoms.py:428`** — do IAM scope thật (xem Bước 2 Should know),
  đổi sang acc3 sẽ AccessDenied thật khi tạo Batch job. Giữ mặc định acc1
  (không truyền `account=` gì cả, dùng default của `get_satellite_client()`).
  Đã báo rõ với Nghiep trước khi quyết định giữ nguyên, không tự ý bỏ qua yêu
  cầu gốc mà không giải thích.
- Sửa luôn các doc comment trong 4 file (adapt.py/faq.py/repair.py/
  s1_from_atom.py) mô tả "Bedrock satellite acc1 Sonnet" → "acc3 (acc1 fallback
  under it)" — không phải "logic khác xung quanh" theo nghĩa hành vi runtime,
  nhưng để lại comment sai sự thật (nói acc1 trong khi code gọi acc3) tệ hơn là
  sửa 1 dòng comment.

## Bước 4 — models.py satellite_used → satellite_account

### Decisions
- Grep lại toàn repo trước khi coi là đủ (không tin số 13 điểm cũ) — tìm được
  đúng các điểm literal-field: models.py (định nghĩa), graph.py x2
  (ContentState type + dict write), v1_pipeline.py (dict read/write + 2 dòng
  comment), admin_pipeline.py x2 (SQL column list + value), 4 test file
  (3 fixture kwarg + 1 integration assertion/docstring). Các hit còn lại sau
  khi sửa xong (log event string `t1_5a_satellite_used`..., DB column name
  `satellite_used` giữ nguyên, comment giải thích rename) đều cố ý, không phải
  sót.
- `admin_pipeline.py`'s INSERT ghi CẢ 2 cột: `satellite_used` (bool, derive từ
  `satellite_account is not None`, giữ backward-compat cho query/dashboard cũ)
  VÀ `satellite_account` (text, giá trị thật) — đúng yêu cầu gốc, không drop
  cột cũ.

## Bước 5 — Migration 100

### Decisions
- Kiểm tra số migration mới nhất bằng `ls api/migrations/ | sort -V | tail`
  thay vì tin số 099 ghi trong CLAUDE.md header (có thể stale) — xác nhận 099
  đúng là mới nhất, migration mới là 100.
- `satellite_account` có CHECK constraint `IN ('acc1', 'acc3')` (cho phép NULL)
  — chặn giá trị rác ngay ở DB layer nếu code sau này gõ nhầm tên account.
- Backfill `satellite_account='acc1' WHERE satellite_used=true` — đây là suy
  luận LỊCH SỬ CHÍNH XÁC duy nhất: mọi row `satellite_used=true` trước AA-397
  chắc chắn đi qua acc1 (acc3 chưa tồn tại lúc đó), không phải phỏng đoán.
- File-only, CHƯA chạy migration lên DB thật — đúng nguyên tắc human-only cho
  schema change (global CLAUDE.md + user's Bước 5 note).

## Bước 6 — Test suite

- 13 unit test (test_aa217/225/226) pass ngay dưới `pyproject.toml`'s
  `asyncio_mode = "strict"` mặc định.
- 3 integration test (`test_compiled_graph_state_propagation.py`) SKIPPED dưới
  strict mode — xác nhận qua `git show HEAD` đây là gap CÓ SẴN từ trước (file
  không có `@pytest.mark.asyncio`/`pytestmark` từ commit gốc AA-281, không
  phải do thay đổi lần này gây ra). Chạy lại với `--asyncio-mode=auto` (chỉ để
  tự verify, không sửa config thật) → cả 3 PASS, xác nhận
  `satellite_account` propagate đúng qua compiled graph thật.
- Tổng: 16/16 pass.

## Bước 7 — Linear + branch + PR

- Issue con mới **AA-399** (con của AA-398, không phải con trực tiếp AA-397 —
  giữ đúng chain phân cấp AA-397→AA-398→AA-399 đã có) — không tái sử dụng
  AA-397/AA-398 (cả 2 đã "Done", tái mở lại issue đã đóng dễ gây nhầm lẫn hơn
  là track scope mới bằng issue mới, đúng pattern AA-397→AA-398 đã làm).
- Branch `pqnghiep1354/aa-399-llminfra-acc3-satellite-routing-chain-that` (tên
  Linear tự gợi ý).
- 5 commit theo nhóm logic riêng (không gộp 1 commit khổng lồ): (1)
  bedrock_satellite.py+client.py, (2) 6 call site, (3) satellite_used→
  satellite_account rename, (4) migration 100, (5) doc này.
- Draft PR — chưa mark ready, chưa merge, chờ Nghiep review + tự apply
  migration 100 lên DB thật.
