# AA-572 — Content Trace (AA-568) fix follow-up: 6 issues

Pure UI fix/enhancement on `/admin/tenant-activity` (frontend/app/admin/tenant-activity/page.tsx).
Per the issue's own instruction, STEP0 not required — no backend/schema change.

## Decisions

- **Points 2 (tenant stats) and 3 (sort) are entirely client-side**, computed/applied on the
  already-loaded `rows` (the endpoint caps at 500 rows, already fetched in full for the table) —
  no new query param, no backend change, matching the issue's explicit scope.
- **Stat header is dynamic, not a fixed set of tiles**: "Total pieces" + one tile per channel that
  actually has ≥1 row in the current (already tenant/channel/status/date-filtered) result set,
  sorted by count descending. A fixed 8-channel grid would show a wall of zero-tiles whenever a
  filter narrows the data — a real breakdown of what's actually on screen is more useful and
  matches "breakdown theo channel" literally.
- **Only Gate count/Retry count/Created at got sort** — the issue's own stated minimum. The other
  7 columns are free-text/categorical (Topic/Tenant/Tour/Channel/Angle/Status/Published) where a
  client-side sort wasn't asked for and adds little value; not extending scope beyond what the
  issue's own "có thể mở rộng nếu hợp lý" invites.
- **Point 5 (lineage) resolved via 6 real per-entity lines**: Tour (always) / Atom (if present) /
  Segment (Segment-sourced picks only) / Route + Hub as TWO separate lines (Route-sourced picks
  only — the old single `sourceLabel()` string literally named both "Route/Hub" in one line,
  `"Route/Hub — {hub} (Day X-Y)"`; genuinely split now) / Slate (always — states whether this
  came via the Slate mechanism at all, vs. the pre-Slate direct-atom picker path). `sourceLabel()`
  helper deleted — no longer called anywhere after this split.
- **Point 6 (sticky header) root cause, more precise than the issue's own guess**: the issue
  suggested the fix was "use `minHeight:0` + `height:"100vh"`, learned from AA-566" — both were
  ALREADY correctly present on this page's own outer scroll container (verified before touching
  anything). The real, additional bug: `ContentTraceTable`'s own wrapper `<div>` had
  `overflowX: "auto"` with `overflowY` left at its default (`visible`) — per the CSS overflow
  spec, that combination forces the browser to compute `overflow-y: auto` too, making this INNER,
  never-actually-scrolling div (its height is unconstrained, so it never clips its own content)
  become the nearest non-visible-overflow ancestor — which is what `position: sticky` scopes
  itself to, regardless of whether that ancestor is the one a human would call "the scroll
  region." The `<th>`'s sticky positioning was therefore anchored to a box that never scrolls, so
  it visually behaved as not-sticky at all. Fixed by moving `overflowX: "auto"` up onto the real
  outer scroll container (`flex:1, minHeight:0, overflowY:"auto"`) and leaving the inner table
  wrapper at the CSS default (`overflow: visible`) — now there is exactly ONE real scrolling
  ancestor, and `<th>{ position: sticky, top: 0 }` correctly anchors to it.

## Changed

- `frontend/app/admin/tenant-activity/page.tsx` — all 6 points, single file (see inline AA-572
  comments at the top of the file and at each specific change for the full reasoning).
- `tests/e2e/aa568-content-trace.spec.ts` — updated the one assertion that hardcoded the old
  Vietnamese "Lý do yêu cầu viết lại" string (now "Retry reason").
- New: `tests/e2e/aa572-content-trace-fixes.spec.ts` — 7 tests, one (or a closely related pair)
  per issue point, all against a real dev server + real deployed backend data.

## Live Verify

Real Playwright, `http://localhost:3000` (frontend dev server) against the real deployed backend
(`.env.local`'s `API_URL=https://api-cis.lumiguides.it.com`) — same "local FE / deployed BE"
pattern this repo has used before. Real admin login (`e2e-test-admin`), real WanderLux Travel /
ExploreAsia Co. data.

**Environment note (unrelated to this issue's own code, but blocked verify until fixed)**: the
dev server's `.next` build cache was stale from before this session — every `/api/*` Next.js
route (including `/api/auth/login`) 404'd at the Next.js routing layer itself (confirmed via
direct `curl`, and confirmed NOT specific to the login route — `/api/admin/tenants` 404'd too).
Fixed by `rm -rf frontend/.next` + restarting `next dev`; not a code bug, not touched further.

All 7 new tests pass, plus the full pre-existing `aa568-content-trace.spec.ts` (6 tests) and
`aa562-content-trace-rename.spec.ts` (5 tests) regression suites — no regressions introduced.

1. **English labels** — `Round N — Retry reason (...)` confirmed rendered; old
   `Lý do yêu cầu viết lại` confirmed absent from the page. Grepped the whole file (+
   `auditPanels.tsx`/`adminUi.tsx`/`AdminSidebar.tsx`) for Vietnamese diacritics first — this was
   the only real UI string; the rest were code comments (not rendered).
2. **Tenant stat header** — screenshot confirms `TOTAL PIECES: 15` + 6 channel tiles summing to
   15 for "All tenants"; re-verified the header updates for real when the Tenant filter narrows
   to WanderLux Travel specifically (asserted the Total tile numerically equals the live
   `tbody tr` row count after the filter response).
3. **Sort** — Created at: first click → strictly non-increasing dates (desc default); second
   click on the same header → strictly non-decreasing (asc), and the two orderings are a real,
   different permutation. Gate count + Retry count: confirmed each produces a monotonic
   (asc-or-desc) ordering on click.
4. **Content no longer height-capped** — computed style asserted `max-height: none`,
   `overflow-y` not `auto`, and `scrollHeight === clientHeight` (no internal clipping) on a real
   approved piece's Content box.
5. **Lineage split** — confirmed on TWO real data shapes: a Segment-sourced pick (`Tour` / `Atom`
   / `Segment` / `Slate` / `Goal`, each its own line, zero "→" concatenation) and a Route-sourced
   Blog pick (`Tour` / `Atom` / `Route: Day 1–5` / `Hub: {hub name}` / `Slate`, confirming Route
   and Hub are genuinely 2 separate lines now, not 1).
6. **Sticky header** — screenshot taken after a real 400px scroll of the page's own scroll
   container shows the `<thead>` row still pinned at the same Y position while body rows have
   visibly scrolled up behind it (one row's text is cut off/overlapping under the sticky header
   in the screenshot — direct visual proof, not just a bounding-box number). Bounding-box Y delta
   before/after scroll asserted `< 20px` (vs. the ~400px a non-sticky header would have moved).

## Post-merge / post-deploy production live-verify

PR #353 merged (`d838744`, squash) → `main`. `Deploy Dev` workflow ran green and triggered the
Vercel deploy hook. Confirmed via the Vercel MCP `get_deployment` on deployment id
`dpl_CgYoTJ8qhisJ68tgVwjCE74jFdRu` (project `aa-cis-frontend`): commit `d838744`, `readyState:
READY`, `target: production`, aliased to `aa-cis.lumiguides.it.com`. (By the time this check ran,
the alias had already advanced past this to AA-573's `450d92e` — a superset that still contains
every AA-572 change, so verifying against the live production URL covers this commit either way.)

Re-ran the full 7-test suite (`tests/e2e/aa572-prod-verify.spec.ts` — a copy of
`aa572-content-trace-fixes.spec.ts` pointed at a separate screenshot dir) with
`BASE_URL=https://aa-cis.lumiguides.it.com` against the real deployed site, real backend data,
real `e2e-test-admin` login (the same account works against prod — `/api/auth/login` proxies to
the same real backend, `api-cis.lumiguides.it.com`, regardless of which frontend deploy serves
the page).

**7/7 passed** against production:

1. English labels — `01-english-retry-label.png`.
2. Tenant stat header, all-tenants + filtered-to-WanderLux — `02a-stat-header-all-tenants.png`,
   `02b-stat-header-wanderlux.png` (visually confirmed: `TOTAL PIECES: 15` + 6 real channel
   tiles for WanderLux Travel specifically, e.g. `INSTAGRAM: 4`, `FACEBOOK: 4`, `BLOG: 3`).
3. Created at sort (asc/desc) — `03-sort-created-at.png`.
4. Gate count / Retry count sort — `04-sort-gate-retry.png`.
5. Content block no fixed max-height — `05-content-no-maxheight.png`.
6. Lineage split into separate lines — `06-lineage-lines.png` (visually confirmed real prod row:
   `Tour: Classic Journey (Sri Lanka)` / `Atom: Anuradhapura — ...` / `Segment: Anuradhapura
   (...)` / `Slate: Picked from the Slate` / `Goal: promotion`, each its own line, zero `→`
   concatenation).
7. Sticky header after scroll — `07-sticky-header-after-scroll.png`.

Screenshots: `tests/e2e/results/aa572-prod/`. Same real production database, real admin session,
real backend as Nghiệp's own post-Done screenshots that originally found these 6 issues — this is
a genuine post-deploy confirmation, not a re-run of the pre-merge local-dev evidence above.

Not set to Done, per the issue's own instruction.
