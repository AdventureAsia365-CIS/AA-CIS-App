## AA-59: Security Hardening — Tasks 1 + 4 + 5

### Decisions
- `security-audit` job uses `|| true` (warn-only) — not blocking CI until all vulns resolved (AA-59 P2 upgrade)
- PYSEC-2024-56 explicitly ignored in pip-audit — known vuln with no upstream fix at time of implementation
- `scan_secrets.sh` excludes `import` keyword from Lambda handler check to avoid false positives on `from shared.secrets import ...` pattern (correct Secrets Manager usage)
- `scripts/` is gitignored globally; used `git add -f` consistent with existing tracked scripts in that dir

### Changed
- Audit Lambda requirements loop covers only `services/acp_brand_brief_parser/requirements.txt` — extend as new Lambda services are added
- `scan_secrets.sh` exclusion list: `get_secret|SECRET_ARN|os.environ|#|import`

### Tradeoffs
- Warn-only pip-audit: reduces noise from transitive deps with no fix available; acceptable short-term — revisit when upgrading deps
- No Gitleaks or Detect-Secrets in CI (heavier tools) — git-secrets covers AWS + custom CIS patterns which are the real risk surface

### Should know
- Task 3 (WAF Terraform) and Lambda DATABASE_URL → Secrets Manager migration deferred to AA-CIS-Infra session (needs terraform plan + apply carefully)
- `setup_git_secrets.sh` is developer-run once, not CI — each dev must run it manually after clone
