# AA-391 — S0->N8 real end-to-end chain verify (Laos + South Korea, TEST_MODE)

STEP 0 (this session): read AA-299/300/301/305/306/320/364/365/367/375/376/377/378/379/380 in
full (Linear + `docs/implementation-notes/*.md`), plus the real code (`v1_atoms.py`,
`admin_atoms.py`, `v1_s1_from_atom.py`, `admin.py` Gate A/B endpoints, `services/acp_planning/
{allocator,quarter,runway}.py`, `services/acp_produce/{slot_runner,pipeline,packets}.py`) before
writing anything. Key finding that reshaped scope: AA-375/377/378/379/380/376 (all Done,
06-07/08/2026) already built and live-verified the N7 slot-runner glue, Slot/run persistence,
keyword_seed fix, CloudWatch IAM, and repair_fn wiring — individually, on ONE hand-picked slot
each time. AA-391's actual gap is that nobody had chained ALL stages in ONE continuous run over
real Laos/Korea data before — not that the individual stages were unproven.

## Decisions

- **File location**: `tests/verify_scripts/aa391_e2e_orchestrator.py`, not the issue text's own
  suggested `scripts/e2e_test_orchestrator.py`. This repo's established convention for this exact
  kind of real-DB, real-Bedrock, non-CI verify script is `tests/verify_scripts/aaNNN_*.py`
  (ADR-2026-037) — AA-364/367/375/376/377/378 all used it. Following the existing convention
  rather than the issue text's guess, same STEP-0 discipline AA-377 itself modeled ("Slot
  phu thuoc run... khop dung signature slot_runner.py da viet").
- **Destination scoping mechanism**: filter the real `Trip` list (`fetch_trips()`) to
  `destination IN ('Laos', 'South Korea')` in Python, BEFORE calling the pure
  `compute_runway_map()`/`compute_quarter_plan()` functions — not via `specials` (force-include
  only, doesn't exclude other destinations) and not a bespoke SQL WHERE clause on
  `v_trip_registry` only this script uses. The script asserts after the fact that
  `quarter_plan.trip_ids` is a subset of the filtered trip_ids, so destination-scoping is a
  verified property of the run, not just an intention.
- **Gate A: skipped, not mocked.** `aa_internal` is confirmed live (STEP 1 of the script) to be a
  pre-existing `is_active=true` tenant — Gate A (`POST /admin/tenants/{id}/gate-a/approve`) exists
  to flip a brand-new tenant's `is_active` false->true during N1 onboarding. Nothing in
  `services/acp_planning/*.py` reads `acp_shared.tenant_onboarding`/Gate A state (confirmed by
  reading every N4-N6 module) — N4-N6 depend on `shared.tenants` and `quarter_plan_version`, not
  on onboarding status. Mocking a gate that isn't in the dependency path would be theater, not
  verification.
- **Gate B: real persisted path, not the in-memory bypass.** Prior verify scripts
  (`aa377_aa378_run_persist_verify.py`) set `quarter_plan.approved = True` directly in memory —
  sufficient for THEIR scope (proving persistence/slot-runner mechanics), but AA-391 explicitly
  asks to prove the real Gate B DB flow. This script calls the real
  `save_quarter_plan_version()` + `approve_quarter_plan_version(version_id, approved_by=
  "TEST_MODE_SCRIPT", pool)` — the exact same functions `POST /admin/quarter-plan` and
  `POST /admin/quarter-plan/{version_id}/approve` call. `approved_by="TEST_MODE_SCRIPT"` is
  written literally into the DB row per the issue's own explicit requirement (never a real name).
- **Gate C / BOFU-pricing**: neither has any code to interact with. Gate C (AA-365) is confirmed
  still 0% code (Backlog) — `deliver_packet()` has no Gate C call. BOFU/pricing classification
  does not exist anywhere in `services/acp_produce/*.py` or `services/acp_planning/*.py` (grepped,
  zero matches) — there is no gate to accidentally auto-approve. Both are recorded as real,
  reportable gaps for whoever builds AA-365 next, not silently worked around here.
- **N3 curation**: implemented as a TEST_MODE bulk auto-star (`UPDATE ... SET starred = TRUE`)
  across every live atom for the Laos/Korea trips, rather than a no-op. Reading
  `fetch_curated_atoms()`/`_eligible_atoms()` confirms `starred` is a weighting signal for N6, not
  a hard membership gate (only `NOT deleted AND NOT is_empty_marker` gates eligibility) — so this
  step is not strictly required for the chain to proceed, but auto-starring exercises the N3 step
  literally, as the issue text allows ("có thể auto-star/approve atom trong TEST_MODE nếu cần").
- **Week scope**: only week=1 of the current month is allocated+produced live. Real Bedrock
  Sonnet + Nova Pro judge + DataForSEO calls stack up fast across weeks x channels x repair
  rounds (AA-367.md's own baseline: up to 6 full attempts for ONE passing piece before repair_fn
  existed). This is a live-run cost-scope decision, not a code limitation — the same
  `allocate_and_persist_week()` mechanism covers weeks 2-4 unchanged.
- **No cleanup.** Every prior ADR-2026-037 verify script deletes its rows because those scripts
  used synthetic/hand-constructed fixtures or forced a single test slot. This script's entire
  purpose is producing real content for real Laos/South Korea tours under the real `aa_internal`
  tenant — the atoms, S1 content, quarter plan version, slots, pieces, and packet it creates are
  real production artifacts, left in place deliberately.

## Real-publish block — confirmed, not assumed

`grep -rniE "wordpress|wp[-_]?api|facebook.*graph.*api|instagram.*api|tiktok.*upload|publish_to|
send_to_tenant|social.*publish|cms.*publish" services/ api/` returns **zero matches** outside
docstrings/tests. `packets.py::deliver_packet()` — the only function anywhere that sets
`packets.status='delivered'` — only calls `write_usage_log()` (internal DB write) and updates
`packets.status`/`delivered_at`; no HTTP call of any kind. `set_publish_mode()` additionally
hard-blocks any `publish_mode` value other than `'propose_only'` via `PublishModeBlockedError`
(AA-364 decision, unrelaxed). This script never calls `set_publish_mode()`. Conclusion: there is
currently no code path in this repo capable of a real publish — nothing exists to comment out or
feature-flag. The script still asserts `publish_mode == 'propose_only'` on the final packet row as
a live, non-assumed check.

## Changed

- New `tests/verify_scripts/aa391_e2e_orchestrator.py` — the orchestrator itself (see its own
  module docstring for the full step-by-step design, gate inventory, and scope decisions,
  duplicated in condensed form above).
- No production code changed. No migration. This issue is a verify-only script, per its own
  Linear text ("Không tự ý bắt đầu bất kỳ issue UI nào").

## Should know (for whoever reads this before AA-387-390 UI work)

- If this run breaks partway, the break point + real input/output at that point is recorded in
  this file's own "Live run" section below (added after execution) — not silently fixed unless
  the fix is a one-line, one-meaning typo/import bug per the issue's own instruction.

## Live run — full chain, real Laos+South Korea data, 09/08/2026

Two attempts. **Attempt 1** ran S1-from-atom on Palmyra X5, hit AA-337's documented 1 req/min
channel throttle mid-run, and was deliberately stopped (see AA-392 — the writer moved to Bedrock
Sonnet). **Attempt 2** (this section) resumed on the same already-decomposed/curated 87 tours
after AA-392 shipped, with real Sonnet, run via `tests/verify_scripts/aa391_e2e_orchestrator.py`.

**Mid-run interruption, unrelated to the chain itself**: attempt 2's ECS task was killed
partway through S1-from-atom by a real ECS deployment — the AA-392 PR's merge to `main`
auto-triggered the repo's own CI/CD pipeline (`push to main -> deploy`), which replaced the
running Fargate task (`task-definition/aa-cis-dev-api:60 -> :61`) mid-verify. Confirmed via
`aws ecs describe-tasks`: `stoppedReason: "Scaling activity initiated by (deployment
ecs-svc/...)"`. Not a chain bug — a real operational hazard worth flagging: **a long-running
verify/production job hosted on the same ECS service as an active deploy pipeline is vulnerable
to being killed by any subsequent merge to `main`, with no isolation between the two.** Resumed
cleanly on the new (already correctly-patched, no manual work needed) task — N0-N2/N3 are
idempotent so nothing was lost, S1-from-atom simply restarted from tour 1.

### Evidence, step by step (independent DB reads, `run_id=737b28f9-69a2-4ed8-bf21-fc01abf938e6`)

| Stage | Real DB evidence |
|---|---|
| N0-N2 decompose | 0 new atoms created, all 87 tours correctly `skipped` (source_hash unchanged) — proves decompose was genuinely already done, not re-faked. |
| N3 curation (TEST_MODE auto-star) | `starred_count == live_count` for all 87 tours (verified via independent read) — every live atom starred. |
| N2b S1-from-atom (Sonnet) | **83/87 passed (95.4%)**, 4 `gate_failed` after 3 attempts each — all 4 failures share one root cause: the entailment checker flagging a real, atom-supported measurement (e.g. "170 metres", "480 metres", "4-kilometre") as a "novel number" on a paraphrased sentence. Not a Sonnet-specific issue (same checker regardless of writer), not fixed here (out of scope — flagged for `services/acp_shared/grounding.py`, same class of finding AA-392 already surfaced once). |
| N4 runway | `compute_runway_map()` ran on all 87 Laos/Korea-filtered trips, no errors. |
| N5 quarter + Gate B | `acp_shared.quarter_plan_version` version `2befe851-9069-494e-8963-43b3f56fd351` persisted `pending` then flipped to `approved`, `approved_by='TEST_MODE_SCRIPT'` (independently re-read after) — real DB Gate B path exercised end to end, unmistakably marked as a test approval. |
| N6 allocate | `fetch_approved_quarter_plan()` read back the exact same `trip_ids` just approved (proves N6 reads DB, not memory). `allocate_and_persist_week()` persisted 3 real slots (`acp_shared.acp_v2_slots`) for week 1 — all 3 for one trip ("Exploring South Korea"), channels blog/facebook/tiktok. |
| **N4-N6 → N7 handoff (the joint Nghiep flagged as highest-risk)** | **Worked correctly.** `fetch_due_slots()` returned the 3 real persisted slots; `run_slot_production()` consumed them with no shape/interface mismatch, resolved the real `Trip`, called real DataForSEO (C1/C2) and got real keyword data. The interface between the two independently-built halves is sound — nothing broke here. |
| N7 slot production | **0/3 pieces produced** — all 3 slots rejected before generation, `kind=no_cta_target` in `acp_shared.unknown_ledger` ("no live CTA target — piece would be a dead end; rejected"). Root cause confirmed via direct query: `acp_deliver.tenant_tour_pages` has **0 rows in the entire database** — this is the exact, already-documented gap from `docs/implementation-notes/AA-364.md`/`AA-367.md` ("F6 fails closed on every real tour today"), re-confirmed at full real scale, not a new regression and not related to AA-392's model change. The demand-law/keyword_seed fix (AA-379) is confirmed working correctly here — real DataForSEO volume was found for the topic (a related PAA showed "~1600/mo") — a *different* gate (F6, missing CTA data) is what blocked these slots, not the AA-375 finding. |
| N8 assembly | `create_packet()` created a real packet (`e66e62fb-6a4a-45c6-a212-46d0f3ae1c9f`), stayed `status='assembling'` with 0 pieces assigned (correct behavior — `assemble_packet()` only ever accepts `status='passed'` pieces, and none existed). |
| Publish block | `publish_mode` stayed `propose_only` throughout (independently re-read) — never delivered. No code exists anywhere in this repo to perform a real publish (re-confirmed via grep, unchanged since attempt 1). |

Note: `acp_contract.s1_from_atom_runs` shows 0 new rows for this run — expected, not a gap. This
script calls `generate_s1_from_atom()` directly (the real production function), not through the
`/v1/s1-from-atom/tours/{id}/generate` router, so the router's own `_log_run()` observability
write (a separate, router-level concern) never fires. The generation itself is real regardless.

### Verdict

**The chain runs thông (end to end, no crash, no architecture break) from S0 through N8,
stopping correctly before publish.** The one real gap found — F6's CTA-target check rejecting
every real slot because `acp_deliver.tenant_tour_pages` has never been populated for any real
tenant/tour — is not new (AA-364.md flagged it 05/08/2026) and is not a chain-wiring problem;
it is a missing real-world data population step nobody has built yet. **This blocks N8 from ever
producing a deliverable packet on real content today**, for any tenant, regardless of writer
model or topic — worth a dedicated issue (populate `tenant_tour_pages` from a real source, e.g.
the tenant's actual published tour page URLs) before AA-365 (Gate C) or any UI work downstream of
N8 packet delivery can mean anything on real data.

### Relevance to AA-387/388/389/345/323/365/390 (per the original AA-391 instruction)

- **AA-365 (Gate C)**: confirmed still correctly out of the loop — `deliver_packet()` has no Gate
  C call. Nothing to build against yet since no packet has ever reached `ready`/`delivered` on
  real content (blocked by the `tenant_tour_pages` gap above, not by Gate C's absence).
- **AA-323/AA-345 (N4-N6/N0-N2 UI)**: no findings here change their scope — the underlying
  service functions all work as those issues would call them.
- **AA-387/388/389/390**: no new findings from this run that change those issues' scope.
- **New, not-yet-tracked finding**: `acp_deliver.tenant_tour_pages` (0 rows, always-empty in
  dev) blocks N8 from ever assembling a real deliverable packet — recommend a new issue before
  any N8/Gate-C UI work, since a UI for a packet that can never contain real content isn't
  verifiable against real data either.
