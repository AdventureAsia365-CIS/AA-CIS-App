# AA-218 — Persist people_also_ask + related_keywords into seo_context

[AA-193·DFS] Blocker for AA-219 / AA-220.

## Decisions
- Migration number = **069** (highest file on disk = 068). DB `schema_versions` max
  to be confirmed at apply time (STEP 6).
- schema_versions INSERT uses the **actual 06x format** seen in 068:
  `INSERT INTO shared.schema_versions (version, description) VALUES ('069', '...') ON CONFLICT DO NOTHING;`
  — NOT the `applied_by/applied_at` columns suggested in the task brief (those columns
  are not used by the 06x migrations).

## Changed
- `seo_context` gains 2 jsonb columns: `people_also_ask`, `related_keywords`.
- `SeoContextRepository.insert` — add both to INSERT columns, values ($11/$12),
  and ON CONFLICT DO UPDATE SET (mirrors keyword_ideas/top_keywords which ARE updated).
- `handler.py` dict — add 2 json.dumps lines from `seo_data` (real key = `related_keywords`).
- `get_tour_seo_context` endpoint — both SELECT branches (country / no-country) gain the
  2 columns; return parsed real values via `_as_list`; drop the "not persisted" note.

## Tradeoffs
- Including the new columns in ON CONFLICT DO UPDATE SET so a cache refresh backfills
  PAA/related on existing rows (consistent with how keyword_ideas refreshes).

## Applied
- Migration 069 applied to RDS (S74) via S3-mediated ECS exec (task f1d16c3a…).
  Verify-after: both jsonb columns present; schema_versions row '069' present.
  Pre-apply schema_versions max numeric = 068.

## Should know
- Real seo_data key is `related_keywords` (not `related`) — S74 driver verify read the
  wrong key; client genuinely returns `related_keywords` with data.
- Endpoint has TWO SELECT branches; both must be updated or no-country tours regress.
