# AA-578 — INTENTIONAL notes + dead route cleanup

## Decisions
- Part 1 (INTENTIONAL comments): placed at the exact query that joins into `published_tours`/
  `raw_tours` (not at the caller), so the comment sits next to the thing a future RLS-rollout
  session would actually be editing. 5 locations: `v1_marketplace.py::_MARKETPLACE_QUERY`,
  `v1_route_hub.py::_tenant_tour_ids()`, `services/acp_shared/slate.py::_tenant_tour_ids()`,
  `tenant_pool.py::_TENANT_TRIP_QUERY`, and the call site in `trip_reallocation.py` (a short
  pointer back to `tenant_pool.py`'s own comment, not a duplicate essay).
- Part 2 (dead route deletion): this reverses AA-511's own "giữ code cũ, chỉ ngưng dùng, không
  xoá" (keep old code, only stop using it, don't delete) convention for this one neighborhood —
  a deliberate, confirmed-dead exception per this session's explicit build instruction, not a
  reversal of that convention generally. `services/acp_planning/quarter.py`'s underlying
  functions (`compute_quarter_plan()`, `save_quarter_plan_version()`,
  `approve_quarter_plan_version()`, `fetch_approved_quarter_plan()`) are NOT deleted — still real
  dependencies of `trip_reallocation.py`'s live `suggest_trip_reallocation()`/
  `confirm_trip_reallocation()`.
- `services/acp_planning/allocator.py::compute_slot_grid()` (the core allocation function) is
  NOT deleted — only `v1_planning.py`'s own dead `get_slot_grid()`/`get_slot_suggestions()`
  route handlers that called it. `compute_slot_grid()` has real, separate callers elsewhere
  (`admin_atoms.py`'s preview-slotgrid, and its own direct unit tests).

## Changed
- `api/routers/v1_planning.py`: deleted `_compute_plan()`, `get_slot_grid()`,
  `get_slot_suggestions()`, `QuarterPlanRequest`, and their 5 route registrations
  (`POST /quarter-plan/preview`, `GET`+`POST /quarter-plan`, `GET /slot-grid`,
  `GET /slot-suggestions`). Removed the now-unused imports these alone pulled in
  (`compute_slot_grid`, `fetch_quarter_lock_status`/`is_quarter_fully_locked`,
  `QuarterPlanNotApprovedError`, `compute_quarter_plan`/`save_quarter_plan_version`/
  `approve_quarter_plan_version`/`fetch_approved_quarter_plan`, `compute_runway_map`,
  `fetch_tenant_trips`/`fetch_tenant_atoms_by_trip`/`fetch_used_atom_ids`,
  `fetch_dfs_relevance_by_tour`) plus one pre-existing dead import (`datetime.date`, unused even
  before this change — same import block, cleaned up alongside it). `_resolve_config()` kept
  (still used by `get_slate()`).
- `tests/unit/test_aa448_v1_planning.py`: removed `TestPreviewQuarterPlan`/
  `TestFinalizeQuarterPlan`/`TestGetQuarterPlan`/`TestGetSlotSuggestions` and the
  `_trip_row`/`_atom_row`/`_plan_side_effect` helpers only they used. Kept
  `TestAuthReusesV1ToursDependency`/`TestMetricsEndpoints`/`TestTripReallocationEndpoints`
  (still-live code).
- 5 files gain the `# INTENTIONAL ... KHÔNG BAO GIỜ chuyển sang RLS pool thật ... xem AA-578`
  comment (Part 1, see Decisions above).

## Tradeoffs
- None of significance — this is a straightforward, evidence-backed deletion (0 frontend caller
  + 0 real CloudWatch traffic since AA-519/AA-522, confirmed in AA-578's own Linear comments)
  plus documentation-only comments elsewhere.

## Should know
- Verify checklist before merge: `trip-reallocation/suggest`+`confirm` still work (same
  dependencies, untouched — confirmed via the full test suite), 0 frontend reference to any
  deleted route (grepped), CI green.
- `docs/implementation-notes/AA-448-t7-content-planning.md`'s own "STOP point"/Gate-B reasoning
  is now partially historical (describes the deleted `POST /quarter-plan` route) — left as-is,
  not rewritten; it documents a real decision that's still true of the underlying mechanism
  `trip_reallocation.py` now exposes instead, just not through that specific route anymore.
