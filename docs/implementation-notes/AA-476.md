# AA-476 — Double-fire fix (S3-trigger Lambda vs Commit endpoint)

## Status
Phần A, B, C coded + tested + pushed. PRs open, waiting for Nghiep's own review + merge (human-
only this round, per explicit instruction — not auto-merge):
- AA-CIS-Infra: `fix/aa-476-remove-s3-ingestion-trigger` → PR #36
- AA-CIS-App: `fix/aa-476-double-fire-and-dashboard-cleanup` → PR #241

`terraform apply` and migration 123 both still WAIT until after PR merge + CI green — not run yet.

Historical backfill (batches already stuck at `'ingesting'` in the real DB): dry-run complete
(read-only), results below. Real UPDATE NOT run — waiting for Nghiep's second confirmation.

## Backfill dry-run results (28/08/2026, read-only, aa-cis-dev-db)
Script: `sync_batch_completion()`'s exact pending-count logic (this task's own fix) applied
read-only against live data, plus a lookup for tours whose LATEST `generated_content` version
is `'rejected'` but `raw_tours.pipeline_status` was never updated to match.

**Step 1 — historical reject backfill (raw_tours.pipeline_status → 'hitl_rejected'):**
Only **1 row**: `tour_id=00164580-b7b2-429f-a185-2f9415d1de11` ("Full day city tour"),
`batch_id=a3183eb2-4f15-4015-b40f-97d3c92e11ee`, currently `pipeline_status='ingested'`,
latest `generated_content` (v1) rejected 2026-08-21T11:59:08Z.

**Step 2 — pipeline_runs.status → 'completed' (re-run completion check on all 16 currently-
`'ingesting'` batches):**

| batch_id | tours_total | pending today | tours fixed by step 1 | pending after | outcome |
|---|---|---|---|---|---|
| 6dd5e658-85ce-4698-9574-70de1684d224 | 1 | 0 | 0 | 0 | → completed |
| dc92e1b5-6896-493a-a03a-55ec16ab0e06 | 1 | 0 | 0 | 0 | → completed |
| a0b806f8-689a-4f78-90b1-5e36a29bb699 | 30 | 0 | 0 | 0 | → completed |
| 4bdd9b22-ea41-4489-bcd6-24de54e545cb | 30 | 0 | 0 | 0 | → completed |
| a3183eb2-4f15-4015-b40f-97d3c92e11ee | 17 | 17 | 1 | 16 | stays ingesting |
| (11 more batches) | — | >0 | 0 | >0 | stays ingesting |

**Summary: 1 raw_tours row updated, 4 pipeline_runs rows flip to 'completed', 12 stay
'ingesting' (genuinely still have unresolved tours — correctly left alone).**

**Real finding, not part of this task's original scope**: the 4 batches that would flip were
ALREADY at `pending=0` under TODAY's (pre-fix) logic too — none of them needed the reject-tour
backfill (`tours_backfilled_in_this_batch=0` for all 4). Meaning `sync_batch_completion()` (or
its precursor logic before this task's refactor) never ran to completion for them even though
every one of their tours was already `'published'`. Candidate root cause (not verified, not
fixed here — flagged for a separate ticket if Nghiep wants it investigated): `process_export()`
has no locking around its read-then-conditionally-write "pending" check — two tours in the same
batch approved/exported near-simultaneously could each read the OTHER as still-pending (classic
check-then-act race), so neither ever observes `pending=0`. The backfill's Step 2 (re-running
the check once, serially, well after the race window) will fix these 4 as a side effect, but
does not address whether the race itself could still happen for a brand-new batch going forward.

Real backfill script prepared (not run): `/tmp/.../scratchpad/aa476_backfill_real.py`
(session-local scratch path) — wraps both steps in one DB transaction (all-or-nothing), reuses
the identical `sync_batch_completion` logic now live in `services/export/handler.py`.

## Backfill REAL run — executed 28/08/2026, Nghiep confirmed (aa-cis-dev-db)
Ran the exact script above via the standard S3-mediated ECS exec pattern, one DB transaction,
COMMITTED successfully:
- `raw_tours`: 1 row updated (`tour_id=00164580-b7b2-429f-a185-2f9415d1de11` → `pipeline_status
  = 'hitl_rejected'`).
- `pipeline_runs`: 4 rows updated (`6dd5e658-...`, `dc92e1b5-...`, `a0b806f8-...`,
  `4bdd9b22-...` → `status='completed'`, `completed_at` stamped).

**Independent post-run verification (separate read-only script, not a re-run of the same
script)**:
1. Direct `SELECT` of all 5 rows — every value matches the intended target exactly (confirmed
   via ECS exec, see session transcript).
2. Re-ran the dry-run's detection queries: **0** tours still need reject-backfill (was 1), **0**
   unexpected `pending=0`-but-still-`'ingesting'` batches remain in the group that was just
   processed (there are still 12 batches at `'ingesting'` total, matching the dry-run's
   prediction exactly — all 12 have genuinely unresolved tours, untouched by design).

Scratch scripts (`aa476_backfill_dryrun.py`, `aa476_count.py`, `aa476_backfill_real.py`,
`aa476_verify.py`) were each uploaded to `s3://aa-cis-bronze-005097885195/scripts/` and deleted
immediately after use — none left behind.

**The race-condition candidate noted above (4 batches never auto-completing despite being fully
published) is still unverified/unfixed** — this backfill corrected the historical data but did
not investigate or patch whatever caused the original completion check to never fire for them.
Flagged for a separate ticket if Nghiep wants it pursued.

## Terraform apply + migration 123 — executed 28/08/2026, Nghiep ran `Terraform Apply Prod`

**Terraform apply verified (read-only AWS checks, post-apply):**
- `aws s3api get-bucket-notification-configuration --bucket aa-cis-bronze-005097885195` →
  empty — no notification config on the bucket at all (the `raw-inbox/` trigger is gone).
- `aws lambda get-function --function-name aa-cis-dev-ingestion` → still exists, `State: Active`
  (kept, as intended — not deleted).
- `aws lambda get-policy --function-name aa-cis-dev-ingestion` →
  `ResourceNotFoundException: The resource you requested does not exist` — confirms the
  `AllowS3Invoke` resource policy statement (`aws_lambda_permission.s3_invoke_ingestion`) is
  gone; nothing can invoke this Lambda via `s3.amazonaws.com` anymore.

**Real double-fire test (the one this whole task exists to fix):** uploaded a real test file to
`s3://aa-cis-bronze-005097885195/raw-inbox/aa476-verify/aa476_trigger_test.xlsx` at
`2026-08-28T09:37:14Z`. Checked `/aws/lambda/aa-cis-dev-ingestion`'s CloudWatch log streams
(`describe-log-streams`, ordered by `LastEventTime` desc) — the most recent stream's last event
is from **2026/08/17**, well before the test upload. **Zero new invocation** — the S3 PutObject
no longer fires the Lambda. Test file deleted from S3 immediately after.

**Migration 123 applied for real** (S3-mediated ECS exec, one transaction): ran cleanly, no
error (as predicted — `IF NOT EXISTS` no-op since the index already existed). Verified by
re-reading both:
- `shared.schema_versions` now has a `version='123'` row (`applied_at` timestamped).
- `pg_indexes` definition for `idx_raw_sources_file_hash` is byte-identical to what it was
  before this migration ran — confirms no accidental schema drift, purely a git-history
  registration as intended.

**AA-476 is now fully closed**: Phần A (audit) → Phần B (Terraform, applied + verified) →
Phần C (code fix, deployed + verified; dashboard bugs #1/#2/#11 addressed; migration 123 applied
+ verified; historical backfill run + verified). Two known follow-ups deliberately left open for
separate tickets: (1) the 2 STEP0 gaps (intra-file tour dedup; dry_run/commit hash-dedup
mismatch) and (2) the race-condition candidate behind the 4 never-auto-completed batches.

## Decisions
- Treated `tour_content_versions` as out of scope for "does raw_tours have versioning" —
  confirmed via migration 026 it was S1 ACPv1-rewrite-output versioning (FK'd to
  `acp_shared.acp_runs`), not raw-ingest versioning, and it was already dropped
  (migration 121, AA-477, 27/08/2026 — confirmed empty). The user's task text cited
  "ADR-016" for this table; ADR-2026-016 is actually the unrelated async run-tour decouple
  ADR (docs/implementation-notes/AA-223.md) — flagged as a likely mis-citation, not
  silently corrected.
- Found the REAL raw_tours versioning mechanism instead: `source_group_id` /
  `source_version` / `source_status` columns on `raw_tours` itself, driven by
  `silver_aa_internal.upload_staging` + `POST /admin/upload-staging/{id}/decide`
  (api/routers/admin_pipeline.py:1295-1434). This is live and wired to a real frontend
  step (frontend/app/admin/upload/page.tsx Step 5, `DuplicateReviewStep`) — verified
  reachable, not just present in code.

## Changed

### Phần B — Terraform (AA-CIS-Infra, `modules/lambda/main.tf`)
- Removed `aws_lambda_permission.s3_invoke_ingestion` + `aws_s3_bucket_notification.bronze_trigger`
  entirely. `aws_lambda_function.ingestion` itself is KEPT (now unreachable — no other invoker) —
  deliberate, not folded into AA-182's SFN-bypass cleanup (that ticket's 28/08 comment explicitly
  scoped ingestion out of it, handled here instead). Decision left for later: delete the Lambda
  outright, or keep the code dormant.
- `terraform validate` passed. Could NOT run `terraform plan` — the `accounts/aa365` backend role
  needs interactive MFA (`AssumeRoleTokenProvider session option not set`), which the harness's
  Bash tool can't supply (same limitation as `reference_mfa_getpass_non_tty_bash` memory). Nghiep
  needs to run `terraform plan` themselves (via `!` prefix or a real terminal) before apply.

### Phần C.1 — pipeline_runs.status stuck at 'ingesting' (bug #1, folds in Phần A's gap #3)
Root cause (found going deeper than Phần A's initial framing): `sync_batch_completion()`
(new name, extracted from `process_export()`'s inline logic — `services/export/handler.py`) is
the ONLY place `pipeline_runs.status` ever advances past `'ingesting'` anywhere in the codebase.
It only ever recognized `raw_tours.pipeline_status = 'published'` as "this tour is done" — but
`reject_review()` (api/routers/v1_pipeline.py) NEVER touched `raw_tours.pipeline_status` at all
(only `review_queue.review_status` + `generated_content.status`), so a rejected tour stayed
`'ingested'` forever and the completion check's `pending` count never reached 0.
- `sync_batch_completion(conn, batch_id, silver=...)`: same tours_passed/pending/status-flip
  logic as before, but the pending query now excludes `'hitl_rejected'` and `'failed'` too
  (`_TERMINAL_TOUR_STATUSES`), not just `'published'`.
- New `mark_tour_rejected(conn, tour_id)`: sets `raw_tours.pipeline_status = 'hitl_rejected'`
  then calls `sync_batch_completion`. Wired into `reject_review()` right after its existing
  `generated_content.status = 'rejected'` UPDATE.
- This is the single shared fix point — Phần A's gap #3 and the original bug #1 are the same
  defect (per Nghiep's instruction not to patch them separately); no second code path added.
- Tests: `tests/unit/test_aa476_batch_completion.py` (new, 5 tests, drives the real functions)
  + updated `tests/unit/test_aa211_212_gate_hitl.py`'s 2 reject tests (extra fetchrow in the
  mocked sequence for the new tour_id lookup, `mark_tour_rejected` patched).
- **Known follow-up, NOT done here** (would require touching real data, blocked by "no apply"):
  any batch ALREADY stuck at `'ingesting'` in the live DB from before this fix ships stays stuck
  — this only fixes the code path going forward. A one-time backfill (re-run
  `sync_batch_completion` for every existing `batch_id`) would be needed to fix historical rows,
  not attempted in this task.

### Phần C.2 — dashboard bucket/filter (bug #2)
No separate code change. Confirmed via `docs/claude_audit/AA-438-05-dashboard-cleanup-verified.md`
(pre-existing audit, line 79) that this bug's own documented root cause IS bug #1 — the
`status != 'ingesting'` filter in `GET /admin/metrics`'s `daily` query permanently excluded any
batch stuck at `'ingesting'`, "always empty even during real rewriting." Fixing C.1 fixes this
filter's behavior for all NEW batches going forward. Historical batches need the same backfill
noted above to show up correctly. Bucketing by `started_at` itself was not found to be
independently wrong — left unchanged.

### Phần C.3 — "LLM Calls" mislabeled (bug #11)
STEP0 for this sub-task found the described fix ("count real LLM calls instead of rows") is not
safely achievable without touching write-side instrumentation, which conflicts with this batch's
"don't touch data-writing logic" constraint — see Tradeoffs. Presented 3 options to Nghiep via
AskUserQuestion; **relabel, don't touch the number** was chosen. Changed:
- `api/routers/admin_pipeline.py`: added explanatory comments at the `models` query and the
  `llm_calls` fetchval (no SQL/logic change — same `COUNT(*)` as before).
- `frontend/app/admin/dashboard/page.tsx`: "LLM Calls" → "Content Versions" (top metric card,
  with a `sub` line explaining the caveat); "Model Usage" table headers "Calls"/"Cost/Call" →
  "Versions"/"Cost/Version", plus an inline caveat note above the table. Backend JSON keys
  (`llm_calls`, `calls`, `cost_per_call`) deliberately left unchanged — pure label fix, no API
  contract change.

### Phần C.4 — file_hash unique index migration
New `api/migrations/123_raw_sources_file_hash_unique_index.sql`. Definition copied verbatim from
a **read-only** query against the live dev DB (`pg_indexes`, via the standard S3-mediated ECS
exec pattern — no data touched, script deleted from S3 after):
```sql
CREATE UNIQUE INDEX idx_raw_sources_file_hash ON silver_aa_internal.raw_sources
    USING btree (file_hash) WHERE (file_hash IS NOT NULL)
```
It's a PARTIAL unique index (guards only non-NULL file_hash), not a plain UNIQUE constraint —
migration reproduces that exactly. Uses `CREATE UNIQUE INDEX IF NOT EXISTS` so it's a safe no-op
on the current DB. **NOT run against the real DB** — per "no apply" instruction, only verified by
comparing the migration's SQL text against the live `pg_indexes` output, not by executing it.

## Tradeoffs
- **Bug #11**: chose "relabel honestly" over "add instrumentation for a real count." STEP0 found
  `brand_audit_status` (the most promising existing signal) is NOT a reliable "call happened"
  proxy — `brand_audit_node` sets it to `'pass'` on 3 separate no-LLM-call skip branches (missing
  `generated`, AA-206 judge-result reuse, missing `OPENAI_API_KEY` — the last is ~100% of rows
  right now per the AA-351 zero-credit finding). Building a "more accurate" count from existing
  columns risked shipping a confidently-wrong number, worse than an honestly-labeled undercount.
  A real fix needs a persisted per-node call counter (`generated_content.metadata.llm_call_count`
  or similar), which is a write-logic change explicitly out of scope for this batch.

## Should know
- Two real, reachable gaps found during STEP0 that are NOT part of double-fire itself.
  Nghiep confirmed (28/08/2026): do NOT fix in this task — create a separate issue later.
  Left exactly as found, no code touched for either:
  1. Intra-file tour duplicates (two rows in the SAME Excel upload with identical
     src_name+provider) are invisible to both the file-hash dedup and the tour-match
     dedup — both rows get inserted as brand-new `raw_tours` with no staging/review.
  2. dry_run preview's file-hash duplicate check requires filename match too
     (admin_pipeline.py:1113-1126: `if incoming_name == stored_name`), but the real
     commit path (`process_file()`, hash-only, services/ingestion/handler.py:97-100)
     blocks on hash alone. A same-content/different-filename re-upload previews as
     "N tours ready", then silently commits as "duplicate" / 0 tours saved with no
     distinguishing UI message (frontend shows a plain green "0 tours saved").
- CLAUDE.md's "EXCEL PARSER RULES: Dedup by src_name + provider when no tour_id_external"
  is stale — `ExcelParser.parse()` (services/ingestion/excel_parser.py) has no dedup logic
  at all; the real dedup happens downstream in `process_file()` against the DB, not in the
  parser.
- Confirmed dashboard bug #1 (pipeline_runs.status stuck at 'ingesting') is real and
  visible directly in `process_file()`: it INSERTs `status='ingesting'` and never advances
  it on the success path — only on exception (→ `ingest_failed`). Some other downstream
  step must be relied on to move it forward; not yet traced (Phần C scope).
