# AA-494 — Build Steps 1-4: schema migration, atom-availability rule, T7→T8 slot-grid wiring

## Build Step 1: choose_angle() unset-others prerequisite fix

**Scope of this PR:** ONLY the `choose_angle()` bug fix identified as AA-494's mandatory
prerequisite (Decision 3 of `docs/claude_tasks/AA-494-design-atom-angle-piece-reuse.md`).
No schema changes, no status-value redesign, no other part of AA-494's design. See that
design doc for the full plan this is step 1 of.

## Decisions

- Fixed via an explicit second `UPDATE ... SET chosen = false WHERE request_id = $1 AND
  idx != $2` in the same transaction as the existing `SET chosen = true`, rather than a
  single `SET chosen = (idx = $2) WHERE request_id = $1` statement. Kept as two
  statements so the existing "no angle option idx=N for this request" existence check
  (via the first statement's `UPDATE 0` result) stays intact unchanged — a single
  combined statement would always report a row count equal to however many options
  exist for the request, losing that specific error signal.
- Did not touch the `status == 'pending_choice'` guard at the top of `choose_angle()`.
  That guard is what makes this bug currently harmless (nothing can call `choose_angle()`
  a second time for the same request in the live code path today) — loosening it is
  explicitly Decision 3's job, a separate future PR, not this one.

## Changed

- `services/acp_angle_gate/service.py::choose_angle()` — added the unset-others UPDATE.
- `tests/unit/test_aa449_angle_gate_service.py`:
  - `TestChooseAngle::test_happy_path_sets_chosen_and_approved` — updated
    `conn.execute.side_effect` from 2 to 3 items (the new unset call is now a 3rd
    `execute()` call) and added an assertion that the unset call fired with the correct
    `(request_id, idx)` args.
  - New `TestChooseAngle::test_choosing_twice_unsets_previous_choice` — the build task's
    required "exercise `choose_angle()` twice" test. The existing status guard blocks a
    real second call today, so this test uses a new stateful fake connection
    (`_StatefulAngleOptionConn`) that tracks `angle_gate_option.chosen` state directly
    across two calls (resetting `request_status` back to `pending_choice` between them,
    simulating a future loosened guard) — verifies only the most-recently-chosen idx
    ends up `chosen=True` after the second call. This is a data-state assertion, not a
    call-count assertion, since that's what the bug actually was.

## Tradeoffs

- The new test double (`_StatefulAngleOptionConn`) is a different mocking style than
  this file's existing `AsyncMock` + `side_effect` convention. Necessary because the
  bug is about final DB state across 2 sequential calls, which a call-count/args-only
  mock can't express — accepted the inconsistency in mocking style for a meaningfully
  stronger test rather than force-fitting the existing pattern.
- No migration, no live schema touch — this is a pure logic fix, matches the build
  task's explicit requirement.

## Should know

- This PR does NOT unlock re-choosing a different angle after `approved` — that still
  requires Decision 3's status-value work (a new `reusable` status value + widening
  `acp_content_writing/service.py::start_write()`'s guard), not built here. This PR only
  removes the landmine that would otherwise corrupt T9's angle-read once that future
  work ships.
- Full 1667-test suite run: 1644 passed, 23 pre-existing failures, all unrelated to this
  change (RLS isolation integration tests requiring a live DB connection, `test_run_health`,
  `test_h3_rule_extractor` — none touch `acp_angle_gate`). Confirmed by `git diff --stat`
  that only `services/acp_angle_gate/service.py` and its own test file were touched.
- flake8 clean on both changed files.
## Post-merge / post-deploy record

PR #253 merged (`5d10cf3`, squash, auto-merge — all 5 required checks green). `Deploy Dev`
(run `33253139110`) green — ECS task def `aa-cis-dev-api:161` (was `:160`), `rolloutState=
COMPLETED`, confirmed via `aws ecs describe-services`.

Real live-verify, S3-mediated ECS-exec pattern, script run inside the live `aa-cis-dev-api`
container against `http://localhost:8000` (real app, real `DATABASE_URL`, real JWT minted
with the default `JWT_SECRET` — confirmed no override in the task def's secrets before
minting):

**Tenant note (new finding, not part of this task's own scope):** `test-n1-flow` — the tenant
every prior AA-448/449/450/451 live-verify used — no longer exists in `shared.tenants`. Only 3
tenants remain: `aa_internal`, `exploreasia-co`, `wanderlux-travel`. Of those, only
`wanderlux-travel` (`a1b2c3d4-0001-4000-8000-000000000001`) has live atoms (7,
`acp_contract.tour_atoms`) — used for this live-verify instead. Not investigated further here
(out of this task's scope) — flagging so a future session doesn't waste time looking for
`test-n1-flow` again; it's gone, use `wanderlux-travel`.

1. Normal single-choice flow, unchanged: `POST /v1/angle-gate/requests` → `POST .../goal`
   (real Bedrock call, 3 real angles) → `POST .../choose {idx:0}` → 200, `status=approved`,
   angle 0 `chosen=true`, angles 1/2 `chosen=false` — both the HTTP response and an
   independent DB re-fetch (`SELECT ... angle_gate_option`) agree. Confirms the fix changes
   nothing about the existing, real single-choice path.
2. **The actual fix, proven live against real Postgres + real HTTP**: manually reset
   `angle_gate_request.status` back to `pending_choice` via direct SQL (simulating the future
   `reusable`-style status Decision 3 will add — the only way to exercise a 2nd real
   `choose_angle()` call today), then `POST .../choose {idx:2}` again through the real
   endpoint. Result: 200, `status=approved`, and — the actual bug this PR fixes — angle 0
   flipped to `chosen=false` and angle 2 became `chosen=true`. Independent DB re-fetch after
   the second call confirmed the exact same state
   (`[{idx:0,chosen:false},{idx:1,chosen:false},{idx:2,chosen:true}]`). Before this fix, angle
   0 would have stayed `chosen=true` forever alongside angle 2.
3. Cleanup: the 1 `angle_gate_request` row (+ its 3 `angle_gate_option` rows) this script
   created were deleted. No other state touched.

Issue itself (AA-494) left for Nghiệp to mark progress on — the design doc's remaining build
steps (schema migration, status redesign, T8→slot-grid wiring) are still not built; only this
prerequisite fix is done.

## Build Steps 2-4 (this session, 29/08/2026): schema migration + atom-availability rule +
## T7→T8 slot-grid wiring

**Scope of this PR:** migration 124 (Decisions 1-3's schema, columns only) + Step 3's
atom-availability rule (`fetch_used_atom_ids()`) + Step 4's `GET /v1/planning/slot-suggestions`.
Does NOT touch the live T8/T9 request/write flow's guards or behavior, and does NOT build
Decision 4 (prior-piece prompt)/Decision 5 (embedding similarity)/the reuse-write UX — see
"Should know" below for exactly why each is deferred, not silently dropped.

### Decisions

- **`angle_gate_request.status` new value name: `reusable`** — the design doc's own proposed
  name (Decision 3), adopted verbatim; no existing status-value convention in this table or
  `content_piece`'s own set conflicts with it.
- **`angle_gate_request.channel`: deprecated-in-place, not dropped.** `create_request()` still
  requires it as a param and T9's CTA lookup (`_fetch_slot_cta`) still reads it live — dropping
  now would break the live flow. Revisit only once AngleGateTab.tsx's write-time-channel redesign
  (explicitly deferred per the design doc's own build order) actually ships.
- **`content_piece.angle_gate_option_id` left NULL for pre-migration rows**, not backfilled —
  every existing row was written under the current one-shot flow (before `choose_angle()` could
  ever be called twice), so there is no ambiguity being papered over; backfilling via a
  point-in-time join to `chosen=true` would imply a precision the data doesn't actually have.
- **`content_embedding vector(1536)`** — matches migration 041/AA-62's only existing precedent
  (Bedrock Titan Embed Text v2 dimension) rather than guessing a different model. pgvector
  extension confirmed already installed live (v0.8.1) via STEP0 read — no `CREATE EXTENSION`
  needed in this migration.
- **`fetch_used_atom_ids()` placed in `services/acp_planning/tenant_pool.py`**, not
  `services/acp_angle_gate/` — matches the build task's own hint ("check which package already
  owns tenant-scoped atom queries, per `fetch_tenant_atoms_by_trip()`'s existing home") and is
  called from the same router (`v1_planning.py`) as its sibling tenant-pool functions.
- **The availability rule collapses to exactly `status = 'approved'`**, not an ordinal "at or
  beyond approved" comparison — live STEP0 confirmed `content_piece.status`'s real value set
  (migration 118) is `processing`/`approved`/`held`/`failed`, with nothing after `approved`. The
  design doc's wording ("at or beyond approved") was written before migration 118 shipped;
  `approved` is the only value that means "passed T10" today.
- **`GET /v1/planning/slot-suggestions` is a new endpoint, not a rewrite of `GET .../slot-grid`**
  — kept the existing endpoint's exact contract untouched (no caller of it needs to change) and
  added the enriched atom/free-atom data as a separate, additive read. Both endpoints share the
  same `compute_slot_grid()` call; not factored into a shared helper in this PR to keep the diff
  minimal against a function neither endpoint owns exclusively.
- **A slot's suggested atoms are never filtered out of the grid even if already used** — the UI
  needs the full slot (design doc: "flips to a success/green state, showing which atom was used")
  rather than a hole where a written slot used to be; `used_atoms` is returned separately so the
  frontend can layer that state on top.

### Changed

- `api/migrations/124_content_piece_reuse_columns.sql` — new migration (see file header for full
  per-decision rationale, mirrored above).
- `services/acp_planning/tenant_pool.py` — new `fetch_used_atom_ids()` + `UsedAtom` NamedTuple +
  `_month_bounds()` helper.
- `api/routers/v1_planning.py` — new `GET /v1/planning/slot-suggestions` endpoint.
- `tests/unit/test_aa448_tenant_pool.py` — `TestFetchUsedAtomIds` (6 tests).
- `tests/unit/test_aa448_v1_planning.py` — `TestGetSlotSuggestions` (4 tests).
- `tests/unit/test_aa494_migration_124.py` — new file, static SQL-text tests for the migration's
  CHECK constraint and column list (no live-DB migration-runner harness exists in this repo — see
  file header for why a text-pinning test is the right level here, matching
  `test_aa249_seo_context_tour_unique.py`'s own precedent).

### Tradeoffs

- Went with a genuinely NEW endpoint (`/slot-suggestions`) instead of adding query params to the
  existing `/slot-grid` to change its response shape — avoids any risk of breaking whatever
  already calls `/slot-grid` today (none confirmed broken by grep, but the existing endpoint's
  contract is simple and worth keeping stable regardless).
- `fetch_used_atom_ids()` joins through `angle_gate_request.channel` (not `content_piece.channel`,
  which migration 124 adds but nothing populates yet) for the `used_atoms[atom_id].channel` field
  — correct today since channel is still chosen at request-creation time; will need to switch to
  `content_piece.channel` once the write-time-channel UX ships and that column is actually
  written.

### Should know

- **Genuine gap found, flagged rather than silently patched**: `content_piece` has
  `UNIQUE(angle_gate_request_id, attempt_number)` (migration 115) — modeling "one write session
  per request," where `attempt_number` 1-2 happens inside a SINGLE row's lifecycle (an UPDATE, not
  a second INSERT — confirmed by reading `_insert_placeholder_piece()`/`_finalize_piece()` in
  `services/acp_content_writing/service.py`). Once `reusable` status ever lets a tenant start a
  SECOND real write on the same request, `start_write()`'s current `_insert_placeholder_piece()`
  (hardcoded `attempt_number=1`) would collide with that UNIQUE constraint the moment a second
  write session is attempted. Decisions 1-3's text does not address this — it's a genuine,
  separate schema question (does `attempt_number` need a second "write session" dimension? does
  the UNIQUE constraint need to drop or change scope? does "the current piece for a request" stop
  being "highest attempt_number" once there can be multiple sessions?) with real architectural
  consequence, not something to guess at while touching a live production table. Needs its own
  decision before any future session wires up the actual second-write flow — do not build that
  wiring against the schema as it stands today without resolving this first.
- **Decision 3's status-transition code (guard widening in `start_write()`, the
  `approved`→`reusable` flip itself) was deliberately NOT built in this PR** — the build task's
  own Step 2 was scoped to "schema changes" only, and wiring live behavior changes into the
  already-shipped T8/T9 request flow ahead of the second-write-flow question above (previous
  bullet) risks shipping a status value nothing can safely act on yet. Adding `reusable` to the
  CHECK constraint is inert until code sets it — safe to ship now, real wiring is separate future
  work.
- **Decision 4 (prior-piece prompt) and Decision 5 (embedding similarity call) were not built** —
  both are legitimately open-ended per the design doc's own language ("decide trigger points
  during build, not assumed here") and, for Decision 5 specifically, this codebase has ZERO
  existing embedding-call infrastructure to build on (confirmed via grep: migration 041's
  `content_embedding` column from AA-62 has no reader/writer anywhere, ever). Building a real
  Bedrock Titan Embed integration from scratch is a separate, substantial task, not a same-PR
  add-on to a schema migration — the column exists now (see Decisions above) so that future work
  doesn't need its own migration.
- Full suite (`tests/unit` + `tests/acp`): 1519 passed, 7 pre-existing failures (`test_h3_rule_
  extractor.py` x3, `test_run_health.py` x4 — both already known pre-existing/live-DB-dependent
  per prior sessions' notes, unrelated to this change, confirmed by name against this PR's own
  diff which touches neither file). This PR's own scope (`-k "aa448 or aa449 or aa450 or aa451
  or aa494"`): 199 passed, 0 failures. flake8 clean on every changed `.py` file.
- Steps 5 (frontend atom-picker UI) and the T8-side of Decisions 1-3 are separate, later PRs —
  see this issue's Linear description / design doc for the running list.

## Build Step 5 (this session, 29/08/2026): frontend atom-picker UI

**Scope of this PR:** the T7 slot-view + T8 atom-picker 4-level drill-down UI (design doc
Decision 6's approved UI/UX section), consuming Step 4's `GET /v1/planning/slot-suggestions`.
No backend changes.

### Decisions

- **New component (`SlotPickerPanel.tsx`), rendered inside `PlanningTab.tsx`** (the existing
  `t7-planning` page/nav entry — "Content Planning"), not a new route/nav entry. This IS "T7's
  weekly view" per the build task's own wording; the existing Quarter Plan section on that same
  page (trip selection) is a different, unrelated concern (which trips get planned this quarter,
  vs. which atom fills which week's slot) that happens to share a page.
- **Self-contained data fetch, independent of the page's existing `preview` state** — a tenant
  can browse any already-finalized year/quarter/month/week without first running a fresh preview
  in this session. A 404 from `/slot-suggestions` (no finalized plan for that quarter) renders a
  plain "finalize this quarter first" empty state rather than trying to auto-trigger anything.
- **Handoff to T8 via `router.push('/portal/t8-angle-gate?atom_id=...')`**, read by
  `AngleGateTab.tsx` via `useSearchParams()` — same pattern `AtomsTab.tsx` already established for
  `?tour_id=` (AA-454), not a new mechanism. Pre-selects the atom only; channel/goal/angle
  selection in `AngleGateTab.tsx` is completely unchanged — Decision 1's write-time-channel move
  is still deferred (see Steps 2-4 above), so the tenant still picks a channel on the T8 page
  itself before continuing.
- **Added a `<Suspense>` boundary to `t8-angle-gate/page.tsx`** — required the moment
  `AngleGateTab.tsx` calls `useSearchParams()`, or `next build`'s static-page generation for that
  route fails (same class of issue `t6-atoms/page.tsx` already documents for AA-454). Verified via
  a real `npm run build` (Turbopack), not just `next dev` — per this repo's own standing lesson
  (`feedback_aa345_round6_clearfilter_prodonly` memory) that router/search-param work must be
  checked against a production build, not dev.
- **Every slot (including `reactive_hold`, which has 0 suggested atoms) still offers the free-atom
  list** — Decision 6's "pick any other atom free this month" applies unconditionally, not only
  to slots that have suggestions. `reactive_hold`'s own Mode-B (what content actually goes there)
  stays undefined per `allocator.py`'s existing comment — this UI doesn't invent an answer to
  that, it just doesn't block a free-atom pick from filling the slot manually either.
- **A slot's "written" state is inferred by atom-id overlap with `used_atoms`**, not a direct
  slot→piece link — there is no such FK (`content_piece` has no `slot_id` column, by design,
  see migration 115's own header). This is a display-only inference, consistent with Decision 6's
  "slots are a display-only suggestion" framing; it does not affect what `free_atom_ids` excludes
  (that's computed independently, straight from Step 3's rule).

### Changed

- `frontend/app/(tenant)/portal/_components/SlotPickerPanel.tsx` — new file, the full 4-level
  drill-down (Year/Quarter → Month → Week → slot-cards → suggested/free atom picker → Start
  writing).
- `frontend/app/(tenant)/portal/_components/PlanningTab.tsx` — renders `<SlotPickerPanel />`
  between the existing Quarter Plan preview/finalize section and the Feedback section.
- `frontend/app/(tenant)/portal/_components/AngleGateTab.tsx` — reads `?atom_id=` via
  `useSearchParams()`, pre-fills `selectedAtomId`.
- `frontend/app/(tenant)/portal/t8-angle-gate/page.tsx` — wrapped in `<Suspense>`.

### Tradeoffs

- Did not build a "start writing directly from an already-written slot" affordance (e.g., pick a
  different unused atom for the same slot after it's green) — the design doc explicitly scopes
  this session to Decision 6's UI, not Decision 3's reuse-write flow; a written slot-card is
  display-only (shows atom/date), not re-expandable in this PR.
- `AngleGateTab.tsx`'s own atom `<select>` is capped at `?limit=100` (pre-existing, not introduced
  here) — a tenant with over 100 curated atoms whose handed-off `atom_id` falls outside that page
  could see a selected-but-not-listed value. Not fixed here (pre-existing limitation, out of this
  PR's scope); flagging so a future session doesn't mistake it for a new bug.

### Should know

- Real `npm run build` (Next.js 16.2.4, Turbopack) succeeded clean — 46/46 pages generated,
  `/portal/t7-planning` and `/portal/t8-angle-gate` both static, no Suspense/prerender errors.
  `tsc --noEmit` and `eslint` both clean on every changed/new file.
- No backend calls were exercised in this build (no dev server run against a live tenant JWT) —
  live-verify (real browser or real HTTP against the deployed frontend + backend) still needed
  post-deploy, per this repo's standing convention; will follow up once merged/deployed.
- Ms. Thư's content/brand sign-off on this UI (design doc's own still-open item) has not
  happened — does not block backend work, and per the build task's own instruction this frontend
  PR proceeds without waiting on it.

## Post-merge / post-deploy record (29/08/2026)

Both PRs merged sequentially per Nghiệp's explicit instruction (real-time in-session request,
not the standing "human merges manually" default — noted here since it's a deliberate exception,
not a policy change going forward):

- **PR #255** (Steps 2-4, schema + rule + wiring) — squash-merged into `main` (`0e949b2`), all 5
  required checks green pre-merge.
- **PR #256** (Step 5, frontend) — originally stacked on #255's branch. After #255's squash-merge,
  GitHub reported it `DIRTY CONFLICTING` once retargeted to `main` (expected: squash-merge
  orphans a dependent branch's history). Fixed with `git rebase origin/main` — git recognized the
  already-applied commit and skipped it cleanly (`warning: skipped previously applied commit`), no
  manual conflict resolution needed. Force-pushed, re-ran full test suite (79 passed) before
  pushing. Retargeting to `main` also correctly triggered the 5 required checks for the first
  time (they never ran while based on a non-`main` branch, apparently path-filtered or scoped to
  PRs against `main`) — all passed, then squash-merged (`9c1251d`).
- Both merges auto-triggered `Deploy Dev` (paths-filter, single pipeline per ADR-2026-023). Waited
  for the later run (from #256's merge) to complete — succeeded. `aws ecs describe-services`
  confirmed task def `aa-cis-dev-api:163` (was `:161` at session start), `rolloutState=COMPLETED`.

**Migration 124 applied live** (S3-mediated ECS-exec, `~/.claude/CLAUDE.md` canonical pattern):
`shared.schema_versions` now shows `124` applied `2026-08-29T13:52:21Z`; direct
`information_schema.columns` read confirmed all 4 new `content_piece` columns
(`angle_gate_option_id` uuid, `channel` text, `content_summary` text, `content_embedding`
USER-DEFINED/vector); `pg_get_constraintdef` confirmed the CHECK constraint now includes
`'reusable'`. Re-ran the same migration file a second time in the same session — completed
without error, confirming the `IF NOT EXISTS`/`DROP CONSTRAINT IF EXISTS` idempotence holds for
real, not just by inspection.

**Live-verify, real HTTP through the actual deployed app (S3-mediated ECS-exec, real tenant JWT
minted with the confirmed-unoverridden default `JWT_SECRET`, real tenant `wanderlux-travel` —
`test-n1-flow` no longer exists, per Step 1's own post-merge note):**

1. `GET /v1/planning/quarter-plan?year=2026&quarter=3` → 200, a real finalized plan already
   existed for this tenant (`trip_ids` populated) — no need to finalize a fresh one.
2. `GET /v1/planning/slot-suggestions?year=2026&month=8` (this session's own new endpoint) → 200.
   Real response: `capacity_posts_per_week=3` (matches `shared.tenants.posts_per_week` exactly),
   `atoms_by_id` count 7 (matches the tenant's real live atom count, independently confirmed via
   direct DB count), `free_atom_ids` count 7 (all free — no pieces written yet), `used_atoms={}`,
   `slot_grid.slots` count 3. A sample slot carried 4 real suggested `atom_ids` (channel=`blog`,
   the multi-atom group) with a real `keyword_seed` (`"bike in Japan"`) and populated
   `trip_name`/`destination`/`activity_type`/`distinctiveness` per atom — confirms the
   `atoms_by_trip` → `atoms_by_id` enrichment join is correct against real data, not just the
   mocked-pool unit tests.
3. **The atom-availability rule itself (Step 3), proven against real Postgres, not just SQL-text
   pinning**: inserted one real `angle_gate_request` (`status='approved'`) + one real
   `content_piece` (`status='approved'`, real `created_at=now()`) for one of the tenant's real
   free atoms (`atom_a06b272594`), directly via SQL (no LLM call — cheaper than a real T8/T9
   write, and this task is testing the read-side rule, not the write path). Re-called
   `GET /v1/planning/slot-suggestions` — the atom immediately disappeared from `free_atom_ids`
   (7 → 6), appeared in `used_atoms` with the correct `channel`/`used_at`, and *stayed* resolvable
   via `atoms_by_id` (confirms the "written slot still shows its atom's info" UI requirement has
   real data to render against). Deleted both test rows afterward; confirmed 0 remaining rows for
   that `request_id` — no leftover test data in `wanderlux-travel`'s real history.
4. Cross-tenant/unknown-tenant call (`GET /v1/planning/slot-suggestions` with a JWT for a
   non-existent tenant_id) → 404, not 500 — no crash on a bad tenant.
5. `GET /health` → 200 throughout.
6. Frontend: `https://aa-cis.lumiguides.it.com/portal/t7-planning` → 307 (redirects to
   `/tenant-login`, expected unauthenticated behavior — confirms the route itself is live on
   Vercel prod, a nonexistent route would 404 instead). Could not verify the actual rendered UI
   behind a real tenant login in this session (no browser session, no live tenant credentials on
   hand) — a real browser click-through (login → Content Planning → Weekly Slots → pick an atom →
   confirm the T8 handoff lands with the atom pre-selected) is still open, flagged here rather
   than assumed working from the build succeeding.
7. All temporary S3 scripts (`aa-cis-bronze-005097885195/scripts/*`) used for this verify deleted
   afterward.

**Not done this session**: the real-browser T7→T8 handoff click-through (item 6 above). AA-494
itself left **not Done** — per standing rule, only Nghiệp or Claude Chat closes it. This is the
last remaining piece of Steps 2-5's own scope; everything else in the build task's Step 2-5 list
is now built, merged, deployed, and live-verified (schema, availability rule, T7→T8 wiring
backend, and the frontend UI's own build/deploy — only its authenticated-session render is
unverified).

## Close-out round (this session, 29/08/2026): debug-line cleanup + real browser click-through

### Item 1 — debug-line leak, a real bug, confirmed and fixed

**Investigation (before touching anything):** grepped for `capacity_note` across the codebase.
Found two, same field name, different models, different design intent:
- `QuarterPlan.capacity_note` (`services/acp_planning/quarter.py`) — already rendered in the
  existing Trips section of `PlanningTab.tsx`. Its actual content is a genuine one-line,
  tenant-appropriate sentence: `"{N} eligible trips at {M} posts/wk — focusing on {K} trips
  (applied)."` No jargon, no concatenation.
- `SlotGrid.capacity_note` (`services/acp_planning/allocator.py::compute_slot_grid()`) — an
  internal reasoning trail built by `_add_note()`, joined with `" | "` across every trip the
  allocator had to drop or shrink that month. Real example content: `"Trip 'X' atom floor: 7
  live atoms < 2x4 planned slots for blog — capacity implicitly reduced, no silent atom repeat.
  | Trip 'Y' has no eligible atoms left for blog this month (atom floor reached or all on
  cooldown) — slot dropped, not repeated."` — pipe-joined, internal jargon ("atom floor",
  "cooldown"), the module's own docstring calls these "a note logged", i.e. written as an
  internal/debug/audit trail, not display copy.
- Confirmed via grep that **nothing in this codebase — admin or tenant — had ever rendered
  `SlotGrid.capacity_note` before this session's own `SlotPickerPanel.tsx` (Step 5, prior
  session).** There is no prior precedent treating this specific field as tenant- or even
  admin-facing; this session's own Step 5 build was the first and only renderer, and it
  incorrectly assumed the two same-named `capacity_note` fields shared the same design intent as
  `QuarterPlan`'s. That assumption was wrong — this is a real bug this session introduced, not a
  pre-existing one being newly discovered.

**Fix:** removed the render (`{data.slot_grid.capacity_note && <span>...}`) from
`WeekLevel()` in `SlotPickerPanel.tsx` entirely — not CSS-hidden, the JSX branch is gone.
Reformatting it into real tenant copy instead of removing it was considered and rejected for
this PR: `SlotGrid.capacity_note` is ONE string for the WHOLE MONTH (not per-slot, not per-trip),
so there's no clean way to attach it as a per-slot tooltip without a new structured backend field
(e.g. a list of `{trip_id, message}` notes) — a real design+schema task, not a same-PR string
rewrite. Flagged as a genuine follow-up (see "Should know" below), not solved here.

**Changed:** `frontend/app/(tenant)/portal/_components/SlotPickerPanel.tsx` only — removed 1 line
of rendering, added a comment documenting the investigation above so a future session doesn't
reintroduce the same mistake for the same reason.

**Verified, both HTTP-build-clean AND against real rendered output (see Item 2 below for the
full browser evidence):** `tsc --noEmit` clean, `eslint` clean, real `npm run build` clean
(46/46 pages). A real browser screenshot against the **currently-deployed (pre-fix) production
app** captured the bug live (see Item 2's screenshot record) — confirms this wasn't a
theoretical/code-only finding, it was actually visible to a real tenant login. A second real
browser pass against a **local production build carrying the fix** (`next build && next start`,
pointed at the real production API via `.env.local`'s `API_URL`, real tenant login) confirmed the
line is gone and nothing else regressed.

**Answer to "is this a real bug or intentional":** real bug, introduced this session (Step 5,
prior session), now fixed. Not intentional, not pre-existing.

### Item 2 — real browser click-through, real tenant login

**Blocker found and resolved, not skipped silently:** the prior session's live-verify was
HTTP-only (minted JWTs, no real login). Attempting a REAL login this time surfaced that
`wanderlux-travel`'s API key (rotated by AA-433) was never committed anywhere and lived only in a
different, now-inaccessible prior session's scratchpad — genuinely no working credential was
available at the start of this task. **Asked Nghiệp** rather than silently either skipping this
item or unilaterally rotating a live tenant credential (an outward-facing, semi-irreversible
action) — approved: generate a fresh key via the same real admin endpoint AA-433 itself used
(`POST /admin/tenants/{id}/generate-key`, real `X-Admin-Secret`). Same handoff discipline AA-433
established: new key written once to this session's own scratchpad (`chmod 600`), used for the
real login flow below, then securely deleted (`shred -u`) at the end of this task — **Nghiệp:
`wanderlux-travel`'s API key has been rotated again as of this session; if anyone still has the
AA-433-era key in hand, it no longer works.**

**Tooling confirmed available and used:** Playwright (both the frontend's own `@playwright/test`
and a Python `playwright` install in the backend venv, bundled Chromium launches clean) — real
headless-browser automation, not a curl/HTTP shortcut.

**Full click-through, real tenant login (`POST /auth/tenant-login` via the actual login page, not
a minted JWT), against the real production app (`https://aa-cis.lumiguides.it.com`) unless noted:**

1. `/tenant-login` → real API key typed into the real form → `Access Portal` → lands on `/portal`
   (real cookie session, no shortcuts).
2. `/portal/t7-planning` (Content Planning) → Weekly Slots section renders with real data:
   `2026 ▸ (pick quarter)`.
3. Click `Q3` → drills to 3 months (Jul/Aug/Sep). Click `Aug` → real
   `GET /v1/planning/slot-suggestions?year=2026&month=8` fires → **this exact screenshot is where
   Item 1's bug was caught live** (pre-fix production): the raw pipe-joined debug string,
   verbatim, between the breadcrumb and the week cards.
4. Click `Week 1` (2 real slots) → `Post 1` auto-expands (by design — `DetailLevel`'s own default
   `expandedSlotId = slots[0]`, confirmed working, no extra click needed) → both "Suggested for
   this slot" (4 real atom cards, same trip, real distinctiveness `MED` badges, real excerpt text)
   and "Other atoms free this month (7)" (real search box + real row list) render simultaneously,
   visually separate, exactly per the approved design — not empty states, real tenant data.
5. Search box: typed `"Chuohashi"` (a real atom's body text, not just trip/destination) → free-row
   count narrowed correctly (11 → 5 MED-badged buttons total: the 4 suggested cards are
   unaffected by the search, as designed — only the free list filters); typed a nonsense string →
   real `"No free atoms match."` empty state rendered.
6. Clicked a free-list atom row (not a suggested card) → confirmed highlight-only, **URL
   unchanged** (no premature navigation, matches "picking an atom does not auto-navigate").
7. Clicked `Start writing` → real navigation to
   `/portal/t8-angle-gate?atom_id=atom_a06b272594` → `AngleGateTab.tsx`'s atom `<select>` landed
   **pre-selected on the exact atom just picked** (`"★ Route A: Tokyo Bay — The route passes
   Chuohashi bridge, where the water and seasonal cherry"`), channel still defaults to
   `facebook` (unchanged, expected — channel-at-write-time is still deferred per Steps 2-4's own
   scope note). Confirms the `?atom_id=` handoff wiring built in Step 5 works correctly end to
   end, not just in the unit tests.
8. Repeated steps 2-7 in full against the local, fixed build (`next start` on `:3100`, real
   production API) — same real login, same real click-through, same result, PLUS Item 1's fix
   confirmed live (step 3's screenshot equivalent now shows only `"Capacity: 3 posts/week"`, no
   debug text).

**Real, pre-existing finding, NOT introduced by AA-494 — flagged, not fixed (out of this task's
scope):** every `/portal/*` page load fires `GET /api/admin/billing` (from `portal/layout.tsx`,
the shared layout wrapping every tenant portal page) and it 401s for `wanderlux-travel` —
reproduced identically on both the real production app and the local build, so this is unrelated
to anything built or changed in this session. Two browser console errors per page load as a
result. `frontend/app/(tenant)/portal/_components/ApiTab.tsx` and `DashboardTab.tsx` also call
the same endpoint — worth its own look (real bug, or a route naming leftover from staff/admin
code reused for a tenant billing view that was never actually wired up), not investigated further
here.

**Screenshots** (session-local, not committed to the repo — sent directly to Nghiệp in-session):
before-fix (raw debug line), after-fix (clean), and the full T7→T8 handoff landing with the
correct atom pre-selected.

**Answer to "did Item 2 pass":** fully passed, real browser, real tenant login, real backend,
both before-fix (bug reproduced live) and after-fix (bug confirmed gone, no regression across the
full click-through). The one open item is the pre-existing `/api/admin/billing` 401 noted above,
unrelated to AA-494.

### Should know (carried forward)

- Per-trip/per-slot structured "why this slot is smaller" transparency (the real signal inside
  `SlotGrid.capacity_note`) is genuinely useful information this session chose to REMOVE rather
  than half-fix — if this is wanted later, it needs a new structured field
  (`list[{trip_id, message}]` or similar) from `compute_slot_grid()`, not a string rewrite of the
  current whole-month blob. Not built here.
- `wanderlux-travel`'s API key was rotated again this session (see Item 2) — Nghiệp needs to
  retrieve/redistribute if anyone downstream still needs it; the value itself was never in chat
  or committed anywhere, matching AA-433's own handling.
- `GET /api/admin/billing` 401ing on every tenant portal page load is a real, separate,
  pre-existing gap — not part of AA-494, flagged for its own look.
