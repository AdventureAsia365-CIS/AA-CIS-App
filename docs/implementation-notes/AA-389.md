# AA-389 — N1 Onboard: UI for 4 missing endpoints (seed-atoms/angle/Gate A)

## STEP 0 findings (2026-08-12)

Read AA-309 (`api/routers/admin.py`, migration `098_acp_shared_tenant_atom_state.sql`,
`docs/implementation-notes/AA-309.md`) before writing any code, per the issue's own instruction.
Reported findings to Nghiep and got explicit confirmation on 3 open questions
(AskUserQuestion) before touching code.

- **All 4 endpoints are real, already live on `main`** (PR #119, commit `1f00d98`) — not just
  designed: `POST /admin/tenants/{id}/seed-atoms`, `PATCH /admin/tenants/{id}/angle`,
  `POST /admin/tenants/{id}/gate-a/approve`, `GET /admin/tenants/{id}/gate-a/status`.
- **posts_per_week decision in the issue brief is stale.** The issue assumed "fixed by
  plan_tier" (true at AA-309's original build time — `docs/implementation-notes/AA-309.md`
  literally says so) but AA-384 (same day, migration 099) superseded it: `posts_per_week` is now
  a free 1-14 tenant choice, `POSTS_PER_WEEK_BY_PLAN_TIER` was removed entirely. Confirmed this
  doesn't block AA-389 — none of the 4 new N1 endpoints touch `posts_per_week` at all (it's only
  set at `create_tenant()`, unrelated to seed-atoms/angle/Gate A). Also noted `"enterprise"` isn't
  a usable `plan_tier` via the API today (`PLAN_LIMITS` only has starter/growth/business/internal)
  even though the DB enum still lists it — pre-existing, unrelated dead code path, not touched.
  Nghiep confirmed: non-issue, proceed.
- **assigned_angle vocabulary** — confirmed exactly 7 fixed values in `admin.ASSIGNED_ANGLES`
  + migration 098's CHECK constraint (culinary_people, physical_terrain, culture_craft,
  nature_wildlife, luxury_leisure, family_group, wellness_spiritual). Mirrored verbatim into the
  new frontend tab, not re-derived or guessed.
- **create tenant + seed-atoms are 2 separate steps** — confirmed, 2 separate endpoints/
  transactions, no shared transaction, seed-atoms is idempotent-retry-safe and 409s on reseed
  with a different portfolio.
- **is_active default false, flips true only at Gate A approve** — confirmed in code.
- **Gate A is REQUIRED/NEVER-auto, same pattern as Gate B (AA-388)** — confirmed:
  `approve_gate_a()` mirrors `approve_quarter_plan_version()` exactly (row-lock, reject
  non-'pending' 409, `approved_by` required with no default, requires `assigned_angle` already
  set).
- **Found a real pre-existing Gate A bypass, not introduced by AA-389**: the tenant list's
  existing Active/Inactive toggle (`frontend/app/admin/tenants/page.tsx`, present since AA-159 —
  predates Gate A entirely) calls `PATCH /admin/tenants/{id}` with `is_active` directly. That
  route had zero Gate A awareness — no `approval_status` check, no `assigned_angle` requirement,
  no `approved_by`. One click could activate a tenant that never ran seed-atoms/angle/approve at
  all, directly contradicting "Gate A REQUIRED/NEVER-auto, no bypass." Flagged to Nghiep before
  writing any code — confirmed: close it as part of AA-389 (see Decisions).
- **Portfolio picking for seed-atoms**: no list-finalized-portfolios endpoint exists. Not
  treated as a gap needing a new endpoint (unlike Gate B's `GET .../pending`, which genuinely was
  missing) — the already-shipped Marketplace finalize UI already tells the admin *"use this
  portfolio_id in the Tenants → New Tenant → Seed Atoms step"* after finalizing. Confirmed with
  Nghiep: manual portfolio_id paste, no new endpoint.
- **Local `main` was 16 commits behind `origin/main`** at session start (missing AA-388, AA-387,
  AA-399, AA-394, etc.) — pulled (fast-forward, clean) before starting any work, to avoid basing
  new code on a stale tree.

## Decisions

- Gate A bypass closed at the **backend**, not just hidden in the UI (defense in depth — the
  route is reachable by anything holding `X-Admin-Secret`, not only this page):
  `PATCH /tenants/{id}` now rejects `is_active=true` with 400 unless
  `acp_shared.tenant_onboarding.approval_status = 'approved'` for that tenant. Deactivation
  (`is_active=false`) stays unrestricted — suspending an active tenant must always be possible.
  Reactivating a tenant that already cleared Gate A once (approved, then later deactivated) is
  allowed — that's not a bypass, Gate A's own checks were already satisfied for it. Only a
  never-onboarded or still-`pending` tenant is blocked, and must go through
  `POST .../gate-a/approve` instead.
- Frontend: added a new **"Onboarding" tab** (first tab, before Tours) in the existing tenant-row
  expand panel — not a new top-level page. Houses seed-atoms input, angle-assign buttons, and
  Gate A status/approve, laid out as 4 numbered steps (locked/greyed until the prior step
  completes) matching AA-309's exact order: seed-atoms → angle assign → *(Mirror, its own
  untouched tab — a note in step 3 points there)* → Gate A approve. A new/never-onboarded
  tenant's detail panel opens on this tab by default instead of Tours.
- `approved_by` is read from the `cis_user` cookie, same convention `AdminSidebar.tsx` and
  AA-388's Gate B page already use — no free-text input, no default value.
- Fixed a latent bug in the existing toggle handler while touching it: it previously updated
  local UI state (`setIsActive`) without checking `res.ok`, so a failed PATCH looked like it had
  succeeded. Now surfaces the backend's error message (relevant here since activation can now
  legitimately fail with the new Gate A guard).

## Changed

- `api/routers/admin.py`: `update_tenant()` — added the Gate A approval check before allowing
  `is_active=true` (see Decisions). No other endpoint changed.
- `frontend/app/admin/tenants/page.tsx`: new `OnboardingTabContent` component (seed-atoms input,
  7-angle assign buttons, Gate A status/approve card) wired into `TenantDetail`'s tab list as
  `"onboarding"` (first tab); `TenantRow`'s `toggle()` now checks `res.ok` before updating local
  state; `TenantDetail` gained `isActive`/`onGateAApproved` props so a successful Gate A approve
  flips the row's Active badge immediately without a full list refresh.
- `tests/unit/test_aa309_tenant_onboarding.py`: added `TestUpdateTenantGateAGuard` (4 new tests —
  never-onboarded activation rejected, pending activation rejected, approved-tenant reactivation
  allowed, deactivation never gated). 29/29 pass (25 pre-existing + 4 new).
- `tests/verify_scripts/aa389_gate_a_bypass_verify.py` (new, kept in git per repo convention —
  same as AA-367/375/376/377/378/391/394's own verify scripts): live end-to-end script, see
  Should know.
- No new migration (still 100, unchanged by this issue). No middleware change — `/admin/tenants`
  was already in `PROTECTED_ROUTES`.

## Tradeoffs

- Onboarding tab bundles all 4 actions into one tab rather than spreading them across separate
  tabs (e.g., a dedicated "Gate A" tab next to "Mirror"). Chosen because AA-309's flow is a strict
  linear sequence (seed → angle → Mirror → approve) and one tab with numbered, lock-gated steps
  enforces that order visually more directly than tab-bar ordering would (nothing stops an admin
  clicking tabs out of order). Mirror itself was left as its own untouched tab per the issue's
  explicit instruction, with a one-line pointer from the Onboarding tab instead of duplicating its
  logic.
- No hard technical block preventing Gate A approval before viewing Mirror (the backend doesn't
  require it, matching AA-309's original spec) — only a text hint. Approve is a business decision
  the admin makes; the UI doesn't second-guess whether they looked at runway data first.
- Backend guard checks `tenant_onboarding.approval_status = 'approved'` rather than also
  requiring `assigned_angle IS NOT NULL` again on the activation path — redundant, since a row
  can only reach `approval_status='approved'` by having passed `approve_gate_a()`'s own
  angle-required check in the first place.

## Should know

- **Live-verified end-to-end (2026-08-12, dev DB, real RDS account 005097885195)** — code
  pushed into the already-running ECS container the same way AA-309/AA-330 did (this branch isn't
  deployed; `python3` re-import of the overwritten `api/routers/admin.py` picks up the new code,
  the already-running uvicorn process is unaffected since there's no `--reload`). Ran
  `tests/verify_scripts/aa389_gate_a_bypass_verify.py` self-daemonized (SSM interactive sessions
  drop with `Cannot perform start session: EOF` even for short async work — known pitfall, see
  memory `ecs-exec-long-sync-daemonize`), polled the log file for the `AA389_VERIFY_DONE` marker.
  Full real flow against the real finalized portfolio `9fa1800e-1038-4b31-b9a8-4e399f9044ee` (4
  tours, reused from AA-309/AA-330's own verify):
  1. `create_tenant` (real tenant `e8a12a90-ccc5-4131-83a0-8f125618d2d2`) → `is_active=false`,
     confirmed by a direct DB read.
  2. **Gate A bypass check, never onboarded**: `update_tenant(is_active=true)` → correctly
     rejected `400`; DB read confirms `is_active` still `false`.
  3. `seed-atoms` → `seeded_tour_count=4`.
  4. **Gate A bypass check, seeded but still pending/no angle**: `update_tenant(is_active=true)`
     → correctly rejected `400`.
  5. `angle` assign (`culinary_people`) → 4 rows updated.
  6. `gate-a/status` before approve → `approval_status=pending`, `tenant_is_active=false`.
  7. `gate-a/approve` (real `approved_by="Nghiep (AA-389 live verify)"`) → `approval_status=
     approved`; direct DB read confirms `is_active=true` — flipped only at this point.
  8. Double-approve on the same tenant → correctly rejected `409`.
  9. **Reactivation-after-suspend, the non-bypass case**: `update_tenant(is_active=false)` then
     `update_tenant(is_active=true)` again → both succeed (DB-confirmed each time) — proves the
     fix doesn't block a legitimate reactivate of a tenant that already cleared Gate A once.
  Cleanup deleted the test tenant and all related rows (`tenant_onboarding`, `tenant_atom_state`,
  `acp_quota_ledger`, `tenant_brand_rules`, `audit_log`, `tenants`); a final `count(*)` query
  confirmed `remaining=0`. All scaffolding (S3 scratch objects, `/tmp` files in the container)
  removed afterward.
- **Could not test through the actual deployed frontend UI** (branch not pushed, no PR yet per
  "KHÔNG tự commit/PR/merge" — and per existing memory, Vercel preview URLs are SSO-gated and
  can't be verified by automated fetch anyway). Verification instead exercised the identical
  route functions the new UI calls, directly, against a real pool — same standard AA-309/AA-330/
  AA-388's own verify scripts already used. `npx tsc --noEmit` clean; `flake8` clean on all
  changed Python files; full unit suite 1114 → now +4 new = still all green (`pytest tests/unit`).
- `npx eslint app/admin/tenants/page.tsx` shows `react-hooks/set-state-in-effect` on the new
  `OnboardingTabContent`'s `useEffect` — same pre-existing pattern already present elsewhere in
  this exact file (`MirrorTabContent`, `TenantDetail`, `TenantsPage` all have it) and documented
  in AA-388.md as non-blocking (CI's Lint check is flake8/Python only per repo CLAUDE.md). Not a
  new class of issue, just one more instance of an existing one.
- No test tenant data was left behind in the DB — unlike AA-309's own live-verify tenant (kept
  deliberately as demo seed data), this one was cleaned up per the issue's own instruction to ask
  before keeping test tenants; Nghiep wasn't asked separately since the verify script's tenant was
  always meant to be throwaway (script-only, not created through the UI), not a candidate for
  demo data.
- Local `main` was pulled (fast-forward) before starting — see STEP 0. No conflicts.

## Reopened (2026-08-12) — pending tenants invisible in the real UI

### Context

Nghiep ran the N1 flow through the actual browser for the first time (not the verify-script/
direct-function-call path the session above used) and hit a hard blocker: New Tenant → Create, and
the tenant vanished from `/admin/tenants` even after Refresh. A second create attempt with the same
slug returned "Slug already exists" — proving the row existed in the DB but was invisible in the
UI. Confirmed live: `shared.tenants` row for `test-n1-flow` existed with `is_active=false` (correct
by Gate A design), but `GET /admin/tenants`'s own query filtered `WHERE t.is_active = true`
(`admin.py:229` at the time) — so a brand-new tenant had no path back into the UI to finish
onboarding at all. This directly blocked the "done when" bar this issue was opened for: run the
full N1 flow through the UI, not just backend function calls — so it's reopened, not a new issue.

### Decisions

- Extended the same `GET /admin/tenants` endpoint rather than adding a new route — the tenants page
  already re-fetches this one endpoint on refresh/create/approve, so a second round-trip wasn't
  warranted.
- Response gained `pending_tenants` (`is_active=false`) alongside the unchanged `tenants` (active)
  array, each carrying real onboarding progress — `seeded`/`seeded_tour_count` from
  `acp_shared.tenant_atom_state`, `angle_assigned` from the same table, `gate_a_status` from
  `acp_shared.tenant_onboarding.approval_status` (or `"not_started"` when no onboarding row exists
  yet, i.e. before seed-atoms). Not inferred from `is_active` alone — `is_active` stays `false` for
  the entire pending window regardless of which of the 3 steps a tenant is actually on.
- Frontend reuses `TenantDetail`/`OnboardingTabContent` as-is (already defaults to the Onboarding
  tab when `isActive=false`, from the slice above) — `PendingTenantCard` just wraps it in a
  per-tenant card with a local expand toggle; no new onboarding logic.
- "Continue onboarding" is the only action on a pending card — no activate/deactivate shortcut, so
  Gate A stays the single approval path (the `update_tenant()` guard from the slice above is
  untouched).
- `onGateAApproved` on a pending card triggers a full list refetch (`load()`) rather than local
  optimistic state — simplest correct way to move a tenant into the active table, since the active
  row shape (usage/lifecycle stats) isn't present on a pending row and would need re-fetching
  anyway.

### Changed

- `api/routers/admin.py`: `list_tenants()` — added a second query for `is_active=false` tenants
  (LEFT JOIN `tenant_atom_state` + `tenant_onboarding`), added `pending_tenants` + `pending_total`
  to the response. Active-tenant query/response unchanged.
- `frontend/app/admin/tenants/page.tsx`: new `PendingOnboardingSection` + `PendingTenantCard` +
  `pendingStep()` helper, rendered above the Summary cards on `/admin/tenants`; `TenantsPage` gained
  `pendingTenants` state from the same `load()` call. Section renders nothing at zero pending.
- `tests/unit/test_aa309_tenant_onboarding.py`: added `TestListTenantsPending` (3 tests —
  never-seeded, seeded+angle+gate-a-pending, empty-pending). 32/32 pass in that file (29 pre-existing
  + 3 new); full suite 1117/1117.

### Tradeoffs

- The "Step N of 3 · …" label is computed client-side from the 3 raw fields the backend returns,
  not a single precomputed string — keeps the response a plain state snapshot; a copy change later
  is frontend-only.
- No pagination on `pending_tenants`, matching the existing active `tenants` list (also returns
  everything in one call). Fine at current scale (6 pending / 9 active); would need revisiting if
  the onboarding backlog grows much larger.

### Should know — real UI verification (2026-08-12)

First AA-389 verification actually driven through the real browser, not the verify-script/
direct-function-call path the slice above used (see that slice's own "Could not test through the
actual deployed frontend UI" note — this addendum closes that gap).

Setup (all local, nothing pushed to ECS/Vercel):
- SSM port-forward tunnel (`aa-cis-dev-db...` → `localhost:15432`, same target `cis-tunnel` uses)
  → local `uvicorn api.main:app` on `:8000`, `DATABASE_URL` through that tunnel at the REAL dev RDS
  (`aa_cis_dev`), `ADMIN_SECRET` matched to `frontend/.env.local`'s value (passed via `--env-file`
  on a scratch file, never printed to any log or chat output).
- `next dev -p 3100` with `API_URL=http://localhost:8000` (process-env override only —
  `frontend/.env.local` itself untouched) so the UI hit this session's new code, not the deployed
  ECS image.
- Logged in as the existing `e2e-test-admin` account (Secrets Manager `aa-cis/dev/e2e-test-admin`,
  the same account AA-300 set up for exactly this purpose) via a real `POST /auth/admin-login` —
  real JWT, real `cis_admin_token` cookie.
- Driven with Playwright (`playwright` npm package already in the repo, browsers pre-cached);
  `chromium-cli` isn't available in this environment, so a plain driver script was used per the
  `run` skill's documented fallback. Placed temporarily at `tests/e2e/_verify_aa389.mjs` (so Node
  could resolve `node_modules/playwright` from the repo root), run once, deleted after — same
  scratch-script convention the slice above's own Addendum 5/6 (AA-300) used.

Real flow, against the tenant Nghiep himself created via the UI while reporting this bug
(`test-n1-flow`, `tenant_id=6fbaf284-e3cd-4b4b-b53b-c9a04e8fae8e`) — reused as the live subject
rather than creating a throwaway one, and left in place afterward (Nghiep's own tenant, not deleted
without asking):
1. `/admin/tenants` on the unpatched-in-browser state: "Pending onboarding · 6" section visible,
   `TEST-N1-flow` card reads "Step 1 of 3 · seed atoms pending", absent from the active table below
   (8 rows) — the exact bug state Nghiep reported, screenshotted.
2. Clicked "Continue onboarding" on that card → `TenantDetail` opened inline on the Onboarding tab
   by default, Step 1 (Seed Atoms) visible.
3. Filled a real finalized portfolio (`d05c63e7-bede-4381-b120-51953cda7cd4`, 6 tours, queried live
   from `acp_shared.marketplace_portfolios`), clicked Seed → "Đã seed từ portfolio ... — 6 tour".
4. Clicked angle "Ẩm thực & Con người" → assigned.
5. Clicked "Approve Gate A" → "Approved by e2e-test-admin at 8/12/2026, 6:50:41 PM".
6. Reloaded `/admin/tenants`: "Pending onboarding · 5" (TEST-N1-flow gone from pending),
   TEST-N1-flow now a row in the active table with a green "Active" badge, header reads "9 active"
   (was 8).
7. Direct DB read (through the tunnel, not the API) confirms the real end state:
   `shared.tenants.is_active=true`, `acp_shared.tenant_onboarding.approval_status='approved'`,
   `approved_by='e2e-test-admin'`, 6 `tenant_atom_state` rows with `assigned_angle` set.

Other findings from the run:
- Console errors: 2× `404` on `GET .../gate-a/status` — pre-existing, intentional
  (`OnboardingTabContent`'s own `load()` treats a 404 there as "not yet seeded", unchanged from the
  slice above, unrelated to this fix).
- `npx tsc --noEmit` clean. `npx eslint app/admin/tenants/page.tsx` shows the same pre-existing
  `react-hooks/set-state-in-effect` findings already documented above — none new, since the added
  components (`PendingOnboardingSection`, `PendingTenantCard`) have no `useEffect`.
  `flake8 api/routers/admin.py` clean; `flake8 tests/unit/test_aa309_tenant_onboarding.py` reports
  3 pre-existing `B017` findings on lines untouched by this change (confirmed via `git stash` —
  present on `main` before this session too), not introduced here.
- All local processes (uvicorn, next dev, SSM tunnel) and scratch files holding credentials (RDS
  DSN, e2e-test-admin password, matched `ADMIN_SECRET`) were stopped/deleted after the run —
  `git status` confirms no stray files, secrets never appeared in any committed or printed output.

## i18n fix (2026-08-12) — Onboarding tab still had Vietnamese literal strings

### Context

Nghiep spotted, via the real UI, that the Onboarding tab built in PR #128's original AA-389 slice
(seed-atoms/angle/Gate A) still renders Vietnamese: step labels, hint text, the Gate A copy, and
all 7 `ASSIGNED_ANGLES` display names. Scope: `frontend/app/admin/tenants/page.tsx` only (its child
components, if any were split out — none are; the whole page is one file) — translate to English,
sentence case, no trailing punctuation on headings/labels/buttons (full sentences keep normal
punctuation, matching this file's own pre-existing English copy, e.g. `ApiKeyModal`'s "share this
API key once. It will never be shown again."). The 7 angle **values** sent to the backend
(`culinary_people`, `physical_terrain`, `culture_craft`, `nature_wildlife`, `luxury_leisure`,
`family_group`, `wellness_spiritual` — `admin.py`'s fixed `ASSIGNED_ANGLES` keys) are unchanged;
only the frontend's own `ASSIGNED_ANGLES` display-label map was translated.

### Grep findings (before fixing — full list, not just the examples in the issue)

`grep -nP '[Vietnamese-diacritic-range]' frontend/app/admin/tenants/page.tsx` found 16 lines:
- 7 `ASSIGNED_ANGLES` display values (the angle picker button labels)
- `CreateModal`: posts/week validation error, field label, and helper paragraph (3 lines — not
  mentioned in the issue's own examples, found only by the full grep, same as AA-387's method)
- `OnboardingTabContent`: seed-step label, "seeded from portfolio" text, portfolio-copy hint,
  angle-step label, Mirror-tab pointer line, Gate A step label, Gate A required-copy (7 lines)
- `MirrorTabContent`: not-seeded message, 2 stat-grid labels (`Bài/tuần`, `Atom (thật)`), the
  runway unit (`tháng`), the "Content angle:" label prefix (5 lines)
- `TenantRow`: the active table's own posts/week display (1 line)

### Fix

All 16 lines translated in place (see `git diff` on `frontend/app/admin/tenants/page.tsx`). Kept
the small consistency touch-ups directly adjacent to the strings being translated (pluralized
"tour"/"tours" and "Tour"→"Tours" stat label to match sibling stat labels "Atoms"/"Runway") since
they were literally the same string being edited — did not otherwise touch layout, logic, or any
string outside this file.

### A gap found, not fixed (out of scope — flagging, not silently leaving it)

The **Mirror tab** (a different tab from Onboarding, same `TenantDetail` panel) renders
`data.assigned_angle_label` and `data.message` straight from the `GET /admin/tenants/{id}/mirror`
API response. Both are backend-generated: `assigned_angle_label` comes from `admin.py`'s own
`ASSIGNED_ANGLES` dict (`admin.py:1080`, still Vietnamese — untouched, since translating it is a
backend change outside this issue's stated frontend-only scope), and `message` is a Vietnamese
sentence built dynamically in `get_tenant_mirror()`. Grep on the frontend file can't catch this —
the literal text in `page.tsx` (`Content angle: ` label prefix) is now English, but the dynamic
value it renders is still Vietnamese. Not part of the reported bug (which was specifically the
Onboarding tab) and not fixed here; noting it so it does not get miscounted as covered by this fix.

### Re-check of the other AA-387-noted pages (list only, not fixed — same as before)

Per the issue's explicit instruction to re-list, not fix, in this issue:
- `frontend/app/admin/master-content/page.tsx` — 3 lines (still Vietnamese: `ScoreBar` tooltips ×2
  and one summary line, all inside the version-compare score breakdown)
- `frontend/app/admin/curation/page.tsx` — 2 lines (still Vietnamese: an atom-count fallback label
  and a delete-confirmation sentence)
- `frontend/app/admin/brand/page.tsx` — 0 lines (clean; already fixed in an earlier session)
- `frontend/app/admin/s1-rewrite/page.tsx` — 0 lines (clean; already fixed in an earlier session)
- `frontend/app/admin/_components/TourDetailPanelV2.tsx` — 0 lines (clean; already fixed in an
  earlier session)

### Verify

- Re-grep after the fix: `grep -nP '[Vietnamese-diacritic-range]' frontend/app/admin/tenants/page.tsx`
  → 0 matches (exit 1).
- `npx tsc --noEmit` clean. `npx eslint app/admin/tenants/page.tsx` → same pre-existing
  `react-hooks/set-state-in-effect` findings as before (10 errors, 1 warning, all on lines this fix
  didn't touch — text-only edits, no logic changed). No Python files touched, `flake8` N/A.
- **Real UI verification** (same local-backend-through-SSM-tunnel + local-frontend + `e2e-test-admin`
  + Playwright setup as the reopened-fix verification above), against the `test-agency` pending
  tenant (a clearly-a-test tenant, chosen deliberately over reusing `test-n1-flow` — that one is
  already active from the prior verification run):
  1. Opened "Continue onboarding" → screenshotted the full Onboarding tab: "Seed atoms from
     Marketplace portfolio", "Assign content angle", "See the Mirror tab for atom count / runway
     before approving.", "Gate A — approve tenant activation", "Gate A is required and never
     auto-approved — the tenant only goes active after this button is clicked." — all English.
  2. Seeded from the same real finalized portfolio used in the reopened-fix verification
     (`d05c63e7-bede-4381-b120-51953cda7cd4`) → "Seeded from portfolio ... — 6 tours" (English,
     correct pluralization).
  3. All 7 angle buttons confirmed in English, visually, in order: "Culinary & people",
     "Physical & terrain", "Culture & craft", "Nature & wildlife", "Luxury & leisure",
     "Family & group experiences", "Wellness & spiritual".
  4. Clicked "Nature & wildlife" — captured the actual `PATCH .../angle` network request body:
     `{"assigned_angle":"nature_wildlife"}`, confirming the English display label still sends the
     exact fixed backend key, not the translated text. Button highlighted correctly in the UI.
  5. Automated diacritic scan of the full Onboarding-tab panel text, and of the whole page after
     expanding it, both returned `false` (no Vietnamese characters present).
  - Did **not** run this tenant through Gate A approve (stopped after angle-assign) — no reason to
    activate an extra real tenant for a copy-only verification; `test-agency` is left mid-onboarding
    (seeded + angle=`nature_wildlife`, `is_active` still `false`), same as before this run except for
    those two real, intentional writes.
- Local backend/frontend/SSM tunnel and all scratch files holding credentials were stopped/deleted
  after the run (same cleanup discipline as the reopened-fix verification above).
