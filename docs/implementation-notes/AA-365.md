# AA-365 [ACPv2][N8-5] — Gate C / trust ramp / veto window — Implementation Notes

## Round 3 — Trang identity gap, re-investigated (14/08/2026)

Round 1 (this doc's original STEP 0, below) concluded the reviewer-identity wiring was
unverified/likely absent, citing AA-232's own S86 note ("still 0 rows, still open") and a
`git log | grep admin_users` that only surfaced migration 074. That check stopped too early —
it missed **AA-252** and **AA-253**, two later, merged PRs that specifically closed this gap
and verified it live. Re-investigated per Nghiep's explicit instruction this round; full
correction is inline in the original STEP 0 bullet below (not deleted, corrected in place so
the history of what was believed when stays legible).

**Branch taken: wiring exists end-to-end (the DECISION RULE's first branch).** Evidence:
`shared.admin_users` has 2 real rows (`nghiep`, `admin`) as of AA-252, both logged into live by
Nghiep in a browser; `requireAdmin()` (AA-253) independently re-verifies the JWT on every BFF
proxy call before forwarding `x-admin-user-id`; `admin_pipeline.py` already writes that header
straight into a real `reviewed_by` UUID column; `middleware.ts` treats `role='reviewer'` as a
fully first-class session on every real admin page. None of this is hypothetical or
code-with-0-callers — it's live, used today by 2 real accounts.

**What this means for `trust_ramp.py`**: `confirm_ramp_transition(..., actor: str, ...)`
already takes `actor` as a plain, non-defaulted, caller-supplied parameter — it never hardcoded
a generic/placeholder value, so there is nothing to "fix" in that function's code for
attribution to work correctly. The moment any future API route wires `confirm_ramp_transition`
to a real endpoint, passing `actor=x-admin-user-id` (read off `request.headers`, same as
`admin_pipeline.py` already does for `reviewed_by`) would give a genuinely real, verified,
per-user-attributed Decision log entry — using the exact same mechanism already proven live,
not a new one. **No code change made to `trust_ramp.py` this round** — see "Should know" below
for why.

**Seeding Trang's row — DONE (14/08/2026).** Nghiep generated the bcrypt hash locally,
out-of-band (same convention as `nghiep`/`admin`), edited it into the scratchpad copy of
`seed_trang_admin_user.py` himself, and confirmed to proceed. Schema checked first (migration
074 is the only file that ever touches `shared.admin_users` — confirmed via
`grep -rl admin_users api/migrations/*.sql`, no later `ALTER TABLE`): 7 columns total, only
`username`/`password_hash` are `NOT NULL` with no default, both covered by the script's INSERT
— nothing missing, ran with zero guessed defaults. Executed via the S3-mediated ECS exec
pattern (`~/.claude/CLAUDE.md`) against the real dev DB, task
`d0e07b0e84f448e5bcc79e67d229f876`:

```
Inserted: {'id': UUID('c18c1400-c4f8-4feb-825c-e777255b724b'), 'username': 'trang',
           'role': 'reviewer', 'is_active': True,
           'created_at': datetime.datetime(2026, 8, 14, 3, 14, 35, tzinfo=timezone.utc)}
```

Real row, real id (`c18c1400-c4f8-4feb-825c-e777255b724b`), `role='reviewer'` — confirmed a
fully first-class session type today (`middleware.ts`'s `PROTECTED_ROUTES` already allows it on
every real admin page, see above). Script itself was never committed to this repo (same
convention as `nghiep`/`admin`) — it lived only in the session scratchpad and, briefly, the S3
bronze bucket's `scripts/` prefix (uploaded for the ECS exec step). **The S3 object was deleted
after the run** (`aws s3 rm s3://aa-cis-bronze-005097885195/scripts/seed_trang_admin_user.py`,
confirmed gone via `aws s3 ls` returning nothing) — nothing containing Trang's bcrypt hash is
left in S3. The local scratchpad copy on disk was intentionally left as-is (never committed,
not part of this repo).

## STEP 0 findings (14/08/2026)

- **N8-4/N8-4b lifecycle is real and current.** `services/acp_produce/packets.py`:
  `create_packet()`, `set_publish_mode()`, `assemble_packet()`, `maybe_mark_packet_ready()`,
  `deliver_packet()`. Schema: `acp_deliver.packets` (migration 094) — `publish_mode` column
  (`propose_only`/`approve_to_publish`/`veto_window_auto`, CHECK-constrained) **already IS**
  the 3-state ramp state described in this issue. No new column/migration needed.
- **F6 status: real AND wired into `run_gates()`**, contradicting this issue's own "if F6 is
  not fully wired" fallback clause. `gates.py::gate_route_to_sellable` is called from
  `pipeline.py::run_piece_through_produce_gates()` (AA-372) as gate 5 of 9 in the real DET
  chain (`output_rules → F1 → F2 → F3 → F4 → F6 → F7 → F8 → F9`). BUT
  `packets.py::ALLOWED_PUBLISH_MODES_UNTIL_F6 = frozenset({'propose_only'})` was deliberately
  left untouched through AA-364 and AA-367 — both implementation notes explicitly say not to
  widen it without a fresh Nghiep decision, none exists. Separately,
  `acp_deliver.tenant_tour_pages` has (had, as of AA-367's live verify) 0 rows for real tours,
  so F6 fails closed independent of the code guard. **This build does not widen the guard** —
  see Decisions.
- **`shared.admin_users` (AA-232, migration 074) exists** — bcrypt username/password login,
  `role` enum `('admin', 'reviewer')`, JWT (`/auth/admin-login`, `/auth/verify-admin`), FE
  wiring (`x-admin-user-id` header, `frontend/app/api/admin/[...path]/route.ts`), and
  `generated_content.reviewed_by`/`raw_tours.reviewed_by` FK columns pointing at it. This
  contradicts the local skill's stale claim of "single shared x-admin-secret, no per-user
  identity at all" — a real per-user mechanism exists in code and schema.
  **CORRECTION (AA-365 round 3, 14/08/2026)**: the "0 rows, never closed" claim above was
  itself stale — it only checked AA-232's own last note (S86) and `git log | grep admin_users`
  (migration 074 only), missing two later PRs that actually finished and *verified* the wiring:
  **AA-252** (fix/aa-252-admin-auth-bypass, merged) retired the legacy `ADMIN_SECRET` shared-
  secret login fallback entirely and confirmed live, via direct DB query, **2 real
  `admin_users` rows exist** (`nghiep`, `admin`, both `role='admin'`, `is_active=true`) — the
  fallback "served no one." Nghiep personally logged into both in a browser against the real
  backend and confirmed `/admin/dashboard` loads. **AA-253** (fix/aa-253-bff-route-auth,
  merged) added `requireAdmin()` (`frontend/lib/auth-server.ts`) to every `/api/admin/*`,
  `/api/pipeline/*`, `/api/tour-full/*` BFF proxy route — each now independently re-verifies
  the caller's JWT (`POST /auth/verify-admin`) before attaching `X-Admin-Secret` to the
  outbound backend request, and forwards `x-admin-user-id` (the verified JWT's `sub`, i.e. a
  real `admin_users.id`) alongside it. `api/routers/admin_pipeline.py`'s review-edit endpoint
  reads that header, parses it as a UUID, and writes it straight into `reviewed_by` — a real,
  currently-live, per-user-attributed column, not a placeholder. `frontend/middleware.ts`'s
  `PROTECTED_ROUTES` table (post-AA-252 rewrite) explicitly allows `role: "reviewer"` on every
  real admin page (dashboard, upload, pipeline, review, curation, brand, etc.) — a
  `role='reviewer'` account is a fully functional session type today, not second-class.
  **Conclusion: the wiring genuinely exists end-to-end and is live**, not merely present in
  code with 0 callers. No "Trang" user has been created yet — that part of the original
  finding still holds. See "Trang identity gap — resolved" below for what this session did
  about it.
- **Prototype confirmed at `docs/AI-gent-for automation works/aa-marketing-v2/aamc/`**:
  `delivery.py` G3 (`RAMP`, `publish_gates()`, `confirm_publish_mode()`), `config.py`
  (`VETO_WINDOW_HOURS = 48`). Ported the RAMP list and the suggest/confirm split; did not port
  G1 `assemble_packet`/G2 `veto_loop`/G4 `shot_debt` — out of this issue's scope (N8-4/N8-4b
  already cover assemble/lifecycle; veto response handling and shot_debt are not asked for
  here).
- **No `funnel_stage`/"pricing" flag on `packets` or `pieces`.** The real (non-prototype)
  `Slot` model (`services/acp_planning/models.py`) has `funnel_stage: FunnelStage` (`TOFU` /
  `MOFU` / `BOFU` / `OFF`), persisted by `persist_slot_grid()` into
  `acp_shared.acp_v2_slots.payload` (jsonb, AA-377/migration 096). `acp_deliver.pieces.slot_id`
  is the join key. Used `funnel_stage='BOFU'` as the pricing/booking-intent proxy for the hard
  block — no dedicated "pricing" flag exists anywhere in the schema. BOFU is also the funnel
  stage this codebase already treats as sales-closing (`acp_planning/constants.py`
  FRAMEWORK_TABLE: `('BOFU', 'blog') -> AIDA`).
- **AA-CIS-Infra `modules/eventbridge/main.tf`**: `acp_daily_ingest` (06:00 UTC daily) and
  `acp_weekly_export` (Sun 23:00 UTC) are the pattern to copy — both just `PutEvents` an
  EventBridge Scheduler → custom bus (`acp_events`) → no `aws_cloudwatch_event_rule` currently
  consumes either event (confirmed via repo-wide grep) — comments on both say a rule gets
  added "when [the downstream Lambda] is provisioned." The new veto-window-close schedule
  follows the identical shape and is, by the same convention, inert until a consuming rule
  exists.

## Follow-up decision — F6 guard unlocked (Nghiep, 14/08/2026, same day as STEP 0)

Closes the "needs a fresh Nghiep decision" note that AA-364 (05/08/2026) and AA-367
(06/08/2026) both left open, and reverses this doc's own original first Decision bullet below
(kept further down, struck through in spirit not in text, for the historical record).

**Decision**: now that STEP 0 confirmed F6 (`gate_route_to_sellable`) is real and actually
wired into `run_gates()` (not just present as a function — AA-372, 5th of 9 gates in the real
DET chain), Nghiep gave the explicit go-ahead this issue's DECISIONS ALREADY MADE section was
waiting on: widen `ALLOWED_PUBLISH_MODES_UNTIL_F6`.

**Exact lines changed** (`services/acp_produce/packets.py`):
- Module docstring (top of file): added a "RESOLVED — AA-365 (14/08/2026)" paragraph recording
  this decision; kept the original AA-364/AA-367 narrative below it as "Historical context",
  not deleted.
- `ALLOWED_PUBLISH_MODES_UNTIL_F6 = frozenset({"propose_only"})` →
  `frozenset({"propose_only", "approve_to_publish", "veto_window_auto"})`. Constant NOT
  renamed (kept for traceability across the AA-364/AA-367/AA-365 history).
- `set_publish_mode()` docstring updated to describe the membership check as a general
  invalid-mode guard now, not an active F6 block.
- `PublishModeBlockedError`'s exception message changed from `"blocked until F6 ... exists"`
  to `"is not a valid mode"` — the old wording became actively false once F6's condition was
  satisfied; the check itself (raise on non-membership) is unchanged code shape, only the copy.

**What did NOT change**: the BOFU/pricing hard block
(`services/acp_produce/trust_ramp.py::packet_has_bofu_piece()` / `BofuVetoBlockedError`) is
untouched and checked before `set_publish_mode()` is ever called in
`confirm_ramp_transition()` — a BOFU packet still cannot reach `veto_window_auto` regardless of
this unlock (verified by
`test_confirm_veto_window_auto_still_blocked_for_bofu_packet_after_aa365_unlock`, see Changed).
F6's own per-piece fail-closed behavior (dead/missing `tenant_tour_pages` route) is also
unchanged and still applies at the gate level, independent of this module.

**Tests updated to match** (expected changes, not regressions — `test_aa364_packets.py`'s own
counter-proof test literally said "if this ever changes, it must be a deliberate edit
accompanying a new decision," which this is):
- `tests/unit/test_aa364_packets.py`: `test_allowed_modes_is_exactly_propose_only_today` →
  `test_allowed_modes_is_all_three_ramp_states_since_aa365` (assertion widened to match);
  `test_set_publish_mode_approve_to_publish_blocked` and `..._veto_window_auto_blocked` →
  `..._succeeds_since_aa365` (now assert success, not `PublishModeBlockedError`);
  `test_set_publish_mode_error_cites_f6_and_the_decision_date` replaced with
  `test_set_publish_mode_rejects_invalid_mode_string` (the F6-specific message no longer
  exists to test).
- `tests/unit/test_aa365_trust_ramp.py`: added
  `test_confirm_approve_to_publish_succeeds_and_logs_since_aa365_unlock`,
  `test_confirm_veto_window_auto_succeeds_for_non_bofu_packet_since_aa365_unlock`,
  `test_confirm_veto_window_auto_still_blocked_for_bofu_packet_after_aa365_unlock` (renamed
  from the pre-unlock version, same assertions — BOFU block is unaffected by the unlock),
  `test_ramp_and_allowed_publish_modes_stay_in_sync`,
  `test_full_ramp_propose_only_to_veto_window_auto_for_non_bofu_packet`. Removed the two tests
  that asserted the old blocked-by-F6-guard behavior for `approve_to_publish`/
  `veto_window_auto` on a non-BOFU packet, since that behavior no longer exists.
- Full suite: **1190 passed** (was 1188 before this issue's first round, +14 from the initial
  `trust_ramp.py` tests, net +2 more from this follow-up's added/renamed tests), 0 failures.

## Decisions

- ~~Do not widen `ALLOWED_PUBLISH_MODES_UNTIL_F6`~~ — **superseded same-day**, see "Follow-up
  decision" above. Original reasoning kept for the record: even though F6 was technically
  wired into `run_gates()`, the STEP 0 session alone had only confirmed the Trang-scoping
  question, not this guard — so the first round left `confirm_to_publish`/`veto_window_auto`
  blocked with `PublishModeBlockedError`, exactly as AA-364/AA-367 had left them, until Nghiep
  gave the explicit call recorded above. `suggest_ramp_transition()` still only computes what
  the ramp *would* suggest (pure function, no DB, no side effect) — confirming remains a
  separate call, matching the prototype's "agency-confirmed, never silently switched" framing;
  what changed is that confirming can now actually succeed.
- **BOFU hard-block checked before, and independently of, the F6 guard** — so it still holds
  even after the F6 guard's widening above. Implemented as a real DB join
  (`packet_has_bofu_piece()`), not a comment/convention. This is the one constraint the
  follow-up decision explicitly did NOT touch — verified by a dedicated post-unlock test.
- **Decision log reuses `acp_shared.audit_log`** (migration 030) rather than inventing a new
  table — same table every other real ACP gate/approval action in this repo already writes to.
  `action='publish_mode_transition'`, `resource_type='packet'`, `details` jsonb carries
  `from`/`to`/`blocked`/`block_reason`. Every `confirm_ramp_transition()` call writes exactly
  one entry, whether it succeeds or is blocked — a blocked escalation attempt must stay visible
  in the log, never silently dropped.
- **No new UI for Trang's veto action.** Per the issue's own instruction: the admin surface
  cannot scope an action to a specific reviewer identity in any way that's actually wired end
  to end today (see STEP 0 admin_users finding) — building UI on top of that would be cosmetic.
  Reported as a real product-decision gap instead (see AFTER DONE).
- **No new migration.** `packets.publish_mode` (094) is the ramp state; `acp_shared.audit_log`
  (030) is the Decision log; the BOFU join uses existing columns (`pieces.slot_id`,
  `acp_v2_slots.payload`, both already live). Nothing to add at the schema level.

## Changed

- `services/acp_produce/trust_ramp.py` (new): `RAMP`, `suggest_ramp_transition()`,
  `packet_has_bofu_piece()`, `confirm_ramp_transition()`, `BofuVetoBlockedError`. Docstrings
  updated in the follow-up round (see above) to describe the F6 guard as unlocked, not blocked.
- `services/acp_produce/packets.py` (follow-up round only): `ALLOWED_PUBLISH_MODES_UNTIL_F6`
  widened to all 3 ramp states; module docstring, `set_publish_mode()` docstring, and
  `PublishModeBlockedError`'s message updated for accuracy — see "Follow-up decision" above for
  the exact line-level diff.
- `tests/unit/test_aa365_trust_ramp.py` (new, then updated in the follow-up round): unit tests,
  mocked `db` following `test_aa367_packets.py`'s convention.
- `tests/unit/test_aa364_packets.py` (follow-up round only): 4 tests updated to match the
  widened guard — see "Follow-up decision" above.
- `AA-CIS-Infra/modules/eventbridge/main.tf`: added `aws_scheduler_schedule.acp_veto_window_close`
  (Wed 18:00 UTC), copying `acp_weekly_export`'s exact shape. Terraform not applied — human-only
  apply, PR only, per house rule.

**Status (14/08/2026, Nghiep): code-complete, not applied — separately tracked, not an AA-365
blocker.** Investigation found the root that calls `module "eventbridge"` (top-level
`AA-CIS-Infra/main.tf`) has an orphaned S3 backend (`aa-cis-tfstate-867490540162` doesn't
exist) and neither AWS account has any of this module's resources live today (`old`/`new`
schedules alike — confirmed via `aws scheduler list-schedule-groups` / `list-event-buses`
directly, not Terraform state). Pre-existing, not caused by this change. Nghiep filed
**AA-402** ("move eventbridge module to `accounts/aa365`, acc2 — backend was orphaned, nothing
ever actually live") to fix this as its own piece of work. The `acp_veto_window_close` diff
here stays exactly as written — correct code, just not yet appliable until AA-402 lands. Does
not block AA-365's Python side (`trust_ramp.py`/`packets.py`/tests), which has no dependency on
this and can commit/PR/merge on its own.

## Tradeoffs

- ~~The ramp is currently suggestion-only in practice for anything past `propose_only`~~ —
  **resolved by the follow-up decision above (14/08/2026)**: `confirm_ramp_transition()` can
  now actually move a non-BOFU packet through all 3 states. Kept here for the record of what
  was true in the first round.
- **Veto-window-close schedule has no consuming Lambda/rule yet**, same as the two schedules it
  copies — this issue only asked for the Terraform resource, not a handler. What "closing" the
  window should actually DO (auto-transition eligible packets? require a `veto_deadline`
  column that doesn't exist on `packets` today — see migration 094's own comment, "no
  consuming Linear issue yet") is unbuilt and unscoped; flagged as a follow-up, not attempted
  here.

## Should know

- `packets.publish_mode` already had 3 valid values matching the ramp before this issue —
  reviewers should not expect a new column in the diff.
- `confirm_ramp_transition()` is the only new call site for `packets.set_publish_mode()`
  besides direct test/verify-script callers. The follow-up round DID change
  `set_publish_mode()` itself (the `ALLOWED_PUBLISH_MODES_UNTIL_F6` widening) — first-round
  `trust_ramp.py` called it unchanged; this line is corrected from the first-round draft, which
  predated that change.
- The BOFU check queries `acp_shared.acp_v2_slots` (AA-377/378, migration 096), a table that
  did not exist before AA-377 — any packet/piece created against the older `acp_shared.acp_runs`
  path (pre-096) would have no matching slot row and `packet_has_bofu_piece()` would correctly
  return `False` for it (no match, not a false positive) — but also can't be proven BOFU either
  way. Fine for current dev state (096 re-pointed the only live FK), flagged in case older data
  ever resurfaces.
