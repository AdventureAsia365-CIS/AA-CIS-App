# AA-323 — N4-N6 UI Phase 2 (4 gaps)

## STEP 0 findings (2026-08-13, 3 rounds before this build — see prior session transcript)

- Preview page confirmed still at `frontend/app/admin/curation/preview/page.tsx`, unchanged
  since AA-300. Gate B UI (`/admin/quarter-plan`, AA-388) and per-tenant `posts_per_week`
  (`/admin/tenants`, AA-384) both shipped *after* the issue was written and partially close
  Gap 1/Gap 3 — but neither route links to/from Preview, so the 4 gaps as experienced from
  Preview itself were all still open going into this build.
- Round 2 (browser discrepancy check) confirmed: Preview's `admin-preview-demo` auto-approve
  runs REAL DB queries (`fetch_trips`/`fetch_atoms_by_trip`) every call — "DEMO" describes the
  in-memory, unpersisted Gate B bypass, not fake data. `allocate_month_from_db()`
  (`allocator.py:266-281`, the function that reads a real Gate-B-approved plan) existed with
  **zero callers** anywhere in the router layer before this build — Preview always took the
  demo path unconditionally.
- `capacity_posts_per_week` duplicate-source risk: `shared.tenants.posts_per_week` (migration
  099, AA-384) already IS this value ("free tenant choice"). Gap 3's new `tenant_config` table
  holds `markets`/`channels` only — capacity is read via a join, not duplicated (see migration
  101's header comment for the full reasoning).
- `markets`/`channels` confirmed multi-value by design (not single-value) by reading
  `runway.py`'s `for mk in markets` loop and `allocator.py`'s `channels[slot_n % len(channels)]`
  round-robin — `tenant_config` columns are `TEXT[]`, matching that shape.
- Scoring math (`quarter.py:117-131`: `runway_fit`/`richness`/`dist`/`forced`) was computed per
  trip but never returned past the aggregate `destination_shares` — Gap 1's "giải trình điểm số"
  requirement needed a new `TripScore` field on `QuarterPlan`, not just UI work.
- `POST /admin/quarter-plan` had no exclude mechanism — `specials[]` only force-includes by
  fuzzy name match, nothing lets a caller remove an auto-selected trip. Decision #3 (Nghiep):
  reuse `specials[]` for "add", add a new `excluded_trip_ids: list[UUID]` for "remove" — no
  change to the scoring weights themselves.

## Decisions

- Gap 1 "who can create a pending plan": no new role gate. `/admin/quarter-plan/create` sits
  under the existing `/admin/quarter-plan` middleware prefix (`roles: ["admin"]`) — same as
  Gate B review itself. **The system has no separate Trang/Nghiep role today** (only
  admin/reviewer/content/tenant) — flagged to Nghiep in the final report, not invented here.
- `TripScore` is additive on the `QuarterPlan` pydantic model — `quarter_plan_version.payload`
  is `JSONB` (migration 092), so no migration needed for this field; old persisted rows without
  it just default to `[]` on read.
- New read-only `POST /admin/quarter-plan/preview` endpoint (no persist) powers the live
  checkbox override UI; the existing persisting `POST /admin/quarter-plan` is only called once,
  on final "Create Quarter Plan for Review".
- Preview endpoint (`admin_atoms.py`) now checks `fetch_approved_quarter_plan()` first and only
  falls back to the in-memory `admin-preview-demo` auto-approve path when no approved version
  exists for the tenant/year/quarter — this is what makes Gap 1's full chain (create → Gate B
  approve → N6 reads the real version) observable end to end from the existing Preview screen,
  with no new N6 "production" endpoint needed.

## Round 4 (2026-08-13) — Nghiep's real-browser walkthrough, 3 follow-ups

Not new bugs — Nghiep verified the create → approve → preview chain end to end in the real
browser (something STEP 0 flagged as still needing a live pass) and found 3 UX gaps that are a
direct continuation of the same "chuỗi thao tác rời rạc" problem this issue was already fixing.

### Việc 1 — History tab (view v1..vN, not just the newest)
- No endpoint listed every version for a tenant/year/quarter — `GET
  /quarter-plan/{tenant}/{year}/{quarter}` only ever returns the latest (`ORDER BY version_no DESC
  LIMIT 1`). New `GET /quarter-plan/{tenant}/{year}/{quarter}/history` (`api/routers/admin.py`)
  returns every version, newest first, **including the full plan payload per row** — not just
  summary fields — so the frontend never needs a second round-trip per row.
- `frontend/app/admin/quarter-plan/page.tsx`: new `TabBar` (Pending / History, reusing
  `adminUi.tsx`'s existing `TabBar` — not a new component) on the list screen. History tab is its
  own tenant/year/quarter selector (same shape as the Create Plan page's picker, since the
  underlying endpoint is scoped to one tenant/quarter — there's no cross-tenant "all history"
  query, matching how the endpoint itself is scoped) + a version table. Clicking a row builds
  `VersionDetail`/`PendingItem` directly from the already-fetched payload and reuses the exact
  same `DetailView` the Pending tab uses — no new detail UI, per Nghiep's "reuse DetailView"
  instruction.
- Decision: put History in a tab on the same page rather than a new `/admin/quarter-plan/history`
  route (Nghiep's stated preference to avoid an extra nav hop). No `middleware.ts` change needed —
  it's the same `/admin/quarter-plan` prefix, already gated.

### Việc 2 — sticky container regression + scope extension
- Found while implementing this round: PR #140's round-3 sticky fix (`position: sticky` on the
  Create button) was **silently dropped** by the later `ba383d8` "N4-N6 UI Phase 2" commit, which
  rewrote `create/page.tsx` from a pre-round-3 base. `origin/main` (post both merges) still had it
  correctly, but it's worth flagging: a big feature commit landing on a stale local branch is how
  this kind of regression slips through — checked `git merge-base`/`git log --graph` against
  `origin/main` before starting this round specifically because of that risk.
- Per the actual ask: merged the pre-submit button and the post-submit "created" banner into ONE
  sticky container that swaps its children by `createdVersionId` state, instead of two separate
  blocks (one sticky, one a plain `Card` appended after it — which reproduced the exact
  scroll-to-find problem the round-3 fix was meant to solve, just one screen-state later).

### Việc 3 — Approved → Preview link
- Added a "View in Slot Grid Preview" button on `DetailView`'s approved-status block, linking to
  `/admin/curation/preview`. Deliberately a **plain navigation link**, not a tenant-scoped deep
  link with `?tenant_id=`/year/quarter query params: `GET /admin/atoms/preview-slotgrid` (the
  endpoint Preview calls) hardcodes `today.year`/`(today.month-1)//3+1` for its quarter — it does
  not read year/quarter from the URL at all — so passing a specific version's year/quarter through
  would silently be ignored and could mislead a reviewer into thinking Preview reflects the
  version they just came from. Wiring that up for real is out of this round's 3-item scope; noted
  here rather than silently doing it or silently skipping it.

## Round 5 (2026-08-13) — Preview version investigation + create-page field audit

### Việc 1 — CONCLUSION: NO BUG. Live DB + live API evidence below.

Nghiep's report (v6 created with 8 different trips than v5, approved, Preview's summary cards
"looked the same" after Recompute) was treated as a serious bug report, not a false alarm, and
investigated against the real live DB/API (AWS was live this session — ECS running, RDS
`available`), not code-reading alone.

**Evidence gathered** (read-only S3-mediated ECS exec — `acp_shared.quarter_plan`/
`quarter_plan_version` for `tenant_id=00000000-0000-0000-0000-000000000001` (aa_internal),
year=2026, quarter=3):
- `quarter_plan.current_version_id` = `98af1ff5-...` = v6's `version_id`. Confirmed correct — not
  stuck on v5 or an older version.
- **v1 through v6 are ALL `approval_status='approved'`** in `quarter_plan_version` — approving a
  new version does NOT flip older versions back to a non-approved status, it only moves
  `quarter_plan.current_version_id` (this is `approve_quarter_plan_version()`'s actual, correct
  behavior — see `services/acp_planning/quarter.py:364-379` — not a bug, but worth knowing since it
  means "approval_status='approved'" alone never identifies the live version; only
  `current_version_id` does).
- Called `GET /admin/atoms/preview-slotgrid` directly via `curl` from inside the running ECS
  container (real deployed code, not a re-derivation) — its `quarter_plan.trip_ids` **exactly
  matched v6's 8 trip_ids from the DB, byte for byte** (`MATCH: True`). `demo_params.demo_mode`
  was `False` ("Reading a real Gate B-approved quarter plan"), confirming the real-DB path was
  taken, not the in-memory demo fallback.
- **Root cause of the visual "looks the same"**: v5 and v6 only share 3 of 8 trips (5 actually
  differ), but `destination_shares` is `{"South Korea": 1.0}` for BOTH versions, and both have
  the same trip COUNT (8). Read `frontend/app/admin/curation/preview/page.tsx`'s "Quarter plan"
  summary card (line ~311, pre-round-5): it renders only `Q{quarter} {year} — {trip_ids.length}
  trips` + a destination-shares bar chart — **it never rendered individual trip names or ids**, so
  two versions with the same count and same 100%-single-destination split are visually
  indistinguishable on that card regardless of which 8 trips are actually selected. Slot cards
  further down show channel/funnel-stage/keyword/atom-count, not trip name either. This is exactly
  the mechanism Nghiep's own hypothesis in the round 5 brief predicted ("output slot pattern
  giống do cùng destination") — confirmed with real data, not assumed.
- Also relevant: `runway_cell_count` (36, shown in the first summary card) is computed by
  `runway_map()` from the tenant's full trip/atom pool for the market, independent of which trips
  a specific quarter plan version selected — it would read identically across ANY version for the
  same tenant/year/markets, by design (N4 Runway Map is destination-level staging, not
  per-quarter-plan-version). Not a bug; a different concern than "which version is this."
- **No code fix needed for Việc 1** — the version-tracking mechanism (`current_version_id`,
  `fetch_approved_quarter_plan`, the live `preview-slotgrid` endpoint) all read correctly end to
  end. Việc 2 (version label) is the fix for the *visibility* gap this uncovered.

### Việc 2 — version number on Preview
- New `services/acp_planning/quarter.py:fetch_current_version_no()` — a small separate lookup
  (not folded into `fetch_approved_quarter_plan`'s return shape, to avoid touching that function's
  contract and its 5 existing call sites/tests). Returns `None` in demo mode (nothing persisted
  yet to have a version number).
- `api/routers/admin_atoms.py`'s `preview_slotgrid` now returns `quarter_plan_version_no` in the
  response.
- `frontend/.../curation/preview/page.tsx`: "Quarter plan" card now reads
  `Q3 2026 · v6 — 8 trips` when a real version is active, unchanged (no `· vN`) in demo mode.
- New tests: `tests/unit/test_aa320_quarter_plan_persist.py::TestFetchCurrentVersionNo` — includes
  a test mirroring the exact multi-approved-versions scenario this round's live investigation
  found (v1 and v2 both `approved`, must resolve to v2 — the current one, not the first approved).

### Việc 3 — Approved → Preview link
- Old text ("Read-only — nothing here is written to the database") was accurate about writes but
  read as "this whole page is a simulation," which stopped being true once `allocate_month_from_db`
  started reading real Gate-B-approved plans (AA-323 Gap 1, this issue). New copy splits the two
  ideas: what's being read (a real approved plan when one exists, else an unpersisted preview) vs.
  the write guarantee (computing the slot grid never writes to the DB either way).

### Việc 4 — create-page trip-selection field audit (investigation only, no UI changes)

Read `services/acp_planning/quarter.py:109-189` (`_score_reason`, `compute_quarter_plan`) to get
ground truth for every field on `/admin/quarter-plan/create`'s trip rows, then pulled v6's full
`trip_scores` array (763 rows — one per non-retired trip, not just the 8 chosen) from the live DB
to check real distributions rather than guessing.

| Field on screen | What it actually is | Notes |
|---|---|---|
| Trip name / Destination | `Trip.name` / `Trip.destination` | As shown, no surprise. |
| Numeric score (e.g. 1.73/0.73/0.00) | `TripScore.score` — a **composite**: `runway_fit*0.4 + richness*0.3 + distinctiveness_score*0.3 + (1.0 if forced else 0)`. Not a single metric. | Max for a non-forced trip is 1.0; forced trips can reach up to 2.0 (the +1.0 bonus is why 1.73 > any possible non-forced score). |
| "Manually added" reason text | Shown **exactly when** `forced=True`, i.e. the trip's name/destination fuzzy-matched one of the `specials` strings (populated when a human clicks the checkbox to add a trip N5 didn't auto-select). `_score_reason()` checks `forced` first and short-circuits — the 3-way score breakdown below is never computed/shown for a forced trip. | |
| "FORCED" badge | **The exact same `forced` boolean** as "Manually added." | Confirmed by reading both code paths — `create/page.tsx`'s `{t.forced && <Badge>forced</Badge>}` and `_score_reason`'s `if forced: return "Manually added"` both key off `TripScore.forced`. Currently the UI shows **two separate labels for one field** on the same row — redundant, not two different concepts. |
| "High runway fit (BOFU/MOFU window this quarter)" reason text | One of 3 possible labels, meant to name whichever of `{runway_fit*0.4, richness*0.3, dist*0.3}` is numerically largest for that trip (`max(contributions, key=...)` over a 3-entry dict). | **CONFIRMED BUG, not a placeholder illusion** — evidence: of the 763 trips in v6's live payload, 594 (78%) show this exact text, and of those, **562 have `runway_fit=0.0`** (real example: Bhutan trips "THIMPHU FESTIVAL" etc., `runway_fit=0.0, richness=0.0, distinctiveness_score=0.0`, reason still "High runway fit..."). Root cause: when all three weighted contributions tie at exactly `0.0` (the common case — most trips have neither a BOFU/MOFU runway window nor curated atoms yet), Python's `max()` on a dict returns the **first** key in insertion order, and `"High runway fit..."` happens to be listed first in `_score_reason`'s `contributions` dict (`quarter.py:117`). The label is not just uninformative for these rows, it is **factually the opposite of true** (claims high runway fit when the value is the minimum possible, 0.0). |
| "Rich atom pool" / "High-distinctiveness atoms" reason text | Same mechanism, chosen correctly when they ARE the max non-tied contributor — 136 trips in the same payload show "Rich atom pool" legitimately (their `richness*0.3` really was the largest term). | Not buggy on their own — only the tie-at-zero case is wrong. |

## Round 6 (2026-08-13) — version-scoped Preview, tenant filter fix, trip table rebuild, sidebar reorder

Final round before AA-323 is treated as closed; Nghiep moves to acc3 Bedrock
satellite work (AA-397/AA-398) after this.

### Phần A — version-scoped Slot Grid Preview

- `services/acp_planning/quarter.py`: new `fetch_quarter_plan_version(version_id,
  pool)` — reads one specific persisted version by its global `version_id` PK
  (tenant/year/quarter come from the row itself via the `quarter_plan` join, so
  no other params are needed), regardless of `approval_status`. Mirrors
  `fetch_approved_quarter_plan`'s "never raises, caller decides" shape.
- `api/routers/admin_atoms.py`'s `preview-slotgrid` gains optional
  `?version_id=`. When given: resolves tenant/year/month from that version's
  own row (not `tenant_id` query param, not `date.today()`); 404s on an
  unknown id; 400s with a clear message if the version isn't `approved`
  (Gate B — a pending/rejected version must never feed N6, historical or
  not); computes against the target quarter's **first month** (a past
  quarter has no "current month" of its own). Omitting `version_id`
  preserves every pre-round-6 behavior byte-for-byte (same code path, just
  reached via an `else` branch instead of being the only path).
- `frontend/app/admin/quarter-plan/page.tsx`: History tab rows now show a
  second "Slot Grid Preview" button (only on `approved` rows — pending/
  rejected can't feed N6) linking to `/admin/curation/preview?version_id=`.
  The existing Approved-detail "View in Slot Grid Preview" button (round 4)
  now also passes `selected.version_id` — previously always linked to
  "whatever's current," which was silently wrong once opened from an old
  History row.
- `frontend/app/admin/curation/preview/page.tsx`: reads `version_id` via
  `useSearchParams()` (Suspense-wrapped, same AA-345-round-5 pattern as
  `curation/page.tsx` — never a lazy `window.location` read). Shows
  "(historical)" next to the version label when present.
- Tests: `TestFetchQuarterPlanVersion` (4 cases, `test_aa320_quarter_plan_persist.py`)
  + `TestPreviewSlotgrid`'s 4 new `version_id` cases (`test_aa300_admin_atoms.py`,
  including a fixture-shape gotcha noted inline: asyncpg returns `tenant_id`
  as a real `UUID`, not the row's raw string — matching it wrong trips
  `compute_slot_grid`'s cross-tenant guard). 1164 unit tests total (was 1153
  going into this round), flake8 clean, `tsc --noEmit` clean.
- **Verify (browser)**: `/admin/quarter-plan` → History tab → pick
  aa_internal/2026/Q3 → Load History → any `approved` row → "Slot Grid
  Preview" button → lands on `/admin/curation/preview?version_id=...` with
  "Q3 2026 · vN — N trips (historical)" in the Quarter Plan card. Compare
  against the plain `/admin/curation/preview` (no query) — should show the
  *current* version's number instead, unaffected.

### Phần B — tenant filter investigation: CONFIRMED BUG, fixed (Nghiep decision)

STEP 0 for this round hypothesized this was likely correct behavior ("chưa
được cấp portfolio nên chưa có gì để chọn"). Live-DB investigation (S3-mediated
ECS exec, real RDS, aa365-admin) found the opposite — **this was a real filter
bug**, not by-design:

- `services/acp_planning/runway.py`'s `fetch_trips()` queried
  `acp_contract.v_trip_registry WHERE tenant_id = $1` — that column is backed
  by `raw_tours.tenant_id` (who *uploaded/owns* the tour row), not
  `acp_shared.tenant_atom_state` (who's been *licensed* the tour via N1 Gate
  A / Marketplace). Confirmed live: `raw_tours` has rows for **aa_internal
  only** (793/793) — no B2B ingestion pipeline has ever written a
  `raw_tours` row for any other tenant, so this filter returned 0 rows for
  every tenant except aa_internal, unconditionally.
- Proved this wasn't explainable by "tenant hasn't onboarded yet": tenant
  `TEST-N1-flow` (`6fbaf284-...`) has a REAL `acp_shared.tenant_onboarding`
  row with `approval_status='approved'` (Gate A passed, 2026-08-12) and 6
  real `tour_id`s in `acp_shared.tenant_atom_state` — all 6 verified to
  exist as valid trips in `v_trip_registry` (e.g. "CLASSIC LAOS", "Northern
  India: Riverside Safaris..."). Despite a fully completed real onboarding,
  `v_trip_registry WHERE tenant_id = 'TEST-N1-flow'` still returned **0
  rows** — the eligibility query simply never looked at the table N1
  onboarding actually writes to. No B2B tenant (including this one) has
  ever had a single `quarter_plan_version` row, confirmed live — eligible
  trips was always 0 for all of them.
- **Decision (Nghiep, confirmed directly, not inferred)**: this is still the
  development stage — product/UX quality takes priority over licensing
  gates right now. Rather than wire N5 eligibility to
  `tenant_atom_state`/Marketplace licensing (a bigger, product-judgment
  change — does a licensed portfolio *replace* scoring or filter it? can one
  platform trip serve multiple tenants concurrently in the same quarter?),
  every tenant TEMPORARILY reads the same full platform catalog (== today's
  aa_internal view, 763 trips) until Marketplace/N1 licensing (D3/D4, PRD
  ACP v2) is actually built and deliberately wired into eligibility.
- **Fix**: `fetch_trips()`'s query drops `WHERE tenant_id = $1` entirely
  (still takes `tenant_id` as a param — unused for filtering now, kept
  because every caller's signature and `RunwayMap.tenant_id` still need it).
  Marked `# TEMP (2026-08-13, Nghiep decision)` in code with the full
  reasoning inline, specifically so it doesn't read as final design to a
  future STEP 0. **Nothing else touched** — `tenant_atom_state`, Marketplace,
  N1 endpoints all untouched, per Nghiep's explicit scope limit.
- **Known residual gap, flagged not fixed (same scope boundary)**:
  `services/acp_planning/quarter.py`'s `fetch_atoms_by_trip()` has the
  *identical* `WHERE rt.tenant_id = $1` pattern (via its own `raw_tours`
  join) for pulling a trip's atoms — structurally the same bug, one level
  down. Left untouched per Nghiep's "chỉ riêng phần N5 eligible trips, không
  động vào logic khác" — but worth knowing: a B2B tenant now sees real trip
  *names* in the create-plan checklist, but `richness`/`distinctiveness`
  score straight to 0 and N6's slot atoms will be empty for them today,
  since atoms are still fetched tenant-scoped. Not materially different
  from many of aa_internal's own not-yet-atomized trips today (which also
  show richness=0), but this is the next thing to revisit alongside the N1
  licensing wiring — not raised as a surprise fix, just named so it isn't
  mistaken for "fully solved."
- Live-verified the fix's data effect directly (SQL-level, not yet the
  deployed API — this branch isn't merged/deployed): old query
  (`WHERE tenant_id=$1`) → 0 rows for both `TEST-N1-flow` and `WanderLux`
  (a tenant that's never even started N1); new query (no filter) → 763 rows,
  same for every tenant. Full API/browser verification needs this branch
  merged and deployed (CI/CD, human-merge-gated) — same caveat as every
  prior round's "Should know."
- Tests: `test_aa301_runway.py::TestFetchTripsDbWrapper` updated (query no
  longer contains `WHERE tenant_id`) — 1164 tests still green.

### Phần C — trip-selection table rebuild (round 5's 4 proposals, now built)

- `services/acp_planning/quarter.py`'s `_score_reason()`: fixed the tie-break
  bug round 5's live audit found (562/763 real trips mislabeled "High
  runway fit" while `runway_fit=0.0`, because `max(dict, key=...)` silently
  resolves any tie to whichever key is listed first). Now: an all-zero (or
  tied-at-zero) contribution set reads "No runway or atom signal yet this
  quarter" instead; a genuine tie among nonzero contributors reads "Balanced
  score (multiple factors tied)"; a real single dominant factor still gets
  its correct label. `forced` still short-circuits to "Manually added"
  before any of this, unchanged. 6 new tests, `TestScoreReasonTieBreak`
  (`test_aa301_quarter.py`) — this function had zero test coverage before
  round 6, which is exactly why round 5's bug shipped unnoticed.
- `frontend/app/admin/quarter-plan/create/page.tsx`: the flat scrolling
  checkbox list (763 rows for aa_internal, one unbroken column of text) is
  now a real `<table>` — Trip / Destination / Status / Score / Why columns,
  reusing `adminUi.tsx`'s `TH`/`TD` tokens (not new one-off styles). Whole
  row is clickable (not just the checkbox) to toggle selection.
- "FORCED" badge + "Manually added" reason text were two labels for the
  same `forced` boolean (round 5 finding) — collapsed into one **Status**
  column: `Manually added` (amber badge) / `Auto-selected` (green badge) /
  `Not selected` (muted text) for the ~750+ non-selected candidate rows.
  The **Why** column now shows the (now-correct) reason text only for
  non-forced rows — a forced row's "Why" would just repeat "Manually added"
  a second time, so it's left as "—" there instead.
  Score column has a real header (not a bare unlabeled number in a row) plus
  a hover tooltip spelling out the composite formula.
- Pagination: reused `_components/Pagination.tsx` as-is (same "50 per
  screen" convention as AA-300's atom list) rather than pulling in a new
  virtualization dependency — client-side slice of the already-fetched
  `trip_scores` array, since `POST /admin/quarter-plan/preview` returns the
  full candidate list in one response (no server-side pagination exists on
  that endpoint, and adding one was out of this round's scope). Page resets
  to 1 on every fresh preview response — toggling a checkbox re-sorts the
  list server-side (a newly-forced trip's score jumps up), so a fixed page
  index could otherwise silently show different trips after a re-preview.
- Sticky submit container (round 3/4) untouched — still wraps the table's
  parent `Card`, unaffected by the table rebuild.
- Verify (browser, once merged/deployed): `/admin/quarter-plan/create` →
  pick aa_internal → real table renders with Score/Status/Why columns,
  pagination controls under the table (50/page), checking/unchecking a row
  re-previews and Status flips between Auto-selected/Manually added/Not
  selected correctly, no row shows both a badge and a redundant "Manually
  added" Why text.

### Phần D — sidebar reorder (STEP 0 recon first, per the round's explicit risk warning)

**STEP 0 recon (code-verified, not guessed)**: the sidebar was assumed to
be one N0→N8 flow with historical clutter. It's actually **three separate
pipelines**:
1. Real ACP v2 (N0-N8): Tenants(N1), Marketplace(N1), Atomize(N2), Atom
   Curation(N2 — deep-links into N4-N6's Runway Map/Slot Grid Preview),
   Quarter Plan Gate B(N5). Only 5 of 17 nav items.
2. AA-internal's own content-authoring pipeline (Upload/S1 Rewrite/Review/
   Brand/Master Content) — confirmed via each page's header comment
   (`api/admin/tours`, `run-tour`, HITL review of `generated_content`) —
   an older system for AA's own tour copy, unrelated to the B2B tenant
   flow N0-N8 describes.
3. ACP v1 (`/admin/pipeline/s2`/`s3`/`s4-blog`/`s4-social`, labeled "ACP
   Pipeline" before this round) — confirmed via each page's header comment
   (`/api/admin/acp/runs`, `/api/admin/acp/gate/s2/approve`, etc. —
   `admin_acp_proxy.py`) to be a separate, still-live legacy B2B pipeline
   predating the N0-N8 redesign. A prior STEP 0 audit (2026-08-09, memory
   `project_step0_n0n8_ui_audit_aug09`) already flagged this exact group's
   "ACP Pipeline" label as an ambiguity risk, since it shared the "ACP" name
   with the real ACP v2 items sitting directly above it in the old
   "Content Team" group.
- Also confirmed: `/admin/pipeline/s1` exists, is middleware-protected
  (`/admin/pipeline` prefix covers it), but has **no sidebar entry at all**
  — an orphaned page, reachable only by typing the URL. Flagged, not added
  — adding a new nav item is out of this round's "reorder only" scope.

**Decision (self-chosen per the round's "ưu tiên tự quyết nếu chỉ là sắp
xếp lại" instruction — pure reorder/relabel, not architecture)**: since a
literal N0→N8 sort doesn't apply to 2 of the 3 pipelines, real ACP v2 items
are grouped and ordered first (N1 setup/approval, then N2 atoms), and the
other two pipelines get honest, unambiguous labels instead of sharing
"Content Team"/"ACP Pipeline" naming with the real flow. Every existing
`{isAdmin && ...}` / unconditional-visibility boundary is preserved exactly
— no item gained or lost access; only which group it's declared in, each
group's order, and each group's label changed:
- `frontend/app/admin/_components/AdminSidebar.tsx`: `CONTENT_NAV` split
  into `ATOMS_NAV` (Atomize, Atom Curation) and `CONTENT_AUTHORING_NAV`
  (the 5 AA-internal items); `PIPELINE_NAV` renamed `ACP_V1_NAV` (items/
  order unchanged). Render order: **"ACP v2 — Setup & Approval"**
  (admin-only, was "Admin": Dashboard, Tenants, Marketplace, Quarter Plan
  Gate B, Run Health) → **"ACP v2 — Atoms"** (all roles, was folded into
  "Content Team": Atomize, Atom Curation) → **"AA Internal Content"** (all
  roles, was folded into "Content Team": Upload, S1 Rewrite, Review Queue,
  Brand Identity, Master Content) → **"Legacy Pipeline (ACP v1)"** (all
  roles, was "ACP Pipeline": S2/S3/S4-blog/S4-social, unchanged order) →
  Settings (admin-only, unchanged position at the bottom, outside any
  group).
- Known tradeoff, flagged not silently made: Quarter Plan Gate B (N5) is
  still admin-only so it renders in the FIRST group, while Atomize/Curation
  (N2) are all-roles and render in the SECOND group — for an admin this
  means N5 visually appears above N2, not in strict numeric order. Merging
  them into one N-ordered group would need per-item (not per-group) role
  checks, a bigger structural change than "reorder/relabel" — intentionally
  not done without Nghiep asking for it.
- **Zero routes, page labels, or items added/removed/renamed** — every
  `href`/`active()` check verified identical before and after (17 nav
  routes, same set).
- Verify (browser): log in as admin — sidebar should read top-to-bottom
  "ACP v2 — Setup & Approval" / "ACP v2 — Atoms" / "AA Internal Content" /
  "Legacy Pipeline (ACP v1)" / Settings, all 17 links still navigate to the
  same pages as before. Log in as content/reviewer role — should see only
  "ACP v2 — Atoms" / "AA Internal Content" / "Legacy Pipeline (ACP v1)"
  (unchanged from before this round's visibility-wise, just relabeled/
  reordered).
- Verified without a live login (admin creds not available this session,
  same limitation as round 4's "Should know"): `next build` (production,
  not dev) succeeded with all 34 routes generated including
  `/admin/pipeline/s1`; `next start` + curl against all 17 nav-linked
  routes returned `307 → /login` (fail-closed as designed, not a
  500/404 — proves no route/import broke), tsc clean, eslint shows only
  1 pre-existing (unrelated, unchanged by this round) `set-state-in-effect`
  warning already present before this file was touched.

## Round 6 overall — Should know
- Live AWS (ECS + RDS) was available and used this session (unlike round 5)
  — Phần B's live-DB investigation and fix verification both ran against
  real `aa-cis-dev-db` via the S3-mediated ECS exec pattern, not mocks.
- This round's changes are **not deployed** — CI/CD only runs on merge to
  `main` (human-only per repo policy). All "verify" steps above that say
  "once merged/deployed" still need a real browser pass after Nghiep
  merges, same as every prior round.
- Nghiep's Phần B decision (full-catalog-for-all-tenants) is explicitly
  TEMPORARY — see the `# TEMP (2026-08-13, ...)` comment in
  `runway.py::fetch_trips()`. Don't let a future STEP 0 read it as settled
  design; revisit when N1/Marketplace licensing (D3/D4) is actually built.

## Round 7 (2026-08-13) — same version, two different Slot Grids (real bug, fixed)

Nghiep's own real-browser verification (right after round 6, branch not yet
merged) found: viewing v6 (aa_internal, Q3 2026, approved, current) via
History tab's `?version_id=` link gave Week 1 with framework "PAS" / mostly
MOFU; viewing it via the plain "Preview Slot Grid (N6)" button on Atom
Curation (no `?version_id=`) gave "AIDA" / mostly BOFU — same version, no
edits in between, two different computed Slot Grids, with nothing on the
UI distinguishing them (both said "v6 ... APPROVED").

### Root cause — confirmed with live code + live data, not guessed

- Enumerated **every** entry point into `/admin/curation/preview`
  (`grep -rn "curation/preview" frontend/app`) — exactly 3, no others exist:
  `curation/page.tsx`'s "Preview Slot Grid (N6)" button (no `version_id`),
  and `quarter-plan/page.tsx`'s two "Slot Grid Preview" links (History row +
  Approved-detail, both pass `?version_id=`).
- Nghiep's leading hypothesis (the Atom Curation button being a pre-AA-323
  leftover that never reads real Gate B state) is **refuted by code**: its
  target route has no `version_id`, so it goes through
  `preview_slotgrid()`'s `else` branch, which — since AA-323 Gap 1, unchanged
  since before round 6 — already checks `fetch_approved_quarter_plan()`
  first and only falls back to the in-memory demo when that returns `None`.
  Round 5's live curl (this exact branch) already found `demo_mode: False`
  with `trip_ids` matching v6 byte-for-byte; round 7 re-confirmed live again
  (`quarter_plan.current_version_id` = v6's `version_id`, `is_current=True`)
  — the demo path was never the issue.
- **Actual root cause**: `preview_slotgrid()`'s two branches computed
  `month` independently and disagreed. The `?version_id=` branch used
  `month = (quarter - 1) * 3 + 1` (Q3 → July, always the quarter's first
  month). The no-`version_id` branch used `month = today.month` (August,
  2026-08-13's real month). Both agree on `year`/`quarter` (both Q3 2026)
  but NOT `month` — and `compute_slot_grid()` (`allocator.py`) computes each
  slot's `funnel_stage` via `runway.stage(dest, market, month)`, then picks
  `framework` from `FRAMEWORK_TABLE[(stage, "blog")]`
  (`MOFU→PAS`, `BOFU→AIDA`, `constants.py`). **Live-proven, not theoretical**:
  ran `runway.stage("South Korea", "US", 7)` and `(..., 8)` against v6's
  real trips/runway data (v6's 8 trips are 100% South Korea, confirmed live)
  inside the deployed ECS container — got `MOFU` for month 7, `BOFU` for
  month 8. Exactly reproduces what Nghiep saw: `?version_id=` (month 7) →
  MOFU/PAS; plain link (month 8) → BOFU/AIDA.
- This is a real regression introduced by round 6, Phần A: adding the
  `?version_id=` branch didn't re-check that its own `month` derivation
  agreed with the pre-existing default branch's — round 6's own tests only
  checked the version_id branch in isolation (a genuinely-historical
  different-quarter case), never a version_id that happens to BE the
  current one.

### Fix

- `api/routers/admin_atoms.py`: new `_preview_month_for_quarter(year,
  quarter)` — single shared function, now the ONLY place `month` is
  derived, used by both branches. Rule: if `(year, quarter)` is the real
  current quarter, use `today.month` (what should ship right now — matches
  what the default/no-`version_id` link has always shown); otherwise
  (a genuinely different quarter) use the quarter's first month, since
  "today" has no meaning for a plan outside the current quarter. This
  guarantees `?version_id=<the current version>` and no `version_id` at all
  always resolve to the identical `(year, quarter, month)` and therefore
  the identical `SlotGrid` — a version_id pointing at a truly historical
  (different-quarter) version keeps the prior "first month" behavior
  unchanged (round 6's own `test_approved_version_id_reads_that_specific_version`
  test, a 2024 Q1 case, still passes untouched).
- Demo path invariant (Nghiep's "must never run when current_version_id
  exists" requirement): confirmed **structurally already guaranteed** by
  `fetch_approved_quarter_plan()`'s own query (`qpv.version_id =
  qp.current_version_id AND approval_status = 'approved'`) — `demo_mode`
  can only be `True` when no such row exists. Not deleted (still the only
  path for a genuinely-unapproved-yet tenant/quarter) — documented inline
  and locked in with `test_demo_mode_never_true_when_current_version_id_exists`.
- 4 new tests (`test_aa300_admin_atoms.py`): `_preview_month_for_quarter`
  direct cases (current quarter vs. different quarter/year), and the core
  regression — `?version_id=<current>` vs. no `version_id` on identical
  mocked data must produce byte-identical `SlotGrid.slots` (excluding
  `reactive_hold`'s intentionally-random `slot_id`). This test genuinely
  exercises the bug: it failed on the pre-fix code (months 7 vs 8 diverge)
  and passes after the fix. 1174 unit tests total (was 1170), flake8 clean.
- Committed as a follow-up commit on round 6's still-open branch/PR (#143)
  — this fixes a bug in round 6's own new code, not a new independent
  change, so it stays in the same PR rather than forking a new one.

### Should know
- Full real-browser re-verification (repeating Nghiep's exact click-through)
  still needs Nghiep after merge — this branch isn't deployed (same
  limitation as every prior round). The fix WAS verified against live real
  data: the underlying `runway.stage()` divergence was reproduced with v6's
  actual trips/runway inside the live ECS container (proving the bug is
  real, not a mock artifact), and the regression test proves the fix
  closes it at the code level.
- Scope check per Nghiep's explicit instruction: investigation did NOT find
  a wider set of broken entry points — all 3 existing entry points are
  accounted for (2 already used `?version_id=` correctly from round 6; only
  the 3rd, pre-existing default link, was affected, and only because the
  OTHER branch's month logic was inconsistent with it — not because the
  default link itself was wrong). No further scope expansion needed.

**Proposals from round 5 (STATUS: 1-3 built in round 6's Phần C, see that
section above — left here only as the original historical record, not open
work anymore):**
1. ~~Fix `_score_reason()`'s tie-break~~ — done, round 6 Phần C.
2. ~~Collapse "FORCED" badge + "Manually added" reason text into one indicator~~ — done, round 6 Phần C (Status column).
3. ~~Relabel the bare numeric score~~ — done, round 6 Phần C (dedicated Score column + tooltip).
4. Given 763 rows is the full non-retired trip pool (not just the 8 shown as selected today's UI
   already filters to what fits capacity_posts_per_week+1 via `max_trips`, so the create page
   itself isn't the 763-row view) — no table-simplification action needed there specifically beyond
   round 6's pagination.

## Changed
(updated incrementally as gaps are built — see per-gap sections below)

### Gap 1
- `services/acp_planning/models.py`: added `TripScore` model + `QuarterPlan.trip_scores`.
- `services/acp_planning/quarter.py`: `compute_quarter_plan`/`plan_quarter` accept optional
  `excludes`; populate `trip_scores` for every non-retired, non-excluded trip with a short
  English reason string.
- `api/routers/admin.py`: `CreateQuarterPlanRequest.excluded_trip_ids`; new
  `POST /admin/quarter-plan/preview` (no persist).
- `frontend/app/admin/quarter-plan/create/page.tsx` (new): N5 preview + checkbox override +
  Create button.
- `frontend/app/admin/quarter-plan/page.tsx`: `DetailView`'s "Selected Trips" card now resolves
  names via `trip_scores` instead of raw UUIDs (falls back to UUID for pre-existing versions
  saved before this change, which have no `trip_scores`).
- `api/routers/admin_atoms.py`: preview-slotgrid reads a real approved Gate B plan first.

### Gap 2
- `api/routers/admin_atoms.py`: response now includes full `runway_cells: RunwayCell[]`.
- `frontend/app/admin/curation/preview/page.tsx`: new destination × month grid section.

### Gap 3
- `api/migrations/101_acp_shared_tenant_config.sql` (new).
- `services/acp_planning/tenant_config.py` (new): `fetch_tenant_planning_config()`.
- `api/routers/admin.py`: `GET`/`PUT /admin/tenants/{id}/config`.
- `frontend/app/admin/tenants/page.tsx`: new "Planning" tab.
- `api/routers/admin_atoms.py`: preview-slotgrid reads config instead of hardcoded constants.

### Gap 4
- `api/routers/admin_atoms.py`: `GET /admin/atoms` gains `atom_ids` filter param.
- `frontend/app/admin/curation/preview/page.tsx`: slot cards clickable → atom detail panel +
  "View in Curation" deep link (`useSearchParams`, not lazy `window.location.search` read).

## Tradeoffs

- No new role gate for "who can create a pending plan" — the system has no Trang/Nghiep
  distinction today (only admin/reviewer/content/tenant), so `/admin/quarter-plan/create` sits
  under the same `roles: ["admin"]` prefix as Gate B review itself. Building a real
  Trang-vs-Nghiep role split was explicitly out of scope (decision #1) — flagged here, not
  invented.
- Gap 1's create-plan UI does not let a user edit markets/channels/capacity inline — those come
  read-only from the tenant's Planning config (Gap 3). Splitting "which trips" from "what
  campaign parameters" keeps each screen single-purpose; changing parameters mid-preview would
  also silently invalidate the score list shown, which seemed more confusing than a link to
  Tenants > Planning.
- `TripScore.reason` is a single dominant-factor label (e.g. "Rich atom pool"), not the full
  breakdown (`runway_fit`/`richness`/`distinctiveness_score` are still returned as separate
  fields in the API response for anyone who wants the raw numbers — the create-plan UI shows the
  numeric `score` plus the label, not a 3-column breakdown, to keep the checklist scannable).
- `excluded_trip_ids` exclusion is enforced by removing a trip from consideration entirely
  before ranking (never entering `eligible`), not by ranking it and then filtering — this means
  an excluded trip's capacity slot is backfilled by the next-highest-scoring eligible trip,
  which is the expected "remove and let the algorithm re-fill" behavior, not "leave a gap."
- Preview-slotgrid's real-vs-demo branch check (`fetch_approved_quarter_plan` first) adds one
  extra DB round-trip on every Preview page load/Recompute click even when no approved plan
  exists (the common case for a fresh tenant). Accepted — this is a low-traffic admin preview
  endpoint, not a hot path, and the alternative (a separate flag/endpoint) would fragment the
  single source of truth for "is there a real plan" logic that `allocate_month_from_db` already
  owns.

## Should know

- **Live AWS/RDS verification was not possible this session** — `aws sts get-caller-identity`
  requires interactive MFA (`arn:aws:iam::005097885195:mfa/aa365_admin_nghiep`), which cannot be
  supplied headlessly, and CLAUDE.md's cost-discipline rule means AWS resources are never
  started without Nghiep's explicit go-ahead. All verification here is: 1153 unit tests
  (including 2 new tests added this session — `test_reads_real_approved_quarter_plan_when_one_exists`
  exercises the exact create→approve→N6-reads-real-version chain at the mock level, since the
  live chain couldn't be driven end-to-end against real RDS), `flake8` clean, `npx tsc --noEmit`
  clean, and a full `next build` (production build, not `next dev` — AA-345 round 6 lesson)
  followed by `next start` + curl smoke test confirming all 4 touched/new routes
  (`/admin/quarter-plan/create`, `/admin/curation/preview`, `/admin/tenants`,
  `/admin/quarter-plan`) resolve through middleware (307 → `/login`, fail-closed as designed,
  not a 500/404) rather than crashing.
- **Real browser click-through against live data still needs Nghiep** — start AWS
  (`cis-start`), log in as admin, then walk each gap's verification steps in the final PR
  report. This is not a formality: STEP 0 round 2 of this same issue found a live discrepancy
  (`/admin/quarter-plan` looked empty because no pending plan existed yet) that pure code
  reading missed — the same class of gap could exist here.
- Migration 101 is additive-only (`CREATE TABLE IF NOT EXISTS`, no `ALTER` on existing tables
  besides the new FK) and has no backfill — every existing tenant behaves identically until
  someone visits Tenants > Planning and saves a config, by design (see migration file header).
- `quarter_plan_version.payload` is `JSONB` (migration 092) — `trip_scores` needed no migration
  of its own; old pending/approved versions saved before this change simply have no
  `trip_scores` key and the frontend/backend both treat that as an empty list, not an error.
