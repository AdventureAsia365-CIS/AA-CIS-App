# AA-562 — Đổi tên "06-08 · Tenant Activity" → "Content Trace"

## Decisions
- Chọn tên **"Content Trace"** — không có comment mới trên Linear issue thay đổi đề xuất mặc định
  của issue tại thời điểm build, nên dùng đúng tên mặc định issue đề xuất.
- Route `/admin/tenant-activity` giữ nguyên (issue yêu cầu rõ, tránh vỡ bookmark/link cũ — cùng
  nguyên tắc AA-557 H.15 đã áp dụng khi di chuyển vị trí menu).
- Ngoài 2 chỗ issue liệt kê rõ (sidebar item trong atom-curation, `h1` của tenant-activity), cũng
  đổi luôn 3 chỗ cross-link khác gọi trang này bằng tên "Tenant Activity" làm anchor text
  (`a4-oversight/page.tsx` ×2, `tenants/page.tsx` ×1) — để không còn chỗ nào trong app hiển thị
  tên cũ, đúng tinh thần "không còn chỗ nào sót tên cũ" issue yêu cầu cho live-verify.
- 2 code comment lịch sử (AA-557 H.15, AdminSidebar.tsx + atom-curation/page.tsx) giữ nguyên
  nguyên văn "Tenant Activity" — đây là ghi chép lịch sử ("was here", "is really...") không phải
  text hiển thị cho người dùng, xóa sẽ mất ngữ cảnh quyết định gốc.

## Changed
- `frontend/app/admin/atom-curation/page.tsx`: dòng mô tả header ("Per-tenant write/review/
  publish activity moved to...") + sidebar nav item "06-08 · Tenant Activity" → "06-08 · Content
  Trace"; thêm 1 dòng comment AA-562 cạnh comment AA-557 H.15 cũ để tránh gây nhầm khi đọc lại.
- `frontend/app/admin/tenant-activity/page.tsx`: `h1` "Tenant Activity — Write / Review / Publish"
  → "Content Trace — Write / Review / Publish".
- `frontend/app/admin/a4-oversight/page.tsx`: 2 anchor text cross-link (Publish Log + Content Log
  sections) "Tenant Activity" → "Content Trace".
- `frontend/app/admin/tenants/page.tsx`: 1 anchor text cross-link "Tenant Activity" → "Content
  Trace".
- Không đụng logic/API/query nào — chỉ text/label.

## Tradeoffs
- Cân nhắc có nên đổi luôn 2 code comment lịch sử hay không — quyết định giữ nguyên vì chúng ghi
  lại quyết định gốc (AA-557 H.15) bằng tên gọi tại thời điểm đó, không phải text người dùng thấy.

## Should know
- Route KHÔNG đổi — chỉ đổi display text. `git grep -n "Tenant Activity"` sau build chỉ còn 2 kết
  quả, cả hai đều là code comment (không phải JSX render ra UI).
- Live-verify bằng Playwright thật: xem "Live Verify" bên dưới sau khi chạy.
