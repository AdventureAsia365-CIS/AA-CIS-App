# AA-475 — Xoá /admin/atomize + /admin/curation + 2551 atom owner_scope='platform'

STEP0: `docs/claude_audit/AA-475-step0-atomize-curation-teardown.md` (read before this file).
Nghiep confirmed: delete the atoms outright, accept that 46 real `acp_v2_slots` production rows
(tenant `aa_internal`) lose traceability through `rollup_atom_weights()` — no snapshot, no special
handling for those 46 rows.

## Decisions

- Deleted `PATCH /admin/atoms/bulk` and `GET /admin/atoms/preview-slotgrid` from `admin_atoms.py`
  along with the 3 curation pages — STEP0 flagged both as "not in the original ask" but orphaned
  the moment `/admin/curation` + `/admin/curation/preview` are deleted (their only real callers).
  Read the build ask's "4 endpoint đã xác nhận an toàn ở STEP0" as: tours-for-atomization +
  admin/atoms/decompose alias (admin_pipeline.py) + atoms/bulk + atoms/preview-slotgrid
  (admin_atoms.py) = 4, with `v1_atoms.py` (whole file) counted separately since STEP0 called it
  out as "toàn bộ file", not a single endpoint.
- Kept `list_atoms`/`atoms_summary`/`patch_atom` in `admin_atoms.py` verbatim — T6
  (`/portal/t6-atoms`) depends on them (STEP0 confirmed, not re-derived here).

## Changed

**Code (Part 1):**
- Deleted `frontend/app/admin/{atomize,curation}/` (3 pages), `AdminSidebar.tsx`'s `ATOMS_NAV`
  entries, `frontend/middleware.ts`'s 2 `PROTECTED_ROUTES` rows.
- Deleted `api/routers/admin_pipeline.py::get_tours_for_atomization()` +
  `admin_decompose_atoms()` (and their now-unused imports: `_V1DecomposeRequest`,
  `THIN_TRIP_ATOM_MIN`).
- Deleted `api/routers/v1_atoms.py` whole file + its `main.py` router registration.
- Deleted `admin_atoms.py::patch_atoms_bulk` + `preview_slotgrid` (and their
  now-unused imports/helpers: `_preview_month_for_quarter`, `_AA_INTERNAL_TENANT_ID`,
  the `services.acp_planning.*` imports, `from datetime import date`) — kept
  `list_atoms`/`atoms_summary`/`patch_atom` untouched (T6 depends on them).
- Deleted `shared/services/atom_decompose_poller.py` (0 callers, dead).
- **New**: `services/acp_shared/atom_extraction.py` — extracted `SYSTEM_PROMPT`/
  `build_user_prompt()`/`source_hash()`/`strip_json_fence()` out of `v1_atoms.py` before
  deleting it, because `services/acp_produce/tenant_pipeline.py::run_t5_atomize` (T5, the
  live tenant-scoped atomize pipeline) imports these 4 pure helpers directly — a real
  dependency STEP0 missed (STEP0 only checked whether T5 calls `v1_atoms.decompose()`, which
  it doesn't, but not whether it imports anything else from that module). Updated
  `tenant_pipeline.py`'s import to the new module (aliased to keep the same local names, so
  no other line in that file needed to change).
- Deleted tests tied 1:1 to removed code: `tests/unit/test_aa299_atom_insert.py` (100%
  `v1_atoms._decompose_inline`), `tests/unit/test_aa345_atomize.py` (100%
  tours-for-atomization + admin/atoms/decompose alias), 4 `tests/e2e/aa3*-curation*.spec.ts`,
  2 `tests/verify_scripts/aa345_round*_timezone_verify.mjs`. Trimmed
  `tests/unit/test_aa300_admin_atoms.py` down to only `list_atoms`/`atoms_summary`/
  `patch_atom` coverage (removed `TestBulkPatchAtoms`, `TestPreviewSlotgrid`,
  `TestNoV1AtomsRegression` — the last one asserted `v1_atoms.decompose` existed, which is
  now the opposite of true).
- `tests/verify_scripts/aa391_e2e_orchestrator.py` (a manual, TEST_MODE-gated script, never
  in CI/pytest — confirmed via `pyproject.toml`/`.github/workflows/ci.yml`, neither references
  `tests/verify_scripts/`) still imports `v1_atoms._decompose_inline` at module level and will
  now fail to import. Left as-is with a note at the top explaining why, rather than rewritten
  to call `run_t5_atomize` instead — that would misrepresent what the script's historical run
  actually verified (N2 platform-scope decompose, not T5).
- 1497 unit tests pass, `next build` clean (no `/admin/atomize`/`/admin/curation` in the route
  output).

**Data (Part 2) — executed 2026-08-27, S3-mediated ECS exec against RDS dev, in one transaction:**
- Final dry run (re-counted at execution time, not reused from STEP0): 2629 `owner_scope='platform'`
  atoms (2551 non-deleted/non-marker + 78 deleted/marker rows) — the same total STEP0 found, no
  drift since then.
- Final `pg_constraint` FK re-check: 0 FKs reference `tour_atoms.atom_id` (still none).
- Final `angle_gate_request`/`quarter_plan_version` cross-reference re-check: 0 new hits (still
  clean) — script was written to abort before any DELETE if either check found anything new.
- `DELETE FROM acp_contract.tour_atoms WHERE owner_scope = 'platform'` inside
  `async with conn.transaction()`: deleted-row count (2629) verified to match the dry-run count
  before falling through to commit (a mismatch would have raised inside the transaction block,
  forcing an automatic rollback — never reached).
- Post-delete: `SELECT COUNT(*) ... WHERE owner_scope='platform'` = 0.
- Logged (informational only, not a gate — Nghiep already accepted this): 45 distinct
  `acp_v2_slots` rows / 180 `atom_id` references now orphaned in `payload->'atom_ids'` JSONB
  (STEP0's estimate was 46/47 — the 1-row difference is noise from STEP0's `total_slots`/
  `distinct_tenants` count being taken at a slightly different moment, not a new/different
  problem; still all `aa_internal`, still all pre-AA-461 history, nothing new).

## Tradeoffs

Kept the extracted `services/acp_shared/atom_extraction.py` symbols non-private (dropped the
`_` prefix v1_atoms.py used) since they're now genuinely a shared module's public API, not an
internal detail of one router leaking out.

## Should know

- STEP0 (`docs/claude_audit/AA-475-step0-atomize-curation-teardown.md`) missed the
  `tenant_pipeline.py → v1_atoms.py` helper-function import (it only checked whether T5 calls
  the `decompose()` endpoint). Anyone re-doing a similar "is this file dead" check should grep
  for `from <module> import` across the WHOLE repo, not just check whether the module's own
  endpoint/entrypoint is called.
- Nghiep explicitly accepted (see AA-475 build-request message) that deleting the 2551
  `owner_scope='platform'` atoms leaves ~45 real `acp_v2_slots` production rows (tenant
  `aa_internal`) with orphaned `payload->'atom_ids'` references — no snapshot, no special
  handling.

## VERIFY (bắt buộc — all run live against RDS dev / real Bedrock, 2026-08-27)

1. **`SELECT COUNT(*) ... WHERE owner_scope='platform'` = 0** — confirmed, see Part 2 execution
   log above.
2. **T5 rewrite→atomize live-verify**: real tenant `wanderlux-travel`
   (`a1b2c3d4-0001-4000-8000-000000000001`), real tour "Route A: Tokyo Bay" — since the
   currently-deployed ECS task still runs pre-AA-475 code (deploy only triggers on push to
   `main`, and this branch isn't merged yet), the *edited* `atom_extraction.py`/
   `tenant_pipeline.py`/`admin_atoms.py` were copied directly onto the running task's
   filesystem (ephemeral, task-local, never touches the image or any other task) via the same
   S3-mediated exec pattern, then exercised in-process — this actually tests the new code, not
   whatever's still live. Called `run_t5_atomize()` directly with new, never-before-seen tour
   content (forces a real Bedrock call + real INSERT, not an idempotent skip): got
   `{'status': 'success', 'atom_count': 3}`, 3 real atom rows written with
   `owner_scope=<tenant_id>`, real extracted text. Confirms the `v1_atoms.py → atom_extraction.py`
   import swap works end-to-end. Test rows deleted immediately after (verify-only content, not
   real tenant data).
3. **T6 (view atoms) live-verify**: same tenant, called the *edited* `admin_atoms.list_atoms()`/
   `atoms_summary()` directly (same ephemeral-file-copy method) — `total=7 count=7`, matching
   `wanderlux-travel`'s pre-existing real atom count (untouched by the platform-atom DELETE,
   as expected — different `owner_scope`). Confirms T6's 3 kept functions still work unchanged.
4. **`rollup_atom_weights()` no-crash check**: `acp_shared.content_metric_snapshot` has 0 rows
   for `aa_internal` in dev — so a live call would return `{}` immediately without ever reaching
   the per-atom `UPDATE` line (no real engagement data has been manually entered yet), which
   wouldn't actually exercise the orphan-atom_id path. Instead ran the exact statement that
   function executes (`UPDATE acp_contract.tour_atoms SET weight=$1 WHERE atom_id=$2 AND
   owner_scope=$3`) directly against a real orphaned atom_id from the just-deleted set: result
   `UPDATE 0`, no exception — asyncpg never raises on a 0-row UPDATE. No bug found; nothing to
   report/fix.
5. **pytest**: 1497 passed (`tests/unit/`), 0 failures, re-run after all code edits.
6. **`next build`**: clean, confirmed no `/admin/atomize`/`/admin/curation` routes in the
   build's route table.
7. **Deploy dev + ECS digest check** — done after merge: PR #236 merged (`d317706`, squash) once
   all 5 required CI checks were green. `Deploy Dev` (run 33045352750) completed clean: ECR
   build/push, Lambda deploy, ECS deploy (task def `aa-cis-dev-api:149`), wait-stable, smoke test
   all passed. Running task's actual `imageDigest` (`sha256:a29f626d...`) confirmed byte-for-byte
   equal to ECR `:latest`'s digest at verify time — not just the same mutable tag string.

## PR

#236 (`feat/aa-475-remove-atomize-curation`), merged `d317706`, squash. Deployed live, task def
`aa-cis-dev-api:149`.
