## AA-79 + AA-84: Output Rules Seed + Benchmark Samples

### Decisions
- **22 rules from compiled_writer_rules.json**: GROUP 1 (18 block rules) derived from hard_forbidden_output + forbidden_patterns + AI filler terms in brand voice. GROUP 2 (4 flag rules) from sanitization_reject_patterns as representative substrings (post-processor is substring-only, not regex).
- **writer note + book now before it's too late**: Present in JSON but NOT included in the 22 to keep to spec count. Can be added in AA-80 seed extension.
- **Sample files**: Copied verbatim from Ms. Thư's pipeline docs (no placeholder). blog1=2389w, blog2=1889w, social=2517w.
- **services/acp_s4_blog/**: New service directory following acp_s3 pattern. Not yet a Lambda — just library code for ValidatorAgent (AA-80).
- **Migration 020**: Gap in migration numbering (021 exists without 020). Created as 020 — idempotent ON CONFLICT DO NOTHING.

### Changed
- GROUP 2 patterns: JSON uses regex (`\b\d{4}-\d{2}-\d{2}...` and `\bfrom \$\d+`). Simplified to substrings `'tour_id'`, `'created_at'`, `'updated_at'`, `'from $'` since post-processor is substring-based.
- `iconic` and `epic` in GROUP 1: not in JSON's explicit lists but consistent with "AI filler" and "SEO sludge" brand voice prohibitions.

### Tradeoffs
- Using substring for DB field leak detection (created_at, tour_id) will have false positives in legitimate content. Acceptable because flag (not block) — reviewer sees it.
- 1889-word blog2_sanmani_trail is below 1500-word test threshold? No — 1889 > 1500 ✅.

### Should know
- validator_agent.py found at docs/AI-gent-for.../src/ — read for AA-80 port.
- acp_output_rules currently has 10 system rules (SYS-001→010). After seed: expect 32.
- sample_loader.py uses @lru_cache — import in unit tests uses same process, cache persists across tests. Tests use load_samples() directly, no mock needed.
