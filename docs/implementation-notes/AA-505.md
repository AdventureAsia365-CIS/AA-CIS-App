# AA-505 — LLM Cost Tracking: persist per-call usage + Tenant→Model→Stage monitoring UI

Built in the SAME session/PR as AA-518 Việc C (per the build prompt's own "gộp 1 đợt" decision,
02/09/2026 S171) — schema, `shared/llm_client/{pricing,role_config,call_log}.py`, and the 16
call-site edits are documented once in `docs/implementation-notes/AA-518.md` (that file's "Việc C
round 3" section) rather than duplicated here. This file covers AA-505's own half: the
`llm_call_log` table's shape/rationale from AA-505's angle, the monitoring UI, and the shared
live-verify pass.

## Decisions

- **`quality_signal` is real and per-stage, never a placeholder** (the 02/09/2026 S171 decision
  this build prompt opened with). Every one of the 16 call sites logs something ACTUALLY computed
  at that call site — a real judge score/pass-fail where a judge exists (`s1_judge`,
  `s1_brand_audit`, `t10_judge`, `n7_judge`), or a real, honest heuristic where none does
  (`t5_atomize`: atoms actually extracted this call; `s1_itinerary_nudge`: did the nudge land
  inside clamp; `t8_angle_gen`: angles generated + avg PAA answers matched; `n7_gap_research`:
  did it produce a real comparison statement; `n7_draft`/`n7_faq`: sections/items parsed vs.
  requested; `n7_adapt`: required channel markers present; `n7_repair`: leaked-reasoning rejected
  or not; `s1_generate`/`s1_atom_writer`: JSON parsed / output length). No stage was found where
  literally nothing measurable exists — every one of the 16 got a real signal, not a fallback
  `{}`.
- **`t9_write`'s own row is a SHALLOW heuristic (output length), not the T10 gate result** —
  deliberate split, not an oversight: `t10_judge`'s own 2 rows (one per gate, `gate_framework`/
  `gate_brand_voice`) carry the REAL, meaningful pass/fail for that same piece. Considered logging
  `t9_write` with the deferred T10 outcome instead (richer signal) but rejected it — it would have
  required changing `write_content()`/`rewrite_with_feedback()`'s return tuple shape (currently
  `(content_text, cost_usd, seo_meta)`, unpacked by name in ~10 existing tests across 2 test
  files), a real regression-risk for a signal that already exists elsewhere in a cleaner, more
  attributable form (the judge's own row, not the writer's row wearing the judge's result).
- **Judge-stage rows (`t10_judge`, `n7_judge`) always log `tenant_id=NULL`**, even for a real
  tenant's T9/T10 run — `gate_framework()`/`gate_brand_voice()`/`gates.py`'s 3 gate functions
  take only `content_text`/rubric text as parameters, by design (both files' own docstrings:
  "gate functions stay channel-agnostic"). Threading tenant identity into 5 functions whose whole
  design point is staying context-free was judged the wrong trade for this task — the SAME
  piece's `t9_write` row (which does carry `tenant_id`/`angle_gate_request_id`) sits right next
  to it in `llm_call_log`, joinable by `created_at` proximity if per-tenant judge-cost ever
  becomes a real requirement.
- **Cost pricing for judge calls uses `pricing.calc_cost()` against whatever `model_used` the
  judge backend reports** — accurate for the real production default (`gpt-4.1`, in
  `COST_TABLE`); an approximation (falls back to Sonnet-tier rates) for a manual `nova_pro`/
  `gpt56` override via `JUDGE_MODEL` env var, which is NOT the shipped default. Acceptable for an
  observability log, explicitly not treated as a billing-grade number.

## Changed

- Migration `137_llm_role_config_and_call_log.sql` — `shared.llm_call_log` (this issue's own
  table) + `shared.llm_role_config` (AA-518's, built together — see that file).
- `shared/llm_client/call_log.py` (new) — `record_call()`/`record_call_with_pool()`/
  `record_call_sync()`.
- 16 call sites across `services/content_generation/*`, `services/acp_produce/*`,
  `services/acp_content_writing/*`, `services/acp_angle_gate/*` — one `record_call*()` call added
  at (or immediately after) each, per AA-518.md's own call-site table.
- `api/routers/admin_llm_ops.py` (new) — `GET /admin/llm-usage/tree` (Tenant→Model→Stage rollup),
  `GET /admin/llm-usage/calls` (flat, filterable — `?content_piece_id=`/`?angle_gate_request_id=`/
  `?tenant_id=`/`?stage=`, reusable by AA-501's own AA/A4 piece view per the build prompt's
  "endpoint đọc lại dùng chung" instruction, though AA-501 itself isn't wired to call it yet —
  that wiring is AA-501's own follow-up, not blocked by this task existing).
- `frontend/app/admin/llm-usage/page.tsx` (new) + `frontend/middleware.ts` (new
  `PROTECTED_ROUTES` entry, admin-only role) + `AdminSidebar.tsx` (new nav item).

## Tradeoffs

- **Tree query computes an "ok_rate" by OR-ing 8 differently-named boolean keys across the 16
  stages' `quality_signal` shapes** (`passed`/`landed_in_clamp`/`meta_landed_in_band`/
  `required_markers_present`/`produced_statement`/`json_parsed`/`output_parsed`/`status='pass'`)
  rather than one uniform "success" field. Considered normalizing every stage's `quality_signal`
  to always include a top-level `"ok": bool` key instead — rejected: it would have hidden the
  actual, stage-specific signal (e.g. "3 items requested, 2 parsed" is more informative than a
  bare `false`) behind a lossy boolean at write time, for a convenience only the READ side (this
  one aggregation query) needs. The OR-list is uglier SQL but keeps every stage's own log row
  fully self-describing.
- **`t5_atomize`'s quality_signal (`atoms_extracted`) and `t9_write`'s (`output_len_chars`) are
  numeric, not boolean** — the tree query surfaces these as separate `avg_atoms_extracted`/
  `avg_output_len_chars` columns rather than trying to force them through the same "ok_rate"
  machinery. The monitoring UI shows whichever of (`ok_rate`, `avg_atoms_extracted`,
  `avg_output_len_chars`, or just `call_count`) is actually non-null for that stage — not every
  leaf renders the same shape, by design (matches the build prompt's own "tự đề xuất cấu trúc hợp
  lý" latitude).

## Should know

- **Cost/quality numbers only exist from the moment this PR deploys forward** — `llm_call_log`
  starts empty; every dollar this codebase has ever spent on LLM calls before this task is real
  but permanently unrecoverable (matches AA-434's own original finding: the old code computed
  cost in memory and discarded it, nothing to backfill from).
- **A1/T2 spend through the 5 `s1_*` stages is NOT tenant-attributable** — see AA-518.md §6's
  "Known, disclosed gap". The monitoring tree will show these under a synthetic `tenant_id=NULL`
  → `"aa_internal"` bucket regardless of whether the underlying call was actually an admin A1 run
  or a tenant T2 rewrite — a real, disclosed limitation of this round, not silently swept under
  the tree UI's own "aa_internal" label.
- Live-verify (real UI model change → real LLM call → real DB row, cache-invalidate check, tree
  matches DB, regression on an untouched stage) is below, once run.

## Live-verify

Real pass, 02/09/2026 23:56-23:57 UTC, against the LIVE dev RDS + real Bedrock/OpenAI, via the
canonical S3-mediated ECS exec pattern — this task's own `shared/llm_client/{pricing,role_config,
call_log,client,models}.py` were copied onto the ALREADY-RUNNING `aa-cis-dev-api` task's
filesystem (overwriting the still-deployed old versions; the already-running uvicorn process keeps
serving the old code from memory, unaffected — this only lets a FRESH `python3` subprocess import
the new code), matching the same "ephemeral file-copy onto running ECS task" pattern used for
AA-475's own live-verify. Full HTTP-level verify (the actual admin UI, both new endpoints wired
through `main.py`, both new FE pages) needs a real deploy — that's the post-merge pass, not run
yet; this pass proves the core mechanism for real before asking Nghiệp to merge a schema-carrying
PR.

Migration 137 applied live first (S3-mediated ECS exec, direct `conn.execute()` of the file) —
confirmed 16 rows in `shared.llm_role_config`, matching the seed table in AA-518.md §1 exactly.

1. **Config read (real DB)**: `get_stage_config("t5_atomize")` →
   `StageConfig(stage='t5_atomize', role='writer', provider='claude', model_id='sonnet',
   account_route='acc3')`; `get_stage_config("n7_judge")` →
   `StageConfig(..., provider='openai', model_id='gpt-4.1', account_route=None)`. Both match the
   seed exactly.
2. **Real Mechanism-A call** (`LLMClient().generate()`, `stage="s1_judge"`) — real OpenAI GPT-4.1
   call, real response `{"ok": true, "marker": "aa518-verify"}`, `cost_usd=0.0002`, persisted via
   `record_call()`.
3. **Real Mechanism-B call** (`invoke_claude()` with the `t5_atomize` stage's resolved
   `sonnet`/`acc3`) — real Bedrock satellite call, `model_used="sonnet-4-6"`, `cost_usd=0.000147`,
   persisted.
4. **Cache invalidate — the build prompt's own hardest requirement, proven end-to-end, not just
   at the config-read level**: `set_stage_config("t5_atomize", "haiku", ...)` (the exact call
   `PATCH /admin/llm-config/t5_atomize` makes) → `get_stage_config()` immediately after shows
   `model_id='haiku'` (no restart) → **a REAL follow-up `invoke_claude()` call, using that
   freshly-read config, was actually served by `haiku-4-5`** (`model_used="haiku-4-5"`, real reply
   `"SWITCHED"`, `cost_usd=0.000012`, ~9x cheaper than the sonnet call above, consistent with the
   real pricing table) — the model change took effect on the very next real LLM call in the same
   process, with no deploy/restart in between. Reverted back to `sonnet` immediately after
   (`updated_by='admin:live-verify-script-revert'`), confirmed by a final `get_stage_config()`
   read — live config left exactly as found.
5. **Regression**: `n7_judge` read again after the `t5_atomize` change — still
   `gpt-4.1`/`openai`, confirming per-stage isolation (no cross-stage cache bleed from the
   invalidate above).
6. **`llm_call_log` query** (`WHERE quality_signal->>'live_verify_marker' = 'aa518-505-pass2'`) —
   7 real rows found (3 full attempts of this script during iteration + the final clean run),
   every one with a real, non-NULL `quality_signal` (`{"parsed_ok": true, ...}` for the judge row,
   `{"atoms_extracted": 0, ...}` for the writer rows, `{"cache_invalidate_check": true, ...}` on
   the post-switch haiku row) — confirms the "never NULL" requirement holds for real writes, not
   just in unit tests.

**Not yet live-verified** (needs the post-merge deploy pass): the actual `PATCH /admin/llm-config/
{stage}` HTTP endpoint and the 2 new FE pages (`/admin/settings`'s "LLM Models" tab,
`/admin/llm-usage`) — steps 1-6 above prove the underlying `role_config.py`/`call_log.py`
mechanism they call is real and correct; the endpoint/UI layer on top is thin (reviewed, not yet
HTTP-exercised against a real deploy). Also not yet exercised live: the T9/T10 (`t9_write`/
`t10_judge`) and N7 (`n7_draft`/`n7_adapt`/`n7_faq`/`n7_repair`/`n7_gap_research`) call sites
specifically — covered by the unit-test suite (1705 passed) and code review, not a real end-to-end
run this pass (would require a real tenant T9 write or a real N7 slot run, out of scope for a
quick pre-merge check) — flagged as the natural next real-traffic confirmation once this deploys
and real usage accumulates.
