# AA-576 — Tenant portal: leftover Vietnamese, /portal/t8-angle-gate dead-code check, sidebar rename proposal

## Decisions

- Scoped "UI text" (Phần 1) to visible text only — labels, tooltips, placeholders, error
  messages, confirm dialogs. Left engineering-log comments untouched (many contain Vietnamese
  in dated AA-XXX changelog entries, e.g. `SlateTab.tsx`'s own file header) — those are
  historical dev notes, not something a tenant ever sees, and out of this issue's stated scope.
  One exception: a present-tense comment on `SlateTab.tsx`'s `pick()` error handler directly
  quoted the OLD Vietnamese button label ("Chọn viết") to describe CURRENT behavior — updated
  that one quote to the new English label ("Pick to write") since it now described a
  non-existent string, not because it's UI text.
- Diacritic-only sweep (`grep -rnP '[àáâãèéêìíòóôõùúýăđĩũơưẠ-ỹ]'`) for STEP0 detection. Reliable
  here because this codebase's Vietnamese always carries diacritics — no evidence of a
  diacritic-free Vietnamese string anywhere in a repo-wide check of the tenant portal tree.

## Changed

`frontend/app/(tenant)/portal/_components/SlateTab.tsx` — 8 real UI strings translated
(file:line, old → new):
- L173 tab tooltip: `"Nhịp tuần"` / `"Theo yêu cầu (on-demand)"` → `"Weekly rhythm"` / `"On-demand"`
- L260 eligible-count line: `"...đủ điều kiện"` → `"...eligible subject(s)"` (reworded, not just
  swapped word-for-word, to read naturally in English)
- L262 weekly-rhythm line: `"· nhịp đề xuất N bài/tuần"` → `"· target rhythm N posts/week"`
- L264 on-demand line: `"· viết theo yêu cầu"` → `"· written on demand"`
- L276-277 empty-state (decided-history, no new eligible): title `"Không còn Subject mới đủ điều
  kiện lúc này"` → `"No new eligible Subjects right now"`; sub `"Xem lại các Subject đã quyết
  định bên dưới, hoặc rewrite/atomize thêm tour để có đề xuất mới."` → `"Review the Subjects
  already decided below, or rewrite/atomize more tours for new proposals."`
- L309 `barReasonText()` on-demand branch: `"Theo yêu cầu — không giới hạn ngưỡng"` → `"On demand
  — no threshold applies"`
- L318 `barReasonText()` said-threshold branch: `"...ký tự ≥..."` → `"...chars ≥..."`
- L320 `barReasonText()` fallback: `"Không có ngưỡng nào áp dụng"` → `"No threshold applies"`
- L374 Cut confirm dialog: `"Bỏ qua đề xuất này? Không thể hoàn tác."` → `"Cut this proposal?
  This cannot be undone."`
- L459/L464 button labels: `"Đang chọn…"` / `"Chọn viết"` → `"Picking…"` / `"Pick to write"`;
  `"Đang bỏ…"` → `"Cutting…"`
- L363-364 (comment only, see Decisions above): stale quoted UI string updated for accuracy.

No other file under `frontend/app/(tenant)/portal/` had Vietnamese outside comments — confirmed
via full diacritic grep across the tree + a live Playwright sweep of every one of the 13 real
`/portal/*` routes (see Live Verify below), not just a static-code claim.

Phần 2 and Phần 3: no code changes (see Should know / Tradeoffs).

## Tradeoffs

- Phần 2 (`/portal/t8-angle-gate`) — STEP0 found it is **NOT dead code**: `Sidebar.tsx` NAV1
  still renders a real `<Link href="/portal/t8-angle-gate">` labeled "Write Content"
  (`_components/Sidebar.tsx:32`), and `AngleGateTab.tsx`'s own header comment explicitly says
  "Route stays /portal/t8-angle-gate — still a real, valid deep-link (e.g. a bookmarked resume
  link), not removed" (a decision already made in AA-564 Nhóm 4.2, not something this task
  reopens). Per the issue's own instruction ("nếu vẫn còn nơi nào dùng → báo lại, KHÔNG tự
  xóa"), nothing was deleted. Live-verified the sidebar link still resolves (test 07).
- Phần 3 — per the issue's explicit requirement, stopped after listing current names + proposed
  alternatives; posted to the Linear comment, no renaming code written this session.

## Should know

- The Cut-confirm-dialog live-verify (test 05) clicks the real "Cut" button on a real proposed
  LinkedIn Subject for WanderLux Travel and captures `window.confirm()`'s message, then
  **dismisses** it (never confirms) — `cut()` returns early on a dismissed confirm, so no real
  mutation happens. Re-checked `GET /v1/slate` after the run: LinkedIn `eligible_count` still 24,
  unchanged.
- A real, pre-existing (not-this-task) finding surfaced while confirming Phần 2: clicking "Write
  Content" directly (no `?resume_request_id=`) starts `AngleGateWizard` from its goal-selection
  step with no Subject/Segment/Route preselected — a materially different, now largely redundant
  entry point since AA-564 already lets a tenant reach the same wizard, pre-populated, straight
  from a Slate row. Not fixed here (out of Phần 2's STEP0 scope, which only asked "still used?
  don't guess, don't delete") — surfaced in the Phần 3 comment as input for the sidebar
  naming/IA discussion.

## Live Verify

`tests/e2e/aa576-live-verify.spec.ts`, run against `next dev -p 3001` (local) fronting the real
deployed dev backend (`https://api-cis.lumiguides.it.com`), real WanderLux Travel tenant session
via the real generate-key + `/tenant-login` form flow (no minted JWT). 20/20 passed:
- All 13 real `/portal/*` routes: zero Vietnamese-diacritic characters in rendered body text.
- Slate tab tooltips ("Weekly rhythm" / "On-demand"), Blog tab's real decided-only empty state,
  LinkedIn tab's real proposed-subject bar-reason text ("...chars ≥..."), Email tab's real
  on-demand copy ("written on demand" / "On demand — no threshold applies"), the real Cut
  confirm-dialog text, and the "Pick to write" button label — all read in English against real,
  already-existing WanderLux Slate data (no synthetic DB setup needed).
- Phần 2: Sidebar's real "Write Content" link still resolves `/portal/t8-angle-gate` (not a 404),
  confirming nothing was deleted.
