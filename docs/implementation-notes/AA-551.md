# AA-551 — Split `/admin/atom-curation` (01-05 platform-wide) + new `/admin/tenant-activity` (06-08)

Source of truth: `docs/investigation/AA-550-admin-ui-real-audit.md` (AA-550, screenshots on that
Linear issue). Wireframe posted to AA-551 Linear comment before any code, per issue requirement.

## Decisions

- **Two pages, not one page with two zones** (Nghiệp's explicit choice, "Phương án B" over the
  audit's "Phương án A"). `/admin/atom-curation` keeps its URL/name; `/admin/tenant-activity` is
  new.
- **Slate (05) stays exactly as-is** — still requires a Tour, still per-tenant (`acp_shared.
  subject.tenant_id NOT NULL` is real schema, not a display bug) — explicitly out of the
  platform-wide scope per the issue's A.3. Only its `PickTourPrompt` copy moves to English.
- **Header stats via one new endpoint** (`GET /admin/dashboard/summary`) rather than deriving from
  each section's own `total` — the header must show all 6 numbers regardless of which section tab
  is open, and a client-side composition of 4 section fetches just to render a header would
  duplicate exactly the round-trip-multiplication problem the 401 investigation (see below) found
  in the first place.
- **"Hub" count is `COUNT(DISTINCT route.hub_name)`, not a real `acp_contract.hub` row count** —
  that table has 0 rows in production (already documented in `admin_dashboard.py`'s
  `hub_grouping_backlog` field, AA-527). Counting the empty real table would show a permanently
  misleading 0; the text-field proxy is what section 04 already renders. Documented inline in the
  endpoint's own docstring, not silently substituted.
- **Route/Hub version history differs by mode**: a single Tour selected keeps today's behavior
  (shows the FULL version history, current + superseded — AA-532's own deliberate audit-view
  choice, "not a bug"); "All tours" mode defaults to current-only (`superseded_at IS NULL`) to
  keep a platform-wide table from listing every historical version of every tour's routes at
  once. Not a toggle in this build — flagged as a possible follow-up, not built speculatively.
- **`BacklogNote` deleted, not translated** — A.5 of the issue explicitly asks to check whether
  this build resolves what the note flags (no admin UI for Hub grouping reasoning) before
  deciding to translate vs. remove. It doesn't (Hub grouping *reasoning* — "why these Routes
  share a Hub" — is still nowhere; only `hub_name` text and a derived count are shown) — but the
  Vietnamese paragraph itself named a stale line-number reference (`AA-525 Phần 12 mục 8`) with no
  new information a reader needs beyond "this is still a gap." Replaced with one short English
  line instead of a translated wall of text, per A.5's own "or xóa nếu nội dung đã lỗi thời" option.
- **401 fix bundled into this same PR** (not a separate one) — see Part B below; the issue allowed
  either, and the bug is directly triggered by this page's own concurrency shape.

## Changed

- `api/routers/admin_dashboard.py`: `tour_id` made optional on `segments`/`score`/`routes`
  (still required-by-convention on `slate`, unchanged); added `market` + section-specific filter
  params; added pagination (`limit`/`offset` + total count); added `tour_id`/`tour_name` to every
  row's output (meaningful now that a row can come from any tour); new `GET /admin/dashboard/
  summary` endpoint.
- `frontend/app/admin/atom-curation/page.tsx`: rebuilt — 01-05 only, common Tour+Market filter,
  per-section extra filters, header stats bar, sticky header+nav fix.
- `frontend/app/admin/tenant-activity/page.tsx`: new — 06-08, ported verbatim (content/behavior
  unchanged) from the old page's Write-Gate/Review/Publish sections, Tour-picker retained, sticky
  fix applied here too (same bug, same root cause, both pages share the layout shell).
- `frontend/app/admin/_components/auditPanels.tsx`: new shared module — `fetchJson`, `EmptyState`,
  `ErrorState`, `AuditTable`, `useTourScopedFetch`, `PickTourPrompt` (English), `STATUS_COLOR`,
  `ContentLogRow`/`useContentLog`, `WriteGateSection`/`ReviewSection`/`PublishSection` — extracted
  so the 06-08 sections aren't duplicated between the two pages' source files.
- `frontend/app/admin/_components/AdminSidebar.tsx`: new nav entry, "Tenant Activity".
- `frontend/middleware.ts`: new `PROTECTED_ROUTES` entry, `/admin/tenant-activity`, admin-only
  (same tier as `/admin/atom-curation`).
- `frontend/lib/auth-server.ts`: `requireAdmin()` gains a short-TTL in-memory cache keyed by token
  (Part B fix — see below).
- `CONTEXT.md`: the "Known tech debt" banner at the top was written 06/09 (AA-542, commit
  `43c18c0`) BEFORE AA-545's actual platform-wide migration landed (commit `15743a9`, same day,
  later) — it currently reads as if Segment/Score/Route/Hub are still per-tenant and carries a
  build-freeze warning ("Do not build any new Admin or Tenant UI/feature that assumes Segment/
  Score/Route/Hub is per-tenant... until a separate design/build issue redesigns them
  platform-wide"). That redesign already shipped (AA-545) — confirmed directly from
  `admin_dashboard.py`'s own already-updated SQL/docstrings (no `tenant_id` anywhere in Segment/
  Score/Route queries). Updated the banner to say platform-wide is DONE, not pending, so the next
  reader doesn't re-block on a freeze that no longer applies. Full glossary body (Segment/Score/
  Route/Hub definitions) left alone — out of this issue's scope to rewrite.

## Tradeoffs

- Considered giving `/admin/dashboard/summary` its own market-aware `atom_count`; atoms have no
  `market` column at all (market is a property of `atom_ranking` rows, computed downstream of
  Atom), so `atom_count` only ever responds to the Tour filter, never Market — documented in the
  endpoint rather than faked.
- Considered a `show_superseded` toggle on the platform-wide Route/Hub table instead of a fixed
  current-only default; deferred — no UI ask for it in the issue, and adding a toggle "in case
  it's useful" is exactly the kind of speculative surface this repo's own conventions avoid.
- Considered fixing the 401 by raising the timeout instead of adding a cache; rejected — a longer
  timeout lowers the odds without removing the actual redundant-round-trip cause, and this page's
  own concurrency (~8 simultaneous `/api/admin/*` calls per Tour selection) is unusual enough that
  "less likely" isn't the same as "fixed."

## Live Verify

Real Playwright run (production, `e2e-test-admin`, 07/09/2026) against the deployed PR #329
(task def `:233`) caught one real bug this task's own build introduced: `SlateSection` used the
new generic `usePlatformFetch` hook with no guard, so deselecting the Tour filter fired
`GET /api/admin/dashboard/slate` with no `tour_id` — Slate's backend still hard-requires it
(unchanged, per A.3), so this 422'd instead of showing the "Select a Tour" prompt. Fixed by
giving `usePlatformFetch` an `enabled` param (mirrors the old `useTourScopedFetch`'s skip-when-
no-tourId guard) and passing `!!tourId` from `SlateSection` only — Segment/Score/Route pass no
guard (they're supposed to fetch with no Tour selected, that's the whole point). Screenshot
`05-slate-no-tour.png` is the pre-fix repro (empty-prompt copy rendered fine, but the network tab
shows the 422 underneath — confirmed via the script's own response listener); re-verified clean
after the fix (see PR #330).

Confirmed working via real screenshots (all attached to the AA-551 Linear comment):
- `/admin/atom-curation`, "All tours" + "All markets": header stat bar shows real live counts
  (Tours 7, Atoms 377, Segments 23, Score rows 138, Routes 1, Hubs 1); Segment/Score/Route
  sections render real platform-wide rows with a Tour column, NOT `PickTourPrompt` — the actual
  gap this whole issue exists to close.
- Market filter (US) narrows the header stats and section 02-04 tables together, correctly, atom
  count staying unfiltered by market (documented behavior, atoms have no market column).
- One Tour selected: Slate shows real per-tenant data, still correctly keeping its Tenant column
  (not a regression — per A.3).
- Sticky fix confirmed by real scroll test (1200px scroll inside the Atomize list): header + Tour/
  Market filter + stat bar + section-nav column all stay pinned; only the (deliberately
  non-sticky) Tours-picker column and atom-card list scroll away.
- `/admin/tenant-activity`: renders as a separate page, Tour-picker required, cross-links to/from
  Atom Curation both present and correct, Write/Gate + Publish empty-states render in English.
- i18n: zero Vietnamese strings found on either page across every screenshot captured — both
  named strings (PickTourPrompt, BacklogNote) confirmed gone.
- No `401` observed on Publish across this run's ~15 page loads/section switches (consistent with
  the original anomaly being a rare timing race, not a deterministic bug) — the verify-cache fix
  is otherwise unverifiable live without deliberately engineering a load spike, which this task
  did not attempt.

## Should know

- The 401 root-cause finding is code-grounded (every candidate 401 source in the actual call chain
  was read and ruled in/out — see the AA-551 Linear comment for the full trace) but NOT
  reproduced under controlled conditions in this task; it remains a plausible, well-evidenced
  mechanism, not a certainty. If the cache fix ships and the bug is later seen again, that's the
  signal this diagnosis was incomplete, not proof the fix is a no-op (the cache would still cut
  real backend load either way).
- Live-verify screenshots for this task: see "Live Verify" section below once added.
