# AA-404 — F3/F8/F9 writer-prompt fix

## STEP 0 recon (before any code) — 15/08/2026

**Scope of this pass: read-only. No code changed.** Goal: confirm the exact structure/root cause of
F3/F8/F9 before touching `adapt.py`/`generation.py`. Read `services/acp_produce/gates.py` (759
lines, full), `adapt.py` (185, full), `generation.py` (232, full), `repair.py` (149, full),
`judge_client.py` (85, full), `pipeline.py` (first 260 lines), `services/acp_planning/constants.py`
(FRAMEWORK_TABLE), `research.py` (`_VARIANCE_DIRECTIVES`/`compile_brief`), `brand_standards.py`
(`AA_BRAND_IDENTITY_PROMPT`), plus `docs/implementation-notes/AA-405.md` (today's earlier session
already did a real-data gate audit against the week=2 run — reused and extended that, not
duplicated). Then pulled **all 33 real `acp_deliver.pieces` rows** (3 real N7 runs, all today,
15/08/2026 — `de8337ba...` week=1, `e64befb4...` week=2, `170a0825...` week=3, tenant
`00000000-0000-0000-0000-000000000001` = aa_internal) live from RDS via the S3-mediated ECS-exec
pattern, to check every claim below against real `held_reason`/`repair_log`/`gate_ledger`/
`brand_seo_audit` data, not code-reading alone.

**Corpus-wide result: 0/33 passed, 33/33 held.** First-failing-gate distribution across all 33 (no
piece's first failure was output_rules/F2/F4/F6 — every held piece's first failure was F1, F3, F8,
or F9):

| gate | week=1 (12 pieces) | week=2 (9 pieces) | week=3 (12 pieces) | total |
|---|---|---|---|---|
| F1_grounding | 2 | 1 | 0 | 3 |
| F3_structural_variance | 2 | 2 | 3 | 7 |
| F8_framework | 4 | 1 | 3 | 8 |
| F9_brand_seo_audit_social | 4 | 5 | 6 | **15** |

F1 already fixed this session (commit `db79847`, PR #152/#150 — `_SENT_SPLIT_RE` markdown-bold fix)
— week=3 shows 0 F1 failures, confirming the fix live. Out of scope for AA-404, not re-litigated
here.

---

### 1. F8_framework — full sub-criteria (code: `gates.py` lines 309-432)

F8 is **framework-specific**: `pipeline.py::_resolve_framework()` picks the rubric key from
`FRAMEWORK_TABLE` (`services/acp_planning/constants.py`) by channel, NOT from the caller's raw
`framework` — blog uses `Brief.framework` (assigned per funnel stage), facebook/tiktok always use
one fixed framework each:

```
("TOFU", "blog"):     "hub"              ("ANY", "facebook"): "hook_story_cta"
("MOFU", "blog"):     "PAS"              ("ANY", "tiktok"):   "hook_beats_payoff"
("BOFU", "blog"):     "AIDA"             ("ANY", "email"):    "reader_as_hero" (no email channel wired in N7 yet)
```

Full `FRAMEWORK_RUBRICS` table (`gates.py:315-326`), every item is an LLM (Nova Pro, acc2,
temperature=0) 1/0 judge criterion **unless noted deterministic**:

| framework | channel | criteria | mechanism |
|---|---|---|---|
| hub | blog/TOFU | "covers the topic comprehensively via subsections" | LLM |
| | | "each section answers a distinct sub-question" | LLM |
| PAS | blog/MOFU | "opens with the reader's problem" | LLM |
| | | "agitates concretely" | LLM |
| | | "resolves with the trip as solve" | LLM |
| AIDA | blog/BOFU | "attention hook first" | LLM |
| | | "interest via specifics" | LLM |
| | | "desire built on concrete moments" | LLM |
| | | "single clear action (CTA)" | LLM |
| hook_story_cta | facebook | "first line is the hook" | LLM |
| | | "one atom, one emotion" | LLM |
| | | **"ends with CTA"** | **DET** (`_ends_with_cta()`, `gates.py:352-363` — checks the CTA phrase `"design this journey"` (case-insensitive) is in the final `\n\n`-paragraph. AA-396 follow-up: pulled off the LLM judge because real data showed the judge scoring it backwards.) |
| hook_beats_payoff | tiktok | "hook stated" | LLM |
| | | "timed beats present" | LLM |
| | | "payoff lands" | LLM |
| reader_as_hero | (email, unused) | "reader is the subject, not the brand" | LLM |
| | | "single CTA" | LLM |
| (fallback) | any unassigned framework | "structure matches the stated framework" | LLM |

**Confirming/correcting AA-404's known 3 sub-criteria against real data:**

- **(a) "ends with CTA" — confirmed already fixed, and the fix verified live.** Week=1 (before
  today's `adapt.py::_FACEBOOK_INSTRUCTIONS` prompt fix, AA-405): **4/4 facebook pieces failed
  "ends with CTA", 100% reproduction.** Week=2 and week=3 (after the fix): **zero "ends with CTA"
  failures** across 9 facebook pieces produced since. Root cause was (a) prompt-missing (never
  fixed) → now genuinely fixed at the source.
- **(b) "one atom, one emotion" (hook_story_cta/facebook) — still actively failing, all 3 weeks.**
  Real violations: week=1 3 pieces ("one emotion" / "one emotion"+CTA), week=2 1 piece, week=3 2
  pieces. Never converges within the repair budget. hook_story_cta's OTHER LLM criterion, "first
  line is the hook", **never once appears as a violation in any of the 33 real pieces** — it passes
  every time.
- **(c) "attention hook" — this is NOT hook_story_cta's "first line is the hook" (that criterion has
  a clean pass record). It is AIDA's "attention hook first" (blog/BOFU), which appeared once in real
  data:** week=3, piece `170a0825...:slot_1b7d50d4d729cdf91049:blog` (channel=**blog**, not
  facebook), held on `"attention hook first"` + `"single clear action (CTA)"` after **7 repair
  rounds** (repair budget scaled up per `compute_repair_budget()` for a piece that started with
  several gates failing at once — `REPAIR_BUDGET_CAP=8`, `models.py:41`). Only 1 real occurrence so
  far (BOFU/AIDA is presumably a smaller slice of the real slot mix), but see root cause below — the
  code-level gap that would produce this is unambiguous, not a fluke.

**Root cause, F8 (evidence, not guesswork):**
- CTA sub-check: was (a) prompt-missing, **now fixed** (verified: 0/9 post-fix vs 4/4 pre-fix).
- "one atom, one emotion" (hook_story_cta): **(a) prompt-missing.** `adapt.py::adapt_channels()`
  (line 121-122) hands the facebook writer **every atom the blog piece cited anywhere**
  (`atom_ids_cited(blog_piece)` → `cited_atom_text`), and `_FACEBOOK_INSTRUCTIONS` (`adapt.py:72-84`)
  only requires "**at least one** `[R:atom_id]` citation" — a floor, not a ceiling. Nothing tells the
  model to pick ONE atom/moment and build the whole caption around it. AA-405's manual read of a
  held piece confirmed this directly: the failing caption cited 4 distinct atoms (yacht ride, cooking
  class, Taekwondo, market, temple) for what's supposed to be a single-atom hook-story-CTA post.
- "attention hook first" + "single clear action (CTA)" (AIDA/blog): **(a) prompt-missing, and total
  — not just weak.** `generation.py::_DRAFT_SYSTEM_PROMPT` (67-81) and `_build_batch_prompt()`
  (177-194) pass `FRAMEWORK: {brief.framework}` as a **bare label** — "AIDA", "PAS", "hub", etc. —
  with **zero explanation anywhere in the writer prompt of what any framework structurally
  requires** (no mention of Attention/Interest/Desire/Action beats, no "open with a hook" guidance).
  The model is told which framework name to use but never what it means. This is a total prompt gap
  for every blog framework, not specific to AIDA — AIDA/BOFU just happens to be the one that
  produced a real held piece so far.

---

### 2. F3_structural_variance — exact logic (code: `gates.py` lines 159-196)

Blog-only (`channel != "blog"` → auto-pass — facebook/tiktok never run this gate at all). Three
**independent** checks, ANY failing fails the gate:

```python
paras = [p for p in body.split("\n\n") if p.strip() and not p.startswith("#")]
# (1) at least one genuinely-one-sentence paragraph must exist:
any(len(re.split(r"(?<=[.!?])\s+", p.strip())) == 1 and len(p.split()) >= 2 for p in paras)

# (2) with >=3 "## " H2 sections, longest must be >=1.4x the second-longest (by word count):
sections = re.split(r"^## ", body, flags=re.MULTILINE)[1:]
if len(sections) >= 3:
    lens = sorted(len(s.split()) for s in sections)
    fail if lens[-1] < lens[-2] * 1.4

# (3) at most 1 "\n\n"-delimited block starting with "- "/"* " in the whole body
blocks = len([b for b in body.split("\n\n") if re.match(r"^\s*[-*] ", b)])
fail if blocks > 1
```

No numeric threshold beyond the `1.4x` ratio for check (2); checks (1)/(3) are pure presence/count.

**Real-data breakdown, 7 held pieces across 3 weeks:**
- **6/7 fail check (1)** "no one-sentence paragraph found" — the dominant failure mode, every week.
- **3/7 also/only fail check (2)** "no section is notably longer than the others".
- **0/7 ever fail check (3)** — the bulleted-list cap has never actually fired in real production
  data; not a live problem.

**Root cause — genuine writer-prompt gap, but a subtler one than "no instruction exists" (evidence
below rules out the naive read):**
- `research.py::_VARIANCE_DIRECTIVES` (lines 50-55) **does** literally spell out all 3 rules:
  `"include ≥1 one-sentence paragraph"`, `"make one section notably longer than the others"`,
  `"≤1 bulleted list in the whole article"` — these ARE threaded into every draft call via
  `Brief.variance_directives` → `generation.py::_build_batch_prompt()`'s
  `"VARIANCE DIRECTIVES (apply across the whole piece): ..."` line (`generation.py:182`). So Q1's
  known-instruction claim is correct — the instruction exists.
- The gap is structural, not textual: `generate_draft()` drafts sections in **batches of 2-3, each a
  separate, independent Sonnet call** (`_batch_sections()`, target=3) that **never see each other's
  output**. A whole-piece constraint ("exactly ONE section should end up notably longer than the
  rest", "at least one short paragraph should exist SOMEWHERE across the whole piece") is repeated
  **identically and unconditionally to every batch**, with no signal about what earlier/other batches
  already produced — so each batch call has no way to know whether the piece-level quota is already
  met, or whose job it is to be "the long section". This explains check (1)'s 6/7 failure rate (a
  directive with no accountability owner across ~2-3 independent calls is easy for every call to
  quietly skip) directly.
- Check (2) is worse than under-instructed — it's **actively unactionable by the writer** as coded:
  `words_per_section = max(words_mid // len(outline), 100)` (`generation.py:136`) computes ONE
  uniform per-section word target, used identically for **every** batch's `max_tokens` sizing
  (`generation.py:142`). No section is ever told "you are the long one" — the length-variance
  directive and the uniform per-section token budget directly contradict each other in the code, not
  just in prompt wording.
- Check (3) never firing in real data is consistent with a single directive line being sufficient
  when the constraint doesn't depend on piece-wide coordination (avoiding a SECOND bulleted list is a
  per-batch-local decision each call can independently satisfy — no cross-batch coordination
  needed, unlike (1)/(2)).
- **Conclusion: (b) — prompt has the instruction, but it's structurally undeliverable for a
  piece-wide constraint under a batched, uncoordinated multi-call writer**, not (a) missing or (c) a
  gate/threshold bug. The 1.4x ratio and one-sentence-paragraph definitions themselves are not shown
  to be wrong by anything in the real data — every held piece's own paragraphs (AA-405's manual read)
  are genuinely uniform 3-7 sentences, not near-miss counts a looser threshold would have passed.

---

### 3. F9_brand_seo_audit_social — full rubric (code: `gates.py` lines 511-585) + real repair-log trace

Per-channel rubric fields (JSON contract, binary 1/0), **not** a parameterization of blog's F9:

- facebook (3 fields): `brand_fit`, `cta_clear`, `human_read`
- tiktok (2 fields): `hook_strength`, `cta_clear` — **no `brand_fit` field in tiktok's contract at
  all**

Shared fixed failure-code vocabulary for BOTH channels regardless of which fields are scored:
`SUMMARY_OFF_BRAND`, `CTA_MISSING_OR_WEAK`, `HOOK_WEAK`, `GENERIC_AI_WORDING`,
`FACT_CHECK_MANUAL_CHECK`. The judge (Nova Pro, temperature=0) always receives the FULL
`brand_rubric_text` (tenant_brand_rules, same voice-attribute/required-forbidden-lexicon content as
blog's F9) regardless of channel or which numeric fields were requested — so it applies full brand
scrutiny to tiktok's script even though tiktok never asks it to score `brand_fit` explicitly.

**F9-social is the single largest failure mode: 15/33 pieces (45%), never zero in any of the 3 real
weeks (4→5→6).** Channel split is lopsided: **11/15 (73%) are tiktok**, only 4/15 facebook — tiktok
fails this gate roughly 3x as often as facebook.

**Repair convergence — pulled all 15 pieces' real `repair_log` (3 rounds each). Confirms AA-405's
"never converges" claim with concrete quotes, now across all 3 weeks, not just week=2:**

Every single held piece shows a **different specific phrase** flagged as `GENERIC_AI_WORDING`/
`SUMMARY_OFF_BRAND` on each of its 3 repair rounds — never the same complaint twice. Concrete example
(`de8337ba...:slot_845eb6ec83cdf1f082ec:blog#tiktok`, week=1):
- round 1: flagged `SUMMARY_OFF_BRAND, CTA_MISSING_OR_WEAK` — "lacks a clear call to action."
- round 2 (after repair added a CTA): flagged `SUMMARY_OFF_BRAND, GENERIC_AI_WORDING` — quotes a new
  sentence as generic, AND says the phrase **"Design This Journey" itself "feels somewhat generic
  and not sufficiently unique"** — i.e. the judge penalized the literal brand-mandated CTA phrase
  (`brand_standards.py:12` requires exactly this phrase) as off-brand.
- round 3 (after a further rewrite): flagged `SUMMARY_OFF_BRAND, GENERIC_AI_WORDING` again, quoting
  yet a third, different sentence ("still forming its identity", "long ago settled into its own").

This pattern repeats identically across all 15 pieces — three different targets, three different
"generic" quotes, in every case. Several of the quoted "generic AI wording" examples read as
reasonably specific, crafted prose (e.g. `"the terrain does its own work"`, `"earns its arrival"`,
`"that contrast is precisely what this journey is designed to deliver"`) — not the templated
AI-tell phrases F2's fixed lexicon already catches (`nestled`, `hidden gem`, `breathtaking`, etc.).

**Root cause — genuinely mixed, both prompt AND rubric-side, evidence for both:**
- **(b) prompt-side, confirmed:** `adapt.py`'s `_FACEBOOK_INSTRUCTIONS`/`_TIKTOK_INSTRUCTIONS`
  inherit the shared `AA_BRAND_IDENTITY_PROMPT` block (so the REQUIRED/FORBIDDEN lexicon text IS
  technically present in every adapt call — `adapt.py:64`), but neither instruction explicitly
  requires working a REQUIRED-lexicon word (Design/Curated/Refined/Tailored/Journey) into the tight
  50-120 (facebook) / 100-150-"spoken/conversational" (tiktok) word budget, nor explains what
  "generic AI wording" means in a way the writer could self-avoid. The tiktok instruction's own
  explicit "spoken, conversational" register (`adapt.py:90`) is in real tension with the brand
  rubric's "calm, assured, curated, composed, premium, controlled" bar, and nothing in the prompt
  tells the writer how to reconcile the two — this maps directly to why tiktok fails ~3x more often
  than facebook in the real data.
- **(c) gate/rubric-side, real concern, not just a prompt gap:** `GENERIC_AI_WORDING`/
  `SUMMARY_OFF_BRAND` have no deterministic anchor (unlike F2's fixed regex lexicon) — they are Nova
  Pro's own subjective "does this sound premium enough" judgment, re-evaluated fresh each round
  against newly-repaired text with no memory of what it flagged last time. A rubric whose target
  moves round to round for what's really one underlying "vibe" complaint is close to unsatisfiable by
  any single prompt fix — `AA-372.md`'s own STEP 0 doc already flagged this rubric was built without
  real FB/TikTok data to calibrate against ("don't guess ahead of data" — line ~90-98); this session's
  3 real weeks are the first real calibration data, and they show the vocabulary/threshold itself,
  not just the writer prompt, is likely under-specified.
- Net: **predominantly (b)**, with a **real, evidenced (c)** layered on top — a pure writer-prompt
  fix (require 1 lexicon word, reconcile "conversational" vs "premium") should reduce this
  substantially given the concrete gaps found, but is unlikely to fully converge repair rounds to
  zero without also tightening what `GENERIC_AI_WORDING` is allowed to mean (e.g., requiring the
  judge to quote why a SPECIFIC prior-round complaint was or wasn't addressed, or narrowing
  `GENERIC_AI_WORDING` to a smaller, checkable definition).

---

### 4. `adapt.py` writer prompt (E3, facebook/tiktok) — presence/absence check, direct quotes

Full prompt read (`adapt.py:62-98`). Per AA-404's 3 questions:

- **(a) one-sentence-paragraph guidance: not applicable / not present.** F3 is blog-only
  (`gates.py:173-174`), so `adapt.py` never needs this and doesn't have it. For blog
  (`generation.py`), the guidance IS present (see §2) but only as one joined directive line, not
  elaborated.
- **(b) 1 atom / 1 emotion: absent.** `_FACEBOOK_INSTRUCTIONS` (`adapt.py:72-84`) requires "**at
  least one** `[R:atom_id]` citation carried over from the blog text" — a floor, not an exclusivity
  rule — and `adapt_channels()` (`adapt.py:121-122`) feeds the model every atom the blog piece ever
  cited, with nothing narrowing selection to one. Confirmed absent by direct read, not inferred.
- **(c) social CTA/hook rubric: partially present, and the part that's present is verified working.**
  `_FACEBOOK_INSTRUCTIONS` (as of today's AA-405 fix) explicitly requires: first sentence = hook,
  last sentence = literal "Design This Journey" CTA phrase before the HASHTAGS line. This directly
  targets hook_story_cta's "first line is the hook" (100% pass rate in real data) + the deterministic
  CTA check (100% pass post-fix). `_TIKTOK_INSTRUCTIONS` (`adapt.py:86-93`) requires a `"HOOK:"` line
  ≤15 words but gives **no guidance on what makes a hook strong**, and nothing addressing
  hook_beats_payoff's "timed beats present"/"payoff lands" criteria specifically — though neither has
  shown up as a real F8 failure yet (tiktok's real failures in this corpus are 100% F9-social, never
  F8) so this is a code-confirmed gap without corpus evidence of actual harm yet.

---

### 5. Root-cause conclusion per gate (summary)

| gate / sub-check | conclusion | evidence |
|---|---|---|
| F3 check (1) one-sentence paragraph | **(b)** prompt exists, structurally undeliverable across independent uncoordinated batch calls | directive text in `research.py` confirmed present; 6/7 real held pieces still show zero one-sentence paragraphs |
| F3 check (2) section-length variance | **(b)**, worse — code actively contradicts the directive (uniform `words_per_section` for every batch) | `generation.py:136-142`; 3/7 real held pieces |
| F3 check (3) bulleted-list cap | not a live problem | 0/7 real occurrences |
| F8 hook_story_cta "ends with CTA" | was **(a)** missing, **now fixed** (verified) | 4/4 pre-fix fail → 0/9 post-fix fail |
| F8 hook_story_cta "one atom, one emotion" | **(a)** prompt-missing | `adapt.py` gives every cited atom + only a citation floor; still failing all 3 weeks |
| F8 AIDA "attention hook first" / "single clear action (CTA)" | **(a)** prompt-missing, total (no framework ever explained to the writer) | `generation.py` passes bare framework label only; 1 real occurrence, code gap applies to every blog framework |
| F9_brand_seo_audit_social | **(b)** predominant, real **(c)** rubric-calibration concern layered on top | 15/33 (45%) held, tiktok 3x facebook, repair rounds never converge on the same complaint across all 15 real pieces traced |

No gate's core detection LOGIC (F3's regex/threshold, F8's deterministic CTA check, F9's
contract/parsing) is shown to be buggy by this pass — every conclusion above is a writer/adapt
PROMPT gap (missing or structurally undeliverable instruction), except F9's `GENERIC_AI_WORDING`/
`SUMMARY_OFF_BRAND` vocabulary, which real repair-round data suggests may also need rubric-level
calibration, not just a prompt fix. This is the basis for the next step's prompt-writing pass — not
implemented in this STEP 0.

## Implementation — 15/08/2026 (branch `pqnghiep1354/aa-404-writer-prompt-f3-f8-f9`)

Built directly on STEP 0's conclusions above — no new recon, no re-reading. 4 parts, each fixing
exactly the mechanism STEP 0 pinned as root cause, nothing broader.

### Decisions

- **Part 1 (F3)**: chose "most atoms = long-section owner, fewest atoms = short-paragraph owner"
  over a random assignment. Deterministic and reproducible for a given Brief (same outline always
  gets the same assignment — easier to debug a held piece later), and the section with the most
  material is the natural fit for "develop this one in more depth" rather than an arbitrary pick.
  Ties break by outline order (Python's `max`/`min` keep first-encountered), not by hashing/random.
- **Part 1**: the length-redistribution multiplier is 1.7x, not the gate's bare 1.4x minimum — margin
  for real writer variance (the model won't hit an exact word target), so a real draft that lands
  short of its 1.7x target by some percent should still clear the 1.4x gate threshold.
- **Part 2 (F8)**: the ceiling is enforced at the DATA layer (`_select_atoms_for_channel()` in
  `adapt.py`, called before the prompt is even built), not only as prompt wording — matches the
  issue's explicit preference ("ưu tiên sửa ở tầng data ... nếu code cho phép"). tiktok is
  deliberately untouched (hook_beats_payoff has no single-atom constraint, and STEP 0 found zero
  real F8 failures on tiktok).
- **Part 3 (F8/AIDA)**: scoped to AIDA only, per the issue text ("khi framework=AIDA"). hub/PAS have
  the identical bare-label gap by inspection but zero real failures so far — flagged below as a
  follow-up rather than fixed here (Mistake-to-Rule, ADR-2026-009 — same stance already applied to
  F9-social's original build).
- **Part 4b (F9 rubric)**: added a `flagged_phrases` evidence field to the judge contract (mirrors F8's
  existing "every score of 1 needs a quoted evidence" pattern, `gate_framework()`) rather than trying
  to string-match free-text `notes`. The allowlist only fires when the CTA phrase is the SOLE quoted
  evidence for `SUMMARY_OFF_BRAND`/`GENERIC_AI_WORDING` — a CTA-phrase quote alongside a different
  real complaint, or any other failure code (`CTA_MISSING_OR_WEAK`, `HOOK_WEAK`), is left untouched.

### Changed

- `services/acp_produce/generation.py` — `generate_draft()`/`_build_batch_prompt()` now take a
  per-section `words_per_section: dict[str, int]` and `extra_directives: dict[str, list[str]]`
  (computed once per piece by new helpers `_select_variance_owners()`, `_compute_words_per_section()`,
  `_build_extra_section_directives()`) instead of one uniform word target and one blanket directive
  line. AIDA framework guidance (`_AIDA_FRAMEWORK_GUIDANCE`) and positional
  attention-hook/single-CTA notes added when `brief.framework == "AIDA"`. The old blanket
  `VARIANCE DIRECTIVES` line is kept as-is (still true, still useful for the non-gate-enforced "vary
  H2 rhythm" directive) — the new targeted notes are additive, not a replacement.
- `services/acp_produce/adapt.py` — new `_select_atoms_for_channel()` restricts facebook to its
  first-cited atom only (tiktok unchanged); `_FACEBOOK_INSTRUCTIONS` states the single-atom
  constraint explicitly; `_ADAPT_SYSTEM_PROMPT_BASE` requires at least one REQUIRED-lexicon word even
  under the tight word budget; `_TIKTOK_INSTRUCTIONS` reconciles "spoken/conversational" with the
  brand's premium register.
- `services/acp_produce/gates.py` — `gate_brand_seo_audit_social()`'s judge contract gained
  `flagged_phrases`; response parsing drops `SUMMARY_OFF_BRAND`/`GENERIC_AI_WORDING` (and upgrades
  `flagged` → `pass` if nothing else remains) only when every quoted phrase is the brand CTA phrase.
- Tests: `tests/unit/test_aa370_generation.py` (+16), `tests/unit/test_aa371_adapt_faq.py` (+4, plus 1
  existing test split into 2 to reflect the new facebook/tiktok asymmetry), `tests/unit/
  test_aa372_gates.py` (+4). Full suite: 1235 passed (was 1213 pre-AA-404), 0 regressions, flake8
  clean.

### Tradeoffs

- Part 1's word-count redistribution changes `generate_draft()`'s per-batch `max_tokens` sizing (now
  summed from the per-section dict instead of one uniform value) — verified this still respects
  `_MIN_MAX_TOKENS`/`_MAX_MAX_TOKENS` and that the total redistributed word budget stays within
  F4_brief_compliance's ±30% tolerance (unit test), but the actual real-world word-count drift can
  only be confirmed against a real Sonnet call, not a mock.
- Part 3 leaves hub/PAS with the same bare-framework-label gap Part 3 fixes for AIDA — a known,
  intentionally deferred gap, not an oversight (see Decisions above).
- Part 4b is a genuine change to F9-social's evaluation criteria, not just a prompt tweak — flagged as
  the highest-risk part of this PR (see PR description). The rubric's `GENERIC_AI_WORDING`/
  `SUMMARY_OFF_BRAND` codes are otherwise untouched; this only prevents one specific, real,
  confirmed false-positive shape (the brand's own mandated CTA phrase getting flagged as generic).
  It does not address the broader "moving target" non-convergence STEP 0 documented — that's a
  bigger rubric-calibration conversation with Ms. Thu (17/08), out of scope here.

### Should know

- None of this was verified against a real Bedrock/Nova Pro call — all tests mock `invoke_claude`/
  `invoke_judge`. The real test is Nghiep's post-merge N7 week=4 run (see PR description) — the
  writer/judge might still not follow the new instructions reliably even though the mechanism is
  now sound (deterministic ownership, data-layer ceiling, evidence-gated allowlist).
- Part 4b's `flagged_phrases` field is new — a judge response that doesn't populate it (older
  behavior, or a model that ignores the instruction) fails closed exactly as before this change
  (verified: `test_f9_social_no_allowlist_when_flagged_phrases_missing`), never silently passes.

## Post-merge verification — 15/08/2026 (investigation only, no code changed)

PR #153 merged, deploy verified (digest `sha256:a62f264b...93115bf2` == ECR `:latest`, task
`aa-cis-dev-api:88`, confirmed by Nghiep before this pass). Real N7 week=4 run, `aa_internal`,
run_id `4d20b52b-76fc-4419-9a8b-32fb06a56b94`, 4 slots / 12 pieces — **0/12 passed, 12/12 held**.
Pulled the full `gate_ledger`/`repair_log`/`brand_seo_audit`/`body_tagged` for all 12 pieces live
from RDS (S3-mediated ECS exec) rather than trusting the UI's held_reason summary alone.

Held-reason distribution: 3 blog on `F4_brief_compliance` (required H2 missing — **not AA-404
scope**, a pre-existing Brief-compilation bug where `required_h2s` entries are absurdly long
sentence-length strings, truncated mid-word in the violation text; noted, not investigated
further this pass), 1 blog on `F3_structural_variance`, 3 facebook on `F8_framework` "ends with
CTA", 1 facebook on `F8_framework` "one emotion", 4 tiktok on `F9_brand_seo_audit_social`.

### 1. F8 "ends with CTA" regression — NOT a Part 3 conflict, NOT a detection bug

Ruled out the hypothesis first: `gates.py::gate_framework()`/`_ends_with_cta()`/`_CTA_PHRASE_RE`
are **byte-identical** to the pre-AA-404 version (`git diff 657ee5f..8b70850 -- gates.py` touches
only the F1 regex (already shipped, AA-405) and the new F9-social block — zero lines changed in
the F8 CTA path). And architecturally, Part 3's AIDA guidance lives entirely in
`generation.py`'s blog-only E2 draft prompt (`_build_batch_prompt`/`_DRAFT_SYSTEM_PROMPT`) —
facebook pieces are generated by a completely separate module/call (`adapt.py`'s E3,
`_invoke_channel_with_retry` with its own `_ADAPT_SYSTEM_PROMPT_BASE`/`_FACEBOOK_INSTRUCTIONS`
system prompt). `pipeline.py::_resolve_framework()` confirms facebook always resolves to
`hook_story_cta`, never `AIDA` — there is no code path by which Part 3's text could ever reach a
facebook generation call. **Both hypotheses in the question are ruled out by direct evidence.**

Traced all 3 failing facebook pieces' real `body_tagged`: the phrase "design this journey" is
**absent from the ENTIRE body**, not just misplaced out of the last paragraph — confirmed via
direct case-insensitive search, not just the gate's own last-paragraph check. This is a genuine
writer non-compliance, not a false-negative in `_ends_with_cta()`.

Critically: all 3 pieces' `repair_log` round 1 (the very FIRST repair attempt, i.e. **the
un-repaired initial draft already failed**) shows `F8_framework: ends with CTA` as the violation —
meaning `adapt.py`'s own initial Sonnet call produced this, before any repair ever ran. This is a
real regression: the identical CTA instruction text (`_FACEBOOK_INSTRUCTIONS`'s "paragraph's LAST
sentence must invite... Design This Journey") produced **0/9 CTA failures** in weeks 2-3
(pre-AA-404) and **3/4** now, with the instruction text itself unchanged. The only prompt changes
between then and now are Part 2's two new bullets ("Build the ENTIRE caption around that one
atom/moment...") inserted **immediately before** the CTA bullet in the same instruction block, and
Part 4a's brand-lexicon bullet appended to the shared `_ADAPT_SYSTEM_PROMPT_BASE` that precedes it.
Both are real, plausible instruction-dilution vectors — more competing hard requirements in the
same system prompt, one of them landing right before the previously-reliable CTA line — though this
pass cannot prove which (or both) caused it without a controlled re-send; that would require
changing code, out of scope here.

Compounding it: all 3 pieces then got **4 repair rounds each**, every single round re-targeting
only `"framework criterion failed: ends with CTA"` — and every round's output still lacks the
phrase entirely. `repair.py::_build_prompt()` (unchanged by AA-404) sends the repair model nothing
but `CURRENT TEXT` + the bare violation string list — it never explains that "ends with CTA" means
a literal required phrase, nor where it goes. Confirmed real weakness, not new: AA-405's own PR
notes already flagged "repair-prompt genericness... NOT touched" as a tradeoff before AA-404 even
started; this run is the first real data showing that latent weakness actually costs 4 wasted
rounds when the initial-draft compliance regresses.

One piece (`slot_1405154a...`) also leaked `"Here is the Facebook caption:\n\n"` as a preamble
before the actual content — the `_CHANNEL_REQUIRED_MARKERS` parseability check only requires
`"HASHTAGS:"` to be present, so this leak didn't trip a retry, but it's an independent signal of
general instruction-following degradation on this call, consistent with prompt dilution.

**Classification: (b) — code fix correct and unchanged, but not strong enough to survive 2 new
competing instructions added to the same prompt; compounded by a pre-existing (b) repair-prompt
weakness that had simply never been exercised by a CTA failure before.**

### 2. F9 rubric allowlist — wiring confirmed correct; scope was always narrow, exactly as documented

Pulled all 4 held tiktok pieces' real `brand_seo_audit` JSON. **`flagged_phrases` is present and
populated in all 4** — direct proof the new judge contract (Part 4b) is live, reaching the judge,
and the judge is complying with it. The allowlist code path (`_is_cta_phrase_only_evidence()`,
`gates.py`) is reachable and would fire correctly if given CTA-phrase-only evidence — **this is not
a wiring bug**.

None of the 13 real quoted phrases across the 4 pieces are the CTA phrase or contain it (checked
against `_CTA_PHRASE_RE` — zero matches). Examples verbatim from this run: `"Korea's first meal is
a considered one."`, `"Seoul rewards a slower passage."`, `"South Korea has 3,000 kilometres of
trail — and most people never know."`, `"Some destinations require no preamble."` — all genuinely
different, specific, reasonably well-crafted sentences, the same "moving target" pattern STEP 0
originally documented pre-AA-404. The allowlist simply had **zero opportunities to fire** this run
because the ONE specific bug it targets (CTA phrase flagged as generic) didn't recur — consistent
with the allowlist doing exactly its documented narrow job, not evidence it's broken.

Also checked Part 4a's brand-lexicon reinforcement: real flagged pieces DO contain required lexicon
words now (`"a curated cycling journey, tailored for riders..."`, `"A well-designed journey here is
precisely calibrated..."`) yet still fail — confirms lexicon-presence is a necessary-but-nowhere-
near-sufficient condition against the judge's actual complaint, which is tone/style ("generic
travelogue style", "hyperbolic language"), not keyword absence.

**Classification: (c) — fix works exactly as scoped and documented (PR description: "does not
attempt the full recalibration, only the one confirmed bug"). The dominant real failure driver
(judge subjectively calling specific, competently-written prose "generic AI wording" with a
different complaint every time) is untouched by design — this is the known, already-flagged
Ms. Thu discussion (17/08), not a new finding.**

### 3. F3 mechanism — confirmed wired and working on first-pass generation; breaks under repair-loop churn

Behavioral proof (stronger than a code read) that `_select_variance_owners()`/
`_compute_words_per_section()` DID run and DID succeed: the one real F3-held piece's `repair_log`
round 1 shows **F9** as the first-failing gate, not F3 — and `F1→F2→F3→F4→F6→F7` all run before F9
in `pipeline.py`'s gate order. The only way F9 is `run_gates()`'s first failure is if F3 (and every
gate before it) **passed** on that piece's very first, un-repaired draft. The mechanism worked.

The real failure appeared only on **repair round 3**, after 2 unrelated repair rounds: round 1
targeted F9 (brand/SEO), round 2 targeted F4 (missing required H2s — the same pre-existing Brief
bug noted above). Round 2's F4 repair — which necessarily added/restructured H2 sections — is what
broke F3: the final body's real section word counts are `[363, 117, 111, 109, 16, 11, 229, 360]`.
Two sections (363 and 360 words) both ended up long, so `gate_structural_variance()`'s own check
(`lens[-1] < lens[-2] * 1.4`) correctly finds 363 is NOT ≥1.4x 360 (504) — even though both really
are much longer than the other 6 sections (11-229 words), the mechanism's "exactly one long
section" invariant broke because a repair round for an unrelated gate rewrote section content with
no awareness of which section was supposed to stay short. (The one-sentence-paragraph sub-check,
by contrast, survived both repair rounds intact — 4 genuine one-sentence paragraphs present in the
final body — that part of the mechanism is more robust to repair churn, at least in this example.)

Root cause, with an exact pointer: `repair_piece()` (`services/acp_produce/repair.py`, completely
untouched by AA-404) receives only `(body_tagged, violations)` —
`_build_prompt()` (lines ~142-146) has no channel for `words_per_section`/`extra_directives`/
`long_title`/`short_para_title`, and `run_gates()`'s `repair_fn` type
(`Callable[[str, list[str]], str]`, `gates.py` ~line 617) has no way to pass that state through
even if repair.py wanted to use it. A repair round fixing gate X has zero visibility into any
OTHER gate's structural requirements — this is a systemic gap, not unique to F3: the same pattern
independently explains the `slot_7d0a77a0...#facebook` piece's F8 "one emotion" failure this run
(gate order proves F8 was passing through 3 rounds of F9-only repair, then broke on round 3's
F9-repair rewrite — repair.py has no more awareness of "stay on one atom/one emotion" than it does
of section-length balance). By the time F3 surfaced as the failing gate, this piece's repair budget
was already exhausted (`repair_count=3` == its `repair_budget`) — F3 never got a single dedicated
repair attempt.

**Classification: (c) — a genuinely untested interaction.** AA-404's own unit tests exercised
`generate_draft()` in isolation (confirmed correct) but never through `run_gates()`'s full
repair loop, so this interaction was never exercised before real production data surfaced it.
Not (a): no bug in the wiring itself — `_select_variance_owners()`/`_compute_words_per_section()`
provably ran and worked. Not really (b) either: the mechanism isn't "too weak," it's simply never
invoked again after the first draft, by design of a repair path that predates AA-404 and was
never extended to carry any structural metadata forward.

### 4. Summary table

| Gate | Classification | Exact pointer |
|---|---|---|
| F8 "ends with CTA" (facebook) | (b) prompt fix diluted by 2 new competing instructions added right next to it, compounded by pre-existing (b) repair.py genericness | `adapt.py::_FACEBOOK_INSTRUCTIONS` (Part 2's 2 new bullets now precede the CTA bullet); `adapt.py::_ADAPT_SYSTEM_PROMPT_BASE` (Part 4a's lexicon bullet); `repair.py::_build_prompt()` (unchanged, no CTA-specific guidance) |
| F9_brand_seo_audit_social | (c) fix correct, scope was always narrow (documented) — dominant real driver untouched by design | `gates.py::_is_cta_phrase_only_evidence()` fires 0/13 times this run because 0/13 real complaints were CTA-phrase false positives — working as scoped |
| F3_structural_variance | (c) genuinely untested interaction: mechanism works on first-pass generation, not preserved across repair rounds for unrelated gates | `repair.py::_build_prompt()` / `repair_piece()` signature has no channel for section-ownership state; same gap independently caused the `slot_7d0a77a0#facebook` F8 "one emotion" regression |

No gate's DETECTION logic (the actual `gates.py` checks) is shown to be buggy by this pass, same
as STEP 0's original conclusion — every finding here is either a prompt-competition effect (F8) or
a repair-loop blind spot that predates AA-404 and was never exercised until now (F3, and by the
same mechanism, F8 "one emotion").

### Side note — 401 pattern (not investigated, per scope)

Checked `/ecs/aa-cis-dev` CloudWatch logs for the run's window (08:47-09:35 UTC, 15/08/2026,
±10min padding) for a `401` pattern — found none in the ECS app logs themselves (only `/health`
200s and one unrelated `GET / 404`). The reported 401 likely originates at a layer this log group
doesn't cover (API Gateway/Lambda authorizer, or the Vercel `/api/admin` proxy) — not root-caused
this pass, per the issue's own explicit deprioritization.

## STEP 0 (mở rộng) — repair.py blind spot, system-wide survey — 15/08/2026 (investigation only)

Goal: before patching F3/F8 narrowly, check how far the "repair regresses an unrelated gate"
pattern actually spreads across ALL gates, using real data from all 4 runs so far (week 1-4, 45
pieces total — `de8337ba` 12, `e64befb4` 9, `170a0825` 12, `4d20b52b` 12), not just the 2 cases
already found.

### 1. `repair.py::_build_prompt()` — exact input, verbatim

```python
def _build_prompt(body_tagged: str, violations: list[str]) -> str:
    return (
        "CURRENT TEXT:\n" + body_tagged + "\n\n"
        "VIOLATIONS TO FIX (fix ONLY these, nothing else):\n- " + "\n- ".join(violations)
    )
```

That is the **entire** input: the current full `body_tagged` string, plus the plain violation
strings from whichever ONE gate `run_gates()` picked as the first failure this round (`gates.py`'s
own `violations.append(...)` text, e.g. `"framework criterion failed: ends with CTA"`, `"no
section is notably longer than the others"`). `repair_piece(body_tagged, violations)`'s only other
input is the fixed `_REPAIR_SYSTEM_PROMPT` (generic "fix only these, preserve everything else" +
the shared `AA_BRAND_IDENTITY_PROMPT` block — same text `generation.py`/`adapt.py` also splice in).

**Not passed to repair, ever, for any gate:** `Brief` (required_h2s, keyword, word_range,
atoms_by_section, cta_target), `atom_text_by_id`, `channel`, `framework`, the F3 section-ownership
decision (`long_title`/`short_para_title`), the F8 facebook single-atom choice, or
`brand_rubric_text` (F9's tenant-specific rubric — only the generic brand block is present, via
the system prompt). One notable exception found while re-checking: `AA_BRAND_IDENTITY_PROMPT`
itself (already in every repair call's system prompt) **does contain** the line `CTA: "Design This
Journey" (never "Book Now")` — so the CTA phrase is technically already in scope for every repair
call today, yet 4/4 repair rounds on 3 real facebook pieces (week=4) still failed to add it. This
means the F8-CTA failure isn't purely "missing information" — the terse violation string
(`"framework criterion failed: ends with CTA"`) doesn't connect to the brand block's one-line CTA
mention elsewhere in context; the model isn't told this violation IS actionable via that specific
already-present fact. Worth folding into whatever fix ships (see §4) — richer STRUCTURAL context
alone may not be sufficient if the violation strings themselves stay this terse.

### 2. Per-gate classification (self-contained vs. piece-wide dependency)

| Gate | Piece-wide invariant it depends on? | Currently preserved across unrelated repairs? |
|---|---|---|
| `output_rules` | N/A — runs BEFORE `run_gates()`, short-circuits to held immediately, never enters the repair loop at all (`pipeline.py`'s own docstring). Out of this survey's scope. | N/A |
| F1_grounding | No fixed piece-wide state, but implicitly at-risk: ANY repair that adds new prose (e.g. to satisfy F4) can introduce a fresh ungrounded claim, since repair has no atom list to check new content against. | Not observed regressing in this data (0/14 events), but structurally unguarded — repair.py never receives `atom_text_by_id`. |
| F2_banned_patterns | Same shape as F1 — local pattern scan, but new prose written blind (no lexicon reminder beyond what's baked into the shared brand block) can reintroduce a banned word. | **No — observed regressing 2x**, both times caused by F4's repair (see §3). |
| F3_structural_variance | **Yes — explicit, AA-404 Part 1's own invariant**: `long_title`/`short_para_title`, computed once in `generate_draft()`, never stored anywhere `repair_piece()` can see. | **No — observed regressing 3x**, every time caused by F4's repair. |
| F4_brief_compliance | Yes — `Brief.required_h2s` (the full list), `keyword`, `word_range`, `internal_links`. Repair only ever sees ONE missing-H2 violation string per round, never the full requirement list, so it can't tell if a fix for a DIFFERENT gate silently dropped/renamed a required heading. | **No — observed regressing 3x** (as a VICTIM, caused by F9's repair) and separately confirmed as the single most common TRIGGER of other gates' regressions (F3 3x, F2 2x — see §3). |
| F6_route_to_sellable | Partially self-contained: 2 of its 3 sub-violations (`no CTA target`, `url_alive not True`) are external DB/caller state and are filtered OUT of the repair loop entirely (`pipeline.py::_is_f6_content_fixable`) before ever reaching `repair_piece()`. The 3rd ("CTA {target} not present in body") is a literal-string check, self-contained. | N/A / not observed as cause or victim in this data. |
| F7_faq_dedup | Self-contained — intra-piece FAQ-vs-body token overlap, a local check against text already fully present in `body_tagged`. | Not observed as cause or victim. |
| F8_framework (hook_story_cta, facebook) | **Yes**: (a) exactly one atom must stay cited — `adapt.py`'s Part 2 ceiling is enforced only at the INITIAL generation call, not at repair time; (b) the CTA phrase must land in the final paragraph. | **No — observed regressing 6x**, every time caused by F9-social's repair (the single most common pattern in the whole dataset). |
| F8_framework (AIDA, blog) | **Yes**: which OutlineSection is first/last in the outline (for the opening-hook/closing-CTA notes, AA-404 Part 3) — same class of gap as F3's, also never stored past `generate_draft()`. | Not enough real AIDA data yet to confirm a regression case (only 1 real AIDA failure total, week=3, and it wasn't preceded by an unrelated repair round) — but structurally identical exposure to F3's, same missing plumbing. |
| F9_brand_seo_audit (blog) | Loosely piece-wide (tone/voice consistency across the whole piece) but its OWN fixes are self-contained — the risk runs the OTHER direction: fixing F9 tends to rewrite substantial prose, which can silently drop/reword a required H2 heading. | Acts only as a TRIGGER in this data, not a victim (3x → regressed F4). |
| F9_brand_seo_audit_social (facebook/tiktok) | Same shape as blog F9 — fixing it rewrites the whole short caption/script, with no reminder of F8's structural rules (one atom, CTA placement, hook position) that the SAME piece must also keep satisfying. | Acts only as a TRIGGER in this data (6x → regressed F8) — the dominant pattern in the whole dataset. |

### 3. Real-data evidence — a detector, not a guess

Built a mechanical detector over all 45 real pieces' `repair_log` (not manual reading): for each
piece, walk its sequence of `(round, gate_targeted)` entries plus the final `held_reason` gate as
one more implicit "check". `run_gates()`'s own logic guarantees round N's `gate_targeted` is always
the FIRST failing gate, in fixed pipeline order (`F1→F2→F3→F4→F6→F7→F8→F9`), on the fresh full-stack
check after round N-1's repair. So whenever round N+1 targets a gate that sits EARLIER in that
fixed order than round N's target, gate order alone proves the earlier gate must have been PASSING
right after round N-1 (else it would have been picked first) and broke as a direct result of round
N's repair. Ran this over all 45 pieces, no manual cherry-picking.

**Result: 14 regression events, across 13 distinct pieces (13/45 = 29% of all real pieces run so
far) — a conservative lower bound, since this detector only catches order-inversions it can prove
from gate position, not same-position thrashing or damage to a gate that was already failing for
an unrelated reason.** Exactly 4 distinct causal pairs, no others:

| Repairing this gate... | ...regressed this gate | Count |
|---|---|---|
| F9_brand_seo_audit_social | F8_framework | **6** (the single dominant pattern) |
| F4_brief_compliance | F3_structural_variance | 3 |
| F9_brand_seo_audit | F4_brief_compliance | 3 |
| F4_brief_compliance | F2_banned_patterns | 2 |

This is **broader than F3/F8 alone** — it spans F2, F3, F4, and F8 as victims (4 of 8 real gates),
triggered specifically by F4's and F9's repairs (both F9 variants). F1, F6, F7 never appear as
cause or victim in this data (consistent with §2's classification: their fixes tend to be narrow,
local edits, not wholesale rewrites). Notably **F4 and F9 are the only two gates that ever trigger
a downstream regression** — both are exactly the two gates whose typical fix requires writing
substantial NEW prose (a missing H2 section's worth of content, or a broadly reworded caption/
paragraph) rather than a small local edit, which structurally makes sense: a small edit has little
surface area to accidentally break something else; a substantial rewrite, produced with none of
generation-time's specialized context (atoms, variance ownership, framework rules, banned lexicon
reminders), has a lot.

### 4. Recommendation: one general fix, not four narrow patches

**One general fix.** The evidence in §3 rules out "this is an F3/F8-specific quirk" — the same
mechanism (repair rewrites broad new content with none of generation-time's context) already hit 4
different gates from 2 different triggers, and by §2's classification, F1 and F8-AIDA are
structurally exposed to the identical gap even though real data hasn't caught them at it yet.
Patching F3 and F8 in isolation would fix exactly the 2 known cases and leave F2/F4's regression
(3x) untouched, plus leave F1 and F8-AIDA unguarded for whenever their turn comes — precisely the
"vá 1 lớp lộ lớp khác" cycle the issue is trying to avoid (AA-396 S146, AA-405's own 2-layer F1 fix
already lived this once).

**Shape of the fix, scoped for feasibility (not a redesign):** most of what's missing is
*re-derivable* from data pipeline.py already has in scope at the `run_gates(..., repair_piece,
...)` call site — this is not a new-state-storage problem, it's a "repair.py never receives it"
plumbing problem:

- `run_piece_through_produce_gates()` (`pipeline.py`) already holds `brief`, `channel`,
  `effective_framework`, `text_by_id` (== `atom_text_by_id`) as closure variables — it already
  wraps every DET/LLM gate function in a closure to bind exactly this kind of extra context
  (`_f1`...`_f9` closures, same file). The identical pattern extends trivially to wrapping
  `repair_piece` the same way before passing it as `run_gates()`'s `repair_fn`.
- F3's `long_title`/`short_para_title` don't need new storage: `generation.py::
  _select_variance_owners()` is a pure function of `outline` (itself derivable from
  `brief.required_h2s`/`brief.atoms_by_section`) — repair can call the SAME function again with the
  same Brief and get the SAME deterministic answer, no new field on `Piece` needed.
- F8 facebook's "stay on one atom" doesn't need new storage either: `atom_usage.py::
  atom_ids_cited(piece)` already computes "which atoms does this body currently cite" from
  `body_tagged` alone — repair can be told "you may cite only the atom id(s) already present,
  never add another" using the SAME helper, on the CURRENT (possibly already-repaired) body.
- F4's required_h2s/keyword/word_range are just `Brief` fields — direct pass-through, no
  computation needed.
- The one piece that DOES need new logic (not just re-derivation): F8-CTA specifically, per §1's
  finding that the brand block already mentions the CTA phrase but the terse violation string
  doesn't connect to it — repair.py would benefit from a small violation-string-to-guidance lookup
  (e.g., when a violation contains `"ends with CTA"`, append an explicit one-line reminder of the
  exact phrase and where it goes) rather than assuming richer context alone fixes it.

**Concretely**: extend `repair_piece(body_tagged, violations, *, brief=None, channel=None,
atom_text_by_id=None)` (all optional, backward compatible with existing tests/callers),
have `_build_prompt()` append a "STRUCTURAL CONTEXT TO PRESERVE (regardless of which violation
below you're fixing)" block built from those optional inputs when present, and update
`pipeline.py`'s one `run_gates(..., repair_piece, ...)` call site to pass a closure supplying them
— the same wrapping pattern already used for every gate function in that file.

### 5. Complexity/risk estimate — feasible before 17/08, with caveats

**Lower risk/effort than it first looks, BECAUSE most of the needed data is re-derivable rather
than needing new state storage or a `Piece`/DB schema change** (no migration, no new `Piece`
field) — this is the main reason to prefer it over 4 separate narrow patches, not just correctness:

- **Small, contained blast radius**: touches `repair.py` (signature + `_build_prompt()`,
  optional/backward-compatible params) and ONE call site in `pipeline.py` (wrap `repair_piece` in
  a closure, identical pattern to the 8 gate closures already there). No other module needs to
  change — `slot_runner.py`, `generation.py`, `adapt.py`, `gates.py` are all untouched.
- **Real effort is in the repair prompt's new context block and its tests**, not the plumbing:
  writing the "STRUCTURAL CONTEXT" text clearly enough that repair actually follows it (the same
  "does the model act on it" risk every AA-404 prompt change already carried), and testing each of
  the 4 confirmed real regression shapes as a regression-guard unit test (mirroring the piece-7
  leak-guard pattern already used in this codebase) — realistically a half-day to a day of focused
  work given STEP 0's exact real-data shapes are already in hand (this doc), not new discovery.
- **Genuine unknown**: whether adding a STRUCTURAL CONTEXT block measurably improves real repair
  behavior can only be confirmed against a real Bedrock call, same caveat as every other AA-404
  prompt change — mocked tests can prove the context reaches the prompt, not that Sonnet reliably
  obeys it. A real N7 run (week=5) would still be needed post-fix to confirm, same as AA-404's own
  "Should know" already states.
- **Comparison to 4 narrow per-gate patches**: narrow patches are NOT obviously cheaper once you
  count all 4 confirmed shapes (F9→F8, F4→F3, F9→F4, F4→F2) — each would need its own
  targeted fix inside `repair.py` (there's only one `_build_prompt()`, so "narrow" patches would
  still end up as 4 special-cased branches in the same function) plus its own regression test, with
  no shared mechanism protecting F1/F8-AIDA from the SAME gap once real data eventually exercises
  them. The general version is a similar amount of code, covers more real+likely cases, and avoids
  a 5th/6th patch later for F1 or F8-AIDA.
- **Recommendation given the 17/08 deadline**: the general fix is feasible in the remaining time
  and is the better bet specifically BECAUSE Ms. Thu's 17/08 conversation is about the F9 rubric,
  and F9 is confirmed (§3) as the single largest TRIGGER of collateral damage (9 of 14 events:
  6 F9-social→F8 + 3 F9-blog→F4) — any rubric recalibration that changes F9's failure rate changes
  how often this whole class of bug fires, so shipping the general repair-context fix BEFORE that
  conversation gives a more stable baseline to reason about rubric changes against, rather than
  debugging rubric changes and repair-context regressions at the same time.

## Implementation — piece invariants object (general repair.py fix) — 15/08/2026 (branch `feature/aa-404-repair-piece-invariants`)

Built directly on the STEP 0 (mở rộng) recommendation above (§4/§5) — no new recon, no re-reading.
One general fix, not four narrow per-gate patches, per that section's own conclusion.

### Decisions

- **`PieceInvariants` lives in `repair.py`, not `models.py`/a new `Piece` field.** It's a plain
  `@dataclass`, built fresh once per gate-stack run by `pipeline.py::_build_repair_invariants()` and
  passed through a closure (`_repair`) — never persisted, never a DB column. Matches STEP 0's own
  explicit rejection of new state storage: everything in it is either a direct `Brief` pass-through
  or re-derived from a pure function `generation.py` already exports/uses
  (`build_outline`/`_select_variance_owners`).
- **`single_atom_required` is a flag, not a stored atom id.** The actual atom id(s) to preserve are
  re-derived fresh from the CURRENT `body_tagged` at prompt-build time
  (`repair.py::_currently_cited_atom_ids()`, same regex as `atom_usage.ATOM_CITE_RE`, reimplemented
  against a bare string since `atom_usage.atom_ids_cited()` requires a `Piece`). A stored id captured
  before round 1 could go stale by round 3; a re-derived one can't.
- **`cta_required`/`single_atom_required` are keyed off `effective_framework == "hook_story_cta"`**,
  the SAME resolved key `pipeline.py`'s own `_f8` gate closure scores against — not `channel ==
  "facebook"` directly. Ties the invariant to the actual mechanism (`FRAMEWORK_TABLE`) instead of a
  parallel hardcoded channel string that could silently drift from it.
- **Added `aida_opening_section_title`/`aida_closing_section_title`** even though STEP 0 found zero
  real regressions there yet (only 1 real AIDA failure, not preceded by an unrelated repair round).
  Included anyway because it's the identical class of gap as F3's (STEP 0's own §2 table already
  flagged this exposure) and costs nothing extra — the outline is already computed for the F3
  section-ownership fields.
- **F4→F2 (banned-word reintroduction) gets no new `PieceInvariants` field.** Instead, one line was
  added to `_REPAIR_SYSTEM_PROMPT`'s existing HARD RULES ("any NEW prose you write ... must still
  avoid every word in the FORBIDDEN language list above") — the forbidden lexicon was already present
  in every repair call via `AA_BRAND_IDENTITY_PROMPT`, so the gap wasn't absence of the list, it was
  the prompt never explicitly connecting "you're about to write new prose" to "check it against the
  list above." A system-prompt-level reminder is unconditional (every repair call, every piece) and
  needs no piece-specific plumbing.
- **The "ends with CTA" violation-string hint (`_violation_hints()`) is unconditional**, not gated on
  `invariants` being passed. STEP 0's own finding was that the terse violation string
  (`"framework criterion failed: ends with CTA"`) never connected to the brand block's one-line CTA
  mention elsewhere in context — the fix for that is keyed off the violation TEXT itself (a string
  only `gates.py::_ends_with_cta()` ever emits), so it fires as defense-in-depth even if a caller
  somehow invokes `repair_piece()` without wiring `invariants` through.
- **`invariants` is keyword-only with a `None` default** (`repair_piece(body, violations, *,
  invariants=None)`) — every pre-AA-404 caller/test keeps working unchanged, and `_build_prompt()`
  with `invariants=None` reproduces byte-for-byte the exact pre-AA-404 prompt shape for the
  non-CTA-violation case (confirmed by `test_repair_piece_with_no_invariants_matches_pre_aa404_prompt
  _shape`).

### Changed

- `services/acp_produce/repair.py` — new `PieceInvariants` dataclass (exported), `repair_piece()`
  gains keyword-only `invariants` param, `_build_prompt()` now also emits a "STRUCTURAL CONTEXT TO
  PRESERVE" block (`_build_structural_context()`) plus a violation-text-keyed CTA hint
  (`_violation_hints()`) when applicable. `_REPAIR_SYSTEM_PROMPT` gained one new HARD RULE line
  (forbidden-lexicon reminder for newly-written prose).
- `services/acp_produce/pipeline.py` — new `_build_repair_invariants()` (pure, builds `PieceInvariants`
  once per gate-stack run from `channel`/`effective_framework`/`brief`, reusing
  `generation.build_outline()`/`generation._select_variance_owners()`); `run_piece_through_produce_
  gates()`'s `run_gates()` call now passes a `_repair` closure (binds `invariants`) instead of the bare
  `repair_piece` reference. Module docstring's "Repair loop" section updated to describe the new
  wiring and trace all 4 confirmed causal pairs to this fix.
- Tests: `tests/unit/test_aa376_repair.py` (+10 new tests: no-invariants backward-compat, the two
  named real regression shapes — F9-social-triggered repair preserving F8's CTA/single-atom, F4-
  triggered repair preserving F3's section-ownership — plus required_h2s/AIDA/empty-invariants/the
  unconditional CTA-violation hint), `tests/unit/test_aa404_repair_invariants.py` (new file, +7 tests,
  pure unit coverage of `_build_repair_invariants()`'s channel/framework routing), `tests/unit/
  test_aa364_pipeline.py` (+1 assertion block on the existing F9-repair-passes test, confirming the
  closure actually threads a correctly-flagged `PieceInvariants` through end-to-end). Full suite
  (`tests/unit/`, CI's actual scope): **1250 passed** (was 1235 pre-this-fix, +15 new tests, 0
  regressions). `flake8 api/ services/ shared/` (CI's exact invocation): clean, 0 new warnings.

### Tradeoffs

- **Coverage is scoped to the 4 confirmed causal pairs + the 2 structurally-identical-but-unconfirmed
  gaps STEP 0 already flagged (F1, F8-AIDA)** — F1_grounding's exposure ("new prose written blind, no
  atom list to check new content against") is NOT given its own invariant field here. STEP 0 itself
  classified this as "not observed regressing (0/14 events), but structurally unguarded" — deferred
  rather than guessed at ahead of real data, same Mistake-to-Rule stance (ADR-2026-009) already
  applied to F9-social's original build and to hub/PAS's AIDA-gap deferral in PR #153. If real data
  ever shows an F1 regression, `atom_text_by_id` is already in scope at the `pipeline.py` closure
  call site and would extend this the same way `required_h2s` did.
- **This fix cannot, by itself, guarantee the model FOLLOWS the new structural context** — same caveat
  every AA-404 prompt change so far has carried (STEP 0's PR #153 "Should know", now proven correct by
  the week=4 regression this task is directly responding to). The mechanism is now sound
  (deterministic invariant computation, correct closure wiring, unit-tested prompt content) but only a
  real N7 run can confirm Sonnet actually preserves these constraints under real repair-round
  pressure — not verifiable against mocked tests.
- **The AIDA opening/closing fields add prompt surface for a case with zero confirmed real
  regressions yet** — a deliberate bet that "cheap to add now, reusing already-computed outline data"
  beats "wait for real data, ship a 5th patch later" (STEP 0's own §4 argument for the general fix
  over narrow patches applies recursively here).

### Should know

- The 4 confirmed causal pairs and how this fix addresses each:
  - **F9-social → F8 (6x, dominant)**: `single_atom_required`/`cta_required` set for every
    hook_story_cta (facebook) piece: `PieceInvariants` survives regardless of which round's violation
    triggered the repair call, so an F9-triggered round still sees the CTA/single-atom preservation
    lines. `test_repair_prompt_preserves_cta_when_f9_social_repair_triggers` and
    `test_repair_prompt_preserves_single_atom_when_f9_social_repair_triggers` reproduce this shape
    directly (F9 violation text, F8 invariants set) plus the end-to-end wiring assertion in
    `test_aa364_pipeline.py`.
  - **F4 → F3 (3x)**: `long_section_title`/`short_para_section_title` survive an F4-triggered round the
    same way. `test_repair_prompt_preserves_section_balance_when_f4_repair_triggers` reproduces this
    shape directly (F4 violation text, F3 invariants set).
  - **F9-blog → F4 (3x)**: `required_h2s` now travels with every repair call for a blog piece,
    regardless of which gate (including F9) triggered the round — a rewrite can no longer silently
    drop/rename a required heading without the model being told what the full required set is.
  - **F4 → F2 (2x)**: addressed at the system-prompt level (see Decisions above), not a new
    `PieceInvariants` field — the forbidden-lexicon reminder is unconditional across every repair call.
- Real verification is still the same as every prior AA-404 layer: a real N7 run against live Bedrock.
  This fix's mechanism (deterministic invariants, correct wiring) is unit-tested; whether Sonnet
  actually obeys the new structural-context block under real repair pressure is not.
- PR #153's already-known follow-ups (hub/PAS AIDA gap, F9's `GENERIC_AI_WORDING`/`SUMMARY_OFF_BRAND`
  rubric recalibration for Ms. Thu's 17/08 conversation) are untouched by this task — out of scope,
  as stated in the task text.

## F9 fix #1+#2+#3 — brand-rubric persona audit + concrete anchor + blog evidence — 15/08/2026 (branch `feature/aa-404-f9-brand-rubric-fix`)

Nghiep's decision (S150): don't wait for Ms. Thu's 17/08 conversation to start F9 work —
`gate_brand_seo_audit_social()` is this team's own AA-372 code, not a rubric she wrote and handed
over; 17/08 is for discussion, not a prerequisite to begin. Built on the F9 STEP 0 root-cause
investigation already in this doc (rubric code, judge prompt, real 3-round flag text, aamc/
comparison) — no new root-cause investigation here, only the persona-row check the task required
before touching fix #1.

### Persona-row investigation (REQUIRED before fix #1 — done first, per task instructions)

Read all 7 `shared.tenant_brand_rules` rows for `tenant_id=aa_internal` in full (not just the
excerpts from STEP 0), plus `created_at`/`brand_name`/`core_idea`/`good_examples`/`voice_examples`
(columns STEP 0 didn't query), and cross-referenced against `shared.tenants`.

**Finding: 6 of 7 rows are confirmed test/demo data for OTHER tenants, mistakenly created against
`aa_internal`'s `tenant_id` — not Adventure Asia's own brand, fragmented or otherwise.**
- `brand_name` values on the 4 richer rows — `WildKind Travel`, `Terra Family Expeditions` (2
  versions), `Trail Pulse`, `Atlas & Hearth` — are the **exact same names** as 4 separate, real rows
  in `shared.tenants` (fictional B2B demo agencies: `wildkind-travel`, `terra-family-expeditions`,
  `trail-pulse`, `atlas-hearth` slugs, all `is_active=true`, all distinct `tenant_id`s from
  `aa_internal`'s). These are per-tenant brand-rule rows for those OTHER tenants' demo accounts,
  created against the wrong `tenant_id` (almost certainly because `aa_internal` was the convenient
  tenant to test the multi-brand upload feature, AA-129, against, and never cleaned up).
- Confirmed independently by content: every one of the 4 rows' `good_examples` is about **Sigiriya,
  Sri Lanka** ("Climb Sigiriya...", "Dawn at Sigiriya before the gates open..."). `aa_internal`'s
  real trip catalog (confirmed via the earlier N0→N7 atom-lineage audit) is South Korea/Sapa/etc. —
  zero overlap. These rows describe a different company's product entirely.
- **One row (`brand_name='default'`, id `262dea1c...`) is the real Adventure Asia identity** —
  `core_idea: "Discreet executive adventure for senior professionals."`, `brand_type: "luxury
  adventure"` — matches `AA_BRAND_IDENTITY_PROMPT`'s own text ("Discreet Executive Adventure",
  "Senior professionals 40-60, $250k+") closely enough to be the same brand. **But its
  `system_prompt`/`style_guide`/`forbidden_words`/`good_examples` are all empty/null** — the row
  exists and is correctly `is_active=true`, but nobody ever populated its actual rubric content.
  Wiring this row in as `brand_rubric_text` today would REGRESS F9 (loses the REQUIRED/FORBIDDEN
  language list `AA_BRAND_IDENTITY_PROMPT` currently has hardcoded, gains nothing).
- The 7th row (`brand_name='Terra Family Expeditions'`, version 1, `is_active=false`) is the
  superseded version of the same mis-attached demo row — not relevant either way.

**Conclusion: fix #1 (wiring `shared.tenant_brand_rules` into `slot_runner.py:148`) is NOT built in
this PR**, per the task's own explicit instruction for this outcome ("nếu 4 row đều là test/seed
data không đáng tin, KHÔNG tự chọn đại 1 row — dừng lại"). None of the 7 rows are a safe choice: the
4 rich ones are a different company, the 1 real one is empty, and picking any of them would either
inject Sri Lanka content into a Korea/etc. tenant's audits or silently drop the REQUIRED/FORBIDDEN
language `AA_BRAND_IDENTITY_PROMPT` already enforces. This is now a separate, larger follow-up (needs
the "default" row's `system_prompt`/`style_guide`/`forbidden_words`/`good_examples` actually
populated with Adventure Asia's real content — a content task, not a code task — before wiring makes
sense) and should probably also include cleaning up the 4 mis-attached demo rows so they stop
cluttering `aa_internal`'s row set for the next person who queries this table.

### Decisions (fix #2 + #3, built)

- **Fix #2's GOOD example is not invented** — it's the exact sentence a real F9 audit (week=1
  retrigger, 15/08/2026, piece `de8337ba...:slot_845eb6ec83cdf1f082ec:blog#facebook`) flagged as
  `GENERIC_AI_WORDING`/`SUMMARY_OFF_BRAND`, and which STEP 0 already confirmed on inspection to be
  specific, verifiable, on-brand prose. Anchoring the prompt to the judge's own real false positive
  targets the exact miscalibration observed, rather than a guessed-at generic example.
- The anchor block (`_GENERIC_AI_WORDING_ANCHOR`, `gates.py`) is shared verbatim between
  `gate_brand_seo_audit()` (blog) and `gate_brand_seo_audit_social()` — same brand, same failure
  codes, same definition problem; no reason to maintain two copies that could drift.
- Fix #3 (blog `flagged_phrases`) intentionally does NOT also port social's CTA-phrase-only allowlist
  (`_is_cta_phrase_only_evidence()`) to blog — the task scoped fix #3 to the evidence requirement
  only, and blog has no deterministic CTA-phrase check the allowlist would be protecting (F8's
  `_ends_with_cta()` is `hook_story_cta`/facebook-only). The shared anchor block's own "required
  language cannot simultaneously be a violation" line covers this at the prompt level for blog
  without needing the code-level allowlist.
- `gate_brand_seo_audit()`'s docstring corrected — it previously claimed "real source is
  shared.tenant_brand_rules", which is false for the only real caller (`slot_runner.py`). Now states
  the actual source (`AA_BRAND_IDENTITY_PROMPT`) and links to the persona-row investigation above for
  why fixing that wiring is deferred.

### Changed

- `services/acp_produce/gates.py` — new `_GENERIC_AI_WORDING_ANCHOR` constant (shared by both F9
  calls); `gate_brand_seo_audit()` (blog) gains `flagged_phrases` in its contract, prompt instruction,
  and audit dict (mirrors social's PR #153 Part 4b shape); both prompts now inject the anchor block
  after `BRAND RUBRIC:`. Corrected docstring (see above).
- Tests: `tests/unit/test_aa372_gates.py` (+6: anchor text present in both blog/social prompts,
  blog now requires `flagged_phrases`, blog audit dict carries it through, defaults to `[]` when
  absent). Full suite: 1255 passed (was 1250 after PR #154, +5 new tests — one anchor-text test is
  shared logic covered once). `flake8` (CI's exact invocation): clean.

### Should know

- Fix #2/#3's real test is the same as every prior AA-404 prompt change: only a live N7 run can
  confirm whether the concrete anchor actually reduces the judge's false-positive rate, or whether
  the "moving target" pattern persists with different phrasing. Per task instructions, **not
  re-triggered in this session** — that's a separate step after PR review/merge, same digest-verify →
  retrigger sequence as PR #154.
- Fix #1 remains open. Next step there is a content task (populate the `default` `tenant_brand_rules`
  row for `aa_internal`) before any code change to `slot_runner.py:148` makes sense — flagged for
  Nghiep to decide, not built here.

## LLM cost — real N7 runs, whole AA-404 window — 15/08/2026 (investigation only, no code changed)

**Task: "how much has AA-404 actually cost in LLM spend so far" — real numbers only, no
estimates.** Repo is read-only for this pass.

### Should know

- **No DB token/cost log exists for N7.** Confirmed by code read, not inferred: `services/
  acp_produce/*` (generation.py/adapt.py/faq.py/repair.py/judge_client.py — the actual E2-E5 writer
  + F8/F9 judge call sites) never write to any cost/token table. `acp_shared.acp_stage_runs` has the
  right-shaped columns (`llm_cost_usd`/`tokens_input`/`tokens_output`) but is only written by the
  OLDER S1-S4 pipeline (`services/acp/s2`, `services/acp_s3`, `services/acp_s4`,
  `services/acp_shared/idempotency.py`) — N7 never calls `record_stage_cost()`. This is a real gap,
  not this session's problem to fix, but worth knowing before anyone else goes looking for N7 cost
  in that table and finds it empty.
- **Real source used instead: CloudWatch application logs** (`/ecs/aa-cis-dev`, 14d retention).
  Every N7 LLM call site logs `usage=` via structlog with real per-call token counts
  (`e2_draft_batch_success`/`e3_adapt_channel_success`/`e4_faq_batch_success`/`e5_repair_success`/
  `judge_llm_success`). Pulled all matching raw log events for the run window via
  `filter-log-events` (not Logs Insights aggregation — some CloudWatch events bundle multiple
  structlog lines into one `\r\n`-joined record, which Insights `stats` would undercount), parsed
  614 individual real LLM calls.
- **All 4 real N7 runs (week 1-4 + the week=1 retrigger) happened on 15/08/2026, 02:12-11:00 UTC** —
  confirmed via `acp_shared.acp_v2_runs.created_at` + `acp_deliver.pieces.created_at`/`updated_at`
  live query, not the guessed 13-14/08 window. PR #153/#154/#155 all merged same day (08:26/10:22/
  11:37 UTC) — used as the phase boundaries below.
- **Real cost: ≈ $3.85 USD total**, high confidence (application-log sourced, not Cost Explorer
  estimate):

  | Stage | Calls | Input tok | Output tok | Model / account | Cost |
  |---|---:|---:|---:|---|---:|
  | E2 draft | 28 | 44,757 | 13,708 | Sonnet 4.6, acc3 satellite | $0.34 |
  | E3 adapt FB/TikTok | 28 | 67,922 | 6,866 | Sonnet 4.6, acc3 | $0.31 |
  | E4 FAQ | 11 | 13,997 | 1,444 | Sonnet 4.6, acc3 | $0.06 |
  | **E5 repair** | **155** | **282,615** | **105,512** | Sonnet 4.6, acc3 | **$2.43 (63% of total)** |
  | F8/F9 judge | 392 | 522,868 | 82,814 | Nova Pro, acc2 native | $0.68 |
  | Gap-fill research (C3, optional) | ~17 | 18,788 | 1,515 | Haiku 4.5, acc3 (medium confidence — CloudWatch metric only, no per-call app log) | $0.03 |

  By phase (PR-merge boundaries): pre-PR#153 (week 1-3) **$2.32**, post-#153/pre-#154 (week 4)
  **$1.23**, post-#154/pre-#155 (week=1 retrigger, 3 pieces) **$0.28**, post-#155 **$0** (no run yet).
  173 repair rounds across 45 real pieces (avg 3.84, max 7) is why E5 dominates cost — matches the
  `n7_repair_budget_computed`/`REPAIR_BUDGET_CAP=8` scaling already documented above.
- **acc1 (pqnghiep-admin fallback) was never touched.** N7's writer/repair calls hardcode
  `account="acc3"` directly against `shared/llm_client/bedrock_satellite.py::invoke_claude()` — they
  don't go through `shared/llm_client/client.py`'s T1/T1.5a/T1.5b fallback chain that can land on
  acc1, so the 3-account chain routing mentioned in the ai-nghiep skill doesn't apply to N7 specifically.
- **Cross-checked against CloudWatch `AWS/Bedrock` account-wide metrics — deliberately NOT used as
  the headline number.** acc3 Sonnet metric shows 278 invocations/$3.76 (vs 222/$3.14 real);
  acc2 Nova Pro shows 488 invocations (vs 392 real). Gap explained: acc3 is also used by
  `services/content_generation/s1_from_atom.py` (a different writer pipeline, not N7), and acc2 is
  the shared native account with other traffic. Account-wide Cost Explorer/CloudWatch metrics would
  overstate AA-404's real cost — flagging this so nobody later "reconciles" against Cost Explorer and
  concludes the app-log number was wrong.
