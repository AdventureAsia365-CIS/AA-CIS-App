# AA-369 — C1/C2 DataForSEO keyword+SERP theo SLOT + C3 Brief compile & demand-law reject

STEP 0 (comment trên Linear AA-369, 06/08/2026) đã xác nhận thiết kế. File này ghi các quyết
định thật trong lúc code (khác STEP 0 ở chỗ nào, số migration thật dùng, v.v).

## Decisions

- **gap_statement giữ trong scope AA-369** (khác đề xuất "scope ra" của STEP 0) — Nghiep chốt
  dùng Bedrock satellite acc1 Haiku (`shared/llm_client/bedrock_satellite.py::invoke_claude(model="haiku")`),
  KHÔNG dùng Palmyra X5 (AA-336/AA-334 bake-off chưa kiểm lại, treo từ S130).
- Migration số thật: xem "Changed" bên dưới sau khi kiểm tra `api/migrations/` mới nhất.
- unknown_ledger đặt ở `acp_shared` (không phải `acp_deliver`/`acp_contract`) — theo đúng split
  D1 của AA-364.md (operational artifact, cùng nhóm quarter_plan/acp_stage_checkpoints).
- Atom pack cho 1 slot: KHÔNG tái dùng `fetch_curated_atoms(tour_id, pool)` (S1, AA-306 —
  fetch theo TOÀN BỘ tour) vì AA-369 cần atom theo `slot.atom_ids` (tập con), đúng tinh thần
  "theo SLOT, không theo TRIP" của cả C1/C2 lẫn atom pack. Viết fetch riêng theo atom_id list.
- Keyword/SERP: tái dùng `services/seo_intelligence/dataforseo_client.py::DataForSEOClient`
  (AA-197, thật, production) cho `fetch_keywords`/`fetch_people_also_ask`/`fetch_keyword_ideas`
  — KHÔNG viết `keyword_research()` mới. Chỉ thêm 1 hàm parse SERP→`SERPProfile` mới trong
  `services/acp_produce/dataforseo.py` (cùng file `parse_top_pages()`, cùng convention httpx).

## Changed (so với STEP 0)

- Migration số thật dùng: **095** (xác nhận `api/migrations/` mới nhất trước khi tạo là 094 —
  094 vẫn là mới nhất, 095 hợp lệ, KHÔNG bị trùng).
- Bảng đặt ở `acp_shared` (không phải `acp_deliver`) — giữ nguyên đề xuất STEP 0.
- Migration **ĐÃ APPLY** trên `aa-cis-dev-db` (06/08/2026, theo yêu cầu trực tiếp của Nghiep,
  qua S3-mediated ECS exec) — xem "Evidence 3" bên dưới cho verify đầy đủ (columns, indexes,
  RLS policy, schema_versions row) + evidence unknown_ledger row thật (đã bị chặn ở lần verify
  trước, giờ đóng xong).

## Tradeoffs

- `fetch_serp_profile()` (dataforseo.py, mới) gọi **1 lần SERP riêng**, KHÔNG chia sẻ call với
  `_top_ranking_urls()` (P0-2, đã test) dù cả 2 gọi cùng 1 endpoint cho cùng 1 keyword — nghĩa
  là 1 slot chạy C1/C2/C3 tốn 2 lần gọi SERP thay vì 1. Cố ý: refactor `_top_ranking_urls()` để
  share call sẽ động vào code P0-2 đã có test pass, rủi ro không đáng so với tiết kiệm 1 API
  call rẻ (AA-298.md đã kết luận DataForSEO cost không bao giờ là điểm chặn thật).
- Không có Facts pack thật (`FactsPack`/`acp_contract.facts` không tồn tại — grep xác nhận) —
  `Brief.facts_ids` luôn rỗng, FAQ matching chỉ so với atom, không so với facts. Đúng scope
  AA-369 (issue chỉ nói "atom pack của slot", không nhắc facts).
- Campaign override cho demand-law: bản gốc `aamc/research.py::compile_brief()` check
  `DecisionsStore.latest("special")` (không có bản thật trong repo) — thay bằng
  `slot.kind == "campaign"` (field thật duy nhất có sẵn) làm demand_reason. Đơn giản hơn bản
  gốc nhưng dùng đúng field Slot thật đang có.

## Should know

- **unknown_ledger DB-row evidence BỊ CHẶN cho tới khi Nghiep apply migration 095** — xác nhận
  live (ECS exec, real RDS): `compile_brief()` gọi đúng `log_unknown()` khi slot bị reject
  (no_atom, trong live run bên dưới), nhưng insert thất bại với
  `UndefinedTableError('relation "acp_shared.unknown_ledger" does not exist')` — đúng như dự
  kiến vì migration chưa được apply. Logic reject + SQL đã verify đúng ở mức unit test (mock
  DB, xem test_aa369_research.py); verify DB-row thật cần chạy lại sau khi Nghiep apply 095.
- **Phát hiện nuance demand-law (không phải bug mới, kế thừa từ bản gốc aamc)**: điều kiện
  reject là `volume is None AND confidence == "heuristic"`. Live test cho thấy DataForSEO có
  thể trả `confidence="dfs"` (API gọi thành công) NHƯNG `volume=None` cho 1 từ khoá thật sự
  không đo được (`search_volume: null` trong response, khác `0`) — trường hợp này KHÔNG bị
  demand-law reject vì confidence vẫn là "dfs", không phải "heuristic". Đây là hành vi y hệt
  bản `aamc/research.py::compile_brief()` gốc (cùng điều kiện), không phải lỗi mới — nhưng
  đáng ghi lại vì có thể khiến 1 slot với từ khoá vô nghĩa vẫn "có bằng chứng nhu cầu" nếu
  API trả về entry có confidence=dfs. Không fix trong AA-369 (ngoài scope, giữ nguyên logic
  gốc) — cân nhắc cho AA-327 nếu cần threshold chặt hơn (VD volume=None LUÔN reject bất kể
  confidence).

### Live smoke test thật (06/08/2026, ECS exec, real DataForSEO + real Bedrock Haiku + real RDS)

Chạy qua ECS exec (S3-mediated pattern) trên task thật của `aa-cis-dev-api` (ECS/RDS đã ở
trạng thái running sẵn khi bắt đầu — không cần start thêm, nhớ nhắc Nghiep stop cuối phiên).

- Tour thật: `08481a8e-ebf6-4832-bdb2-cf8500690234` ("Trip 3 – Meiji-no-Yakata", Japan), atoms
  thật `atom_398a4bcec0`/`atom_7b1664aa91` từ `acp_contract.tour_atoms`.
- C1 thật: keyword "Japan adventure tour" → `volume=90, confidence=dfs`.
- C2 thật: SERP thật → `intent_verdict=commercial`, 10 domain thật, 8 related search thật,
  **`word_count_range=None`** (xác nhận B13 vẫn chưa fix, đúng chủ đích), `paa_questions=[]`
  cho keyword cụ thể này (API thật không trả PAA cho từ khoá này — không phải bug; B14 đã xác
  nhận hoạt động đúng ở mức unit test với response có PAA thật).
- `parse_top_pages()` thật (P0-2, tái dùng nguyên): 2 URL tìm được, 1 crawl xong trong budget
  45s (`japantouradventure.com`, 5363 ký tự nội dung thật), 1 timeout bị DROP đúng theo thiết
  kế (không fabricate).
- C3 thật: Brief hợp lệ đầy đủ, **gap_statement thật từ Haiku** (satellite acc1, ADR-2026-031):
  > "The competitor page emphasizes tour operator credibility and experience (1,500+ reviews,
  > in-house tour design, local expert guides) and highlights immersive cultural activities
  > ... Our atoms focus only on a single historic villa/restaurant and don't address tour
  > provider credentials, guide expertise, or the breadth of cultural experiences..."

  Nội dung bám sát đúng trang crawl thật (không bịa số liệu ngoài trang) — xác nhận Haiku đang
  so sánh thật, không rubber-stamp.
- Reject-path thật: slot `atom_ids=[]` → `no_atom` reject đúng, `log_unknown()` được gọi đúng
  (lần chạy đầu bị chặn bởi bảng chưa tồn tại — đã đóng ở Evidence 3 bên dưới).
- Full Brief JSON + log thật đã lưu trong báo cáo Notion (AFTER DONE).

### Evidence 3 — Migration 095 APPLIED live + unknown_ledger row thật (06/08/2026, theo yêu cầu Nghiep)

Áp dụng qua S3-mediated ECS exec (không dùng psql trực tiếp, đúng pattern chuẩn). Verify ngay
sau khi apply, đọc lại từ `information_schema`/`pg_indexes`/`pg_policy` (không chỉ tin "lệnh
chạy không lỗi"):
- `to_regclass('acp_shared.unknown_ledger')` → tồn tại.
- Đủ 10 cột đúng kiểu (uuid/text/integer/date), đúng default (`gen_random_uuid()`,
  `CURRENT_DATE`, `1`).
- Đủ 4 index: `unknown_ledger_pkey`, `unknown_ledger_tenant_id_kind_detail_key` (UNIQUE — đúng
  target mà `ON CONFLICT` của `log_unknown()` cần), 2 index phụ.
- RLS: `relrowsecurity=True`, policy `tenant_isolation` với biểu thức đúng
  `tenant_id = current_setting('app.tenant_id', true)` (không phải GUC chết `app.current_tenant`).
- `shared.schema_versions` có row `095`.

Sau khi apply, chạy lại đúng kịch bản reject (`atom_ids=[]`, slot `verify-095-reject-1`) —
lần này KHÔNG còn `UndefinedTableError`:
```json
{
  "id": "3d9991fd-72d3-498e-9624-2a378ff3e2ba",
  "tenant_id": "00000000-0000-0000-0000-000000000001",
  "kind": "no_atom",
  "detail": "Slot verify-095-reject-1: no live atoms — topic rejected.",
  "demand_cost": null,
  "slot_id": "verify-095-reject-1",
  "keyword": "asdkjqwleqwe nonsense phrase xyz123",
  "count": 1,
  "first_seen": "2026-08-06",
  "last_seen": "2026-08-06"
}
```
Row test đã xoá sau khi verify (không để lại rác trong bảng thật). **Cả 2 evidence yêu cầu ban
đầu (Brief JSON thật + unknown_ledger row thật) giờ đã đóng đầy đủ.**
