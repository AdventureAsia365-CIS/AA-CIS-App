# AA-454 — 4 việc nhỏ gộp 1 PR: dead code, Settings chip, retry_count dead field, nav T4↔T6

Nguồn: `docs/claude_audit/AA-446-02-followup-investigation-s158.md` "Việc 2". 4 mục độc lập,
không file nào overlap. Không migration.

## Decisions

- **#9 (dead code)**: khi grep xác nhận 0 caller thật, phát hiện thêm `tests/integration/
  test_export_service.py` là consumer DUY NHẤT còn lại của `ExportService` — toàn bộ 17 test
  trong file đó chỉ test chính module chết này, chạy được nhờ `tests/integration/conftest.py`
  tự `ALTER TABLE ... ADD COLUMN IF NOT EXISTS hmac_signature/max_attempts` lên
  `webhook_deliveries` CHỈ để phục vụ test đó (không dùng ở đâu khác — verify bằng grep trước khi
  xoá). Quyết định: xoá cả `export_service.py` + `test_export_service.py` + đúng block
  `hmac_signature`/`max_attempts` trong conftest.py — KHÔNG xoá block `published_tours`
  `slug`/`country`/`is_active`/`quality_score` ngay phía trên, vì `test_export_integration.py`
  và `test_pipeline_e2e.py` vẫn dùng raw SQL độc lập lên đúng các cột đó (verify bằng grep, 2 file
  này không import `ExportService`).
- **#16 (Settings chip)**: đọc `services/content_generation/graph.py::build_graph()` xác nhận
  thứ tự edge thật: `generate→validate→llm_judge→brand_audit→flag_fix→revalidate` (nhánh happy
  path). KHÔNG thêm `increment_retry`/`hitl` vào list — đó là node điều khiển retry-loop, không
  phải bước tuần tự trong chip hiển thị (đúng như audit gốc chỉ flag thiếu `llm_judge`/
  `revalidate`, không flag thiếu 2 node kia).
- **#17 (retry_count)**: xoá đồng thời cả BE (`acp_health.py` response dict) và FE (TS interface
  `run-health/page.tsx`) — verify trước bằng grep FE xác nhận field không render ở JSX nào trong
  cùng file.
- **#3 (nav T4↔T6)**: phát hiện quan trọng khi đọc `portal/page.tsx` — portal đã đổi từ tab-switch
  nội bộ sang **route riêng cho mỗi T-stage** từ AA-430 (không còn `setActiveTab`). Vì vậy nav
  dùng `next/link` (`Link href="/portal/t6-atoms?tour_id=..."`), theo đúng convention
  `Sidebar.tsx` đã dùng, không phải prop callback như audit gốc phỏng đoán.
  - Phát hiện thêm: `GET /admin/atoms` (endpoint `AtomsTab.tsx` đã gọi sẵn) **đã hỗ trợ sẵn**
    `tour_id` query param filter (`api/routers/admin_atoms.py::list_atoms`) — không cần sửa
    backend cho chiều lọc atoms. Chỉ cần backend đổi 1 chỗ: thêm `pt.tour_id` vào SELECT của
    `list_my_versions` (`v1_tours.py`) để CatalogTab có id gốc (`raw_tours.tour_id`) build link
    — cột này vốn đã tồn tại (`published_tours.tour_id`, UNIQUE, FK `raw_tours.tour_id`), chỉ là
    chưa được SELECT ra trước đây.
  - Chiều T4→T6: `Link` "View atoms →" trong header panel chi tiết (khi `selected.tour_id` có),
    trỏ `/portal/t6-atoms?tour_id=<tour_id>`. AtomsTab đọc `tour_id` qua `useSearchParams()`,
    truyền thẳng vào query param `GET /api/tenant/admin/atoms?tour_id=`, hiện banner "Atoms for:
    <tour_name>" + link "Clear filter".
  - Chiều T6→T4: link "← My Catalog" LUÔN hiện (không chỉ khi filter) — audit gốc ghi "zero
    navigation" là thiếu hoàn toàn, không phải thiếu khi filter — trỏ `/portal/t4-pool?tour_id=`
    khi đang filter (để CatalogTab lọc lại đúng version của tour đó), trỏ generic khi không.
    CatalogTab lọc client-side (list đã fetch page_size=50 sẵn, không cần thêm backend param).
  - `useSearchParams()` bắt buộc Suspense boundary (Next.js App Router) — wrap ở
    `t4-pool/page.tsx`/`t6-atoms/page.tsx`, theo đúng pattern đã có ở
    `frontend/app/admin/curation/page.tsx`.

## Changed

- `shared/services/export_service.py` — xoá (dead code, 0 caller thật, tham chiếu cột không tồn
  tại trên `published_tours` production).
- `tests/integration/test_export_service.py` — xoá (test duy nhất của module trên).
- `tests/integration/conftest.py` — xoá block `ADD COLUMN IF NOT EXISTS hmac_signature/
  max_attempts` (chỉ phục vụ test đã xoá), giữ nguyên block `published_tours` schema patch
  (dùng chung với 2 test file khác).
- `api/routers/admin_settings.py` — `PIPELINE_GATES["pipeline_flow"]` thêm `llm_judge`,
  `revalidate` đúng thứ tự edge thật.
- `api/routers/acp_health.py` — xoá field `retry_count: 0` khỏi response `GET /admin/acp/
  run-health`.
- `frontend/app/admin/run-health/page.tsx` — xoá `retry_count` khỏi TS interface `RunHealth`.
- `api/routers/v1_tours.py::list_my_versions` — thêm `pt.tour_id` vào SELECT (additive, không
  đổi shape response cũ, chỉ thêm 1 field mới).
- `frontend/app/(tenant)/portal/_components/CatalogTab.tsx` — thêm `tour_id` vào `Version`,
  `useSearchParams()` filter theo `tour_id`, banner clear-filter, link "View atoms →" trong
  detail header.
- `frontend/app/(tenant)/portal/_components/AtomsTab.tsx` — `useSearchParams()` đọc `tour_id`,
  truyền vào fetch atoms, banner "Atoms for: ..." + clear-filter, link "← My Catalog" luôn hiện.
- `frontend/app/(tenant)/portal/t4-pool/page.tsx`, `t6-atoms/page.tsx` — wrap `<Suspense>`.

## Tradeoffs

- Filter T4↔T6 dùng `raw_tours.tour_id` làm khoá thật (không phải so tên `tour_name`/`aa_name` —
  2 giá trị này khác nhau hoàn toàn vì `aa_name` là tên ĐÃ rewrite, `tour_name` trong atoms là
  `src_name` gốc chưa rewrite) — đây là lý do phải sửa backend thêm 1 cột thay vì chỉ làm FE
  thuần, nhưng đổi lại nav trỏ đúng 100%, không đoán theo tên.
- CatalogTab lọc theo `tour_id` client-side, không thêm backend param cho `my-versions` — chấp
  nhận được vì `page_size` hiện tại tối đa 50 (đã fetch hết trong 1 lần), không phải phân trang
  lớn.
- Không thêm link "View atoms" cho từng dòng trong list bên trái (chỉ thêm ở detail panel khi đã
  chọn 1 version) — giữ thay đổi UI tối thiểu, tránh làm rối danh sách; đây là bước đủ để đóng gap
  "zero navigation" mà audit AA-439 #3 nêu, không phải thiết kế lại toàn bộ UX.

## Should know

- Migration: không có.
- Test suite: `pytest tests/unit/` (đúng scope CI) — 1585 passed, 0 fail, 0 lỗi import sau khi
  xoá `export_service.py`.
- `next build` (frontend) — compile sạch, TypeScript sạch, cả `/portal/t4-pool` và
  `/portal/t6-atoms` prerender thành công (○ static) — xác nhận Suspense boundary đủ, không lỗi
  "useSearchParams should be wrapped in a suspense boundary".
- #16 verify: gọi TRỰC TIẾP hàm `get_settings()` thật (không mock logic, chỉ mock DB pool trả về
  1 tenant giả) — output `pipeline_gates.pipeline_flow` khớp chính xác
  `["generate","validate","llm_judge","brand_audit","flag_fix","revalidate"]`.
- #3 verify: **không dùng browser thật** (không có tool browser-automation trong phiên này) —
  thay vào đó chạy đúng câu SQL mới (`pt.tour_id` thêm vào `my-versions`) + câu filter atoms
  (`tour_id = $1 AND owner_scope = $2`, giống hệt `admin_atoms.py::list_atoms` dùng) trực tiếp
  trên **RDS thật** qua S3-mediated ECS exec pattern, tenant thật `test-n1-flow`
  (`6fbaf284-e3cd-4b4b-b53b-c9a04e8fae8e`). Kết quả thật: version "Southern Laos: Plateau,
  Temples & the Four Thousand Islands — 5 Days" → `tour_id=4bf83a2c-cec0-4127-ac82-1a26fbbd48a3`
  → khớp với atom thật `atom_0e9a4a62ed` (cùng atom_id đã xuất hiện trong live-verify AA-451) —
  xác nhận cơ chế `tour_id`-based nav đúng trên dữ liệu thật, KHÔNG phải suy luận. Script chạy
  read-only, không ghi DB, đã xoá khỏi S3 sau khi chạy xong.
- CI Lint job chỉ chạy flake8 trên Python (`api/`, `services/`, `shared/`) — không lint frontend;
  đã tự chạy `flake8` với đúng flag CI dùng trên 3 file Python sửa, sạch.

## Post-merge / post-deploy record

PR #211 merged (`b9c1335`, squash, auto-merge — 5/5 required checks green: Lint, Security Audit,
Unit Tests, Integration Tests, Docker Build Check). `Deploy Dev` (run `32750906273`) green — ECS
task def `aa-cis-dev-api:134` (was `:133`), `rolloutState=COMPLETED`, confirmed via `aws ecs
describe-services`.

Real post-deploy HTTP live-verify (24/08/2026), via the S3-mediated ECS-exec pattern, real app on
`http://localhost:8000` inside the live container (real `DATABASE_URL`, real `ADMIN_SECRET`/
`JWT_SECRET` already resolved in the container's own env — never fetched or printed by this
session; the JWT-mint step reused the exact `sub`/`name`/`plan_tier`/`role` payload shape
`auth.py::_create_jwt()` builds, same precedent as AA-449's own live-verify), real tenant
`test-n1-flow`:

1. **#16** — `GET /admin/settings` → `pipeline_gates.pipeline_flow` =
   `["generate","validate","llm_judge","brand_audit","flag_fix","revalidate"]`, exactly matching
   the real deployed graph.
2. **#3** — `GET /v1/tours/my-versions?page_size=3` → real row now carries
   `tour_id="4bf83a2c-cec0-4127-ac82-1a26fbbd48a3"` (was absent before this PR). Same `tour_id`
   passed to `GET /admin/atoms?tour_id=4bf83a2c-...` → returned the real matching atom
   (`atom_6d25e9c335`, `tour_name="SOUTHERN LAOS"`) — confirms the full CatalogTab → AtomsTab
   round trip is correct on the live deployed system, not just in a pre-deploy SQL check.
3. **#17** — `GET /admin/acp/run-health` → response keys confirmed NOT to include `retry_count`
   (`['completed_at','cost_cap_breached','country','evaluator_score','evaluator_warning',
   'gate_statuses','run_id','stages','started_at','status','stuck','tenant_id',
   'total_cost_usd']`).
4. **#9** — no live HTTP check applicable (deletion); covered by CI's Integration Tests job
   passing clean (exercises the edited `conftest.py`).

Not done this session: a real browser click-through of the CatalogTab/AtomsTab nav UI (no
browser-automation tool available) — the underlying data mechanism is now live-HTTP-proven above,
which is the part that could actually be wrong; the UI itself is a thin `<Link href>` over that
already-correct data.

Issue itself (AA-454) left for Nghiệp to mark Done — same convention as recent sessions.
