(same verbatim build prompt as docs/claude_tasks/AA-512.md's "Issue 2/3 — AA-513" section —
copied in full below per the build prompt's own "save verbatim to each issue's own file" rule.)

## Issue 2/3 — AA-513 [T9] Route-aware write

Blocked by AA-512 (vừa xong ở bước trên).

### STEP0 bắt buộc trước khi code
1. Đọc `services/acp_content_writing/prompts.py` thật — xác nhận cấu trúc prompt T9 hiện tại nhận atom/context theo cách nào.
2. Đọc F5 (`quality_gates.py`, AA-452, đã Done) — xác nhận cơ chế `[R:atom_id]` + strip-trước-khi-trả hiện xử lý CHÍNH XÁC thế nào với 1 atom duy nhất, để biết cần sửa gì cho nhiều atom khác nhau trong cùng 1 bài.
3. Đọc `docs/adr/0009-provenance-tagged-writing-and-ported-gates.md` (repo Ms. Thư) — đối chiếu Provenance Tag `[A:…][T:…][F:…]` gốc, xác nhận phần nào AA-CIS đã port đủ, phần nào còn thiếu cho multi-segment.
4. Xác nhận `subject.route_id`/`ordered_segment_ids` (AA-510/AA-511) đọc được từ đâu, đúng field nào — không suy đoán tên cột.

### Việc cần làm
1. Sửa prompt T9 — khi `subject.route_id IS NOT NULL`, đưa cả `ordered_segment_ids` vào context theo đúng thứ tự Route (không chỉ 1 atom như 7 channel còn lại).
2. Xác nhận/sửa cơ chế tag+strip xử lý đúng NHIỀU `[R:atom_id]` khác nhau trong cùng 1 bài (không chỉ 1 loại atom_id lặp lại như hiện tại).

### Live-verify bắt buộc
- Tạo 1 bài Blog thật với Subject có `route_id` gắn ≥2 Segment — xác nhận prompt T9 nhận đủ context tất cả Segment theo đúng thứ tự.
- Xác nhận output bài viết có nhiều tag `[R:atom_id]` khác nhau, tag được strip sạch trước khi trả về tenant (không lộ tag thô).
- Regression: 1 bài channel khác Blog (1 atom, không Route) vẫn chạy đúng như cũ — không phá luồng 7 channel còn lại.

Báo cáo xong AA-513 rồi chuyển thẳng sang AA-514, không chờ xác nhận.
