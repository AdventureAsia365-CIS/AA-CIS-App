## Task cho Claude Code: Audit T9→T10→T11 (Final Write → Quality Pass → Publish) — task CUỐI của nhịp Tenant (AA-439) — KHÔNG sửa code

Mục tiêu: Task cuối cùng của AA-439. T7/T8 đã audit + quyết định kiến trúc (viết lại T8, giữ trust ramp). Task này audit 3 stage cuối T9-T10-T11, theo cùng bộ 5 câu hỏi chuẩn (Trigger/Backend/Frontend/Data/Gap) đã dùng xuyên suốt AA-438/439.

Repo: AA-CIS-App
Branch: feature/aa-439-tenant-tier-audit (đã tồn tại, đã gộp AA-438+AA-440 — checkout, KHÔNG tạo branch mới, KHÔNG push, KHÔNG PR).

Files cần đọc trước:
- `docs/claude_audit/AA-439-06-t7-t8-audit.md`, `AA-439-07-s4social-workflow-comparison.md` — đọc trước, đặc biệt: `services/acp_produce/pipeline.py::build_outline()` đã xác nhận là bước ngay trước drafting (N7), `quality_pass()`+`evaluate_quality()` trong `handler.py` (module CŨ, acp_s4_social) đã audit — task này audit bản TƯƠNG ĐƯƠNG trong pipeline N7/N8 THẬT (`services/acp_produce/`), không phải module cũ.
- `services/acp_produce/pipeline.py` — đọc TOÀN BỘ (không chỉ đoạn build_outline đã trích) — đây là pipeline N7 thật, cần xác nhận nó có bước "quality/editor pass nội bộ" tương đương T10 không.
- ADR-2026-038 mục 4, dòng T9: Input="Angle đã duyệt", Xử lý="Viết content theo channel structure (`Channel_Output_Structures.xlsx`)", Map code cũ="Phần 'generate' N7 cũ, giờ có gate T8 chặn trước".
- ADR mục 4, dòng T10: Input="Draft T9", Xử lý="QA nội bộ, tái dùng F1-F9 gate stack", Map code cũ="= N7, giữ nguyên hạ tầng QA".
- ADR mục 4, dòng T11: Input="Content pass T10", Xử lý="Xuất ra kênh", Output="Published social content", Map code cũ="= N8, vẫn chưa tồn tại".
- ADR mục 11.2: "T9-T10: 🔵 Đang debug (F1-F9, nhiều issue có sẵn), Nằm trong T8". "T11: ❌ Chưa tồn tại".

## Steps

1. **T9 (Final Write)**: Xác nhận `Channel Output Structures.xlsx` (7 kênh, structure/style/avoid — đã đọc nội dung đầy đủ ở AA-439-07 §A3) có được dùng trong `services/acp_produce/pipeline.py` khi viết content không, hay pipeline hiện tại viết theo cách khác (không theo per-channel rules từ file này). Xác nhận rõ: pipeline N7/N8 hiện tại có đọc file này, port nội dung này vào code, hay hoàn toàn không dùng.
2. **T10 (Quality/Editor Pass)**: Tìm "F1-F9 gate stack" trong code — grep `F1`, `F2`... `F9` hoặc tên hàm liên quan trong `services/acp_produce/` và `services/content_generation/`. Xác nhận: đây có phải cùng 1 bộ gate dùng ở A2/T3 (validate_node, grounding) hay là bộ khác riêng cho content social/T9-T10? ADR nói "tái dùng F1-F9" — xác nhận tái dùng thật hay build riêng.
3. **T11 (Publish)**: Xác nhận ADR nói đúng "chưa tồn tại" — tìm bất kỳ code nào liên quan "publish"/"distribute"/"export to channel" trong `services/acp_produce/`, `services/acp_deliver/` (nếu có) — phân biệt rõ với A2→A3 "published_tours" (khác hoàn toàn, đã audit ở AA-438) và với việc "publish_mode"/trust_ramp (đã audit AA-439-06, đó là AI ĐƯỢC PHÉP publish, không phải BẢN THÂN hành động publish ra kênh social thật). Nếu có manual/tool nào cho việc đăng bài thật lên LinkedIn/Facebook/etc — tìm, xác nhận có hay không.
4. Query dev DB: `acp_deliver.pieces` (đã biết held=125, passed=10 từ AA-439-06) — xem 10 piece "passed" đã đi đâu tiếp theo, có bằng chứng nào chúng đã publish thật hay chỉ dừng ở trạng thái "passed" trong DB.
5. Tổng kết toàn chuỗi T0-T11: dựa trên TẤT CẢ audit đã có (AA-439-01 đến 07 + task này), liệt kê NGẮN GỌN trạng thái thật cuối cùng của từng stage (1 dòng/stage: Live/Partial/Missing + lý do ngắn).

## Output — ĐÂY LÀ TASK CUỐI, viết thêm báo cáo TỔNG HỢP toàn bộ AA-439

- Báo cáo riêng: `docs/claude_audit/AA-439-08-t9-t10-t11-audit.md`.
- Báo cáo TỔNG HỢP: `docs/claude_audit/AA-439-00-SUMMARY-tenant-tier-audit.md` — tổng hợp TOÀN BỘ 8 task (01-08), theo đúng format đã dùng ở `AA-438-00-SUMMARY-admin-tier-audit.md` (bảng bug/gap tổng hợp, bảng trạng thái từng stage T0-T11, danh sách quyết định kiến trúc đã chốt trong ADR mục 0.1-0.5).

Verify: Không sửa code. Mọi kết luận có bằng chứng path:line hoặc query thật.

Sau khi done:
- Copy CHÍNH task prompt này vào `docs/claude_tasks/AA-439-08-audit-t9-t10-t11.md`.
- git commit trên branch `feature/aa-439-tenant-tier-audit` — KHÔNG push, KHÔNG PR (chờ lệnh tổng hợp push cuối, sẽ có task riêng, gộp chung với AA-438 luôn thành 1 PR duy nhất theo đúng quyết định đã có).
- Comment tóm tắt lên Linear AA-439 (không đổi status — Nghiệp sẽ đọc báo cáo tổng hợp rồi quyết định bước tiếp theo, có thể là build A4 hoặc bắt đầu build các stage T-series).
- Paste nội dung báo cáo tổng hợp (hoặc link file) về Claude Chat.
