# Build prompt — chuỗi tiếp theo (05/09/2026, phiên 5)

(Lưu nguyên văn từ comment Linear AA-527, đăng bởi nghiep pham quoc, 2026-09-05T05:51:41Z,
trước khi bắt đầu thực thi — theo đúng yêu cầu "Lưu nguyên văn prompt này vào
docs/claude_tasks/ trước khi bắt đầu.")

---

Giao Claude Code chạy **tuần tự, không dừng hỏi giữa chừng trừ khi ghi rõ ở dưới**: AA-527
(bổ sung) → AA-532 (mới). Mỗi issue tự STEP0 → (wireframe nếu có FE mới, đăng comment trước
khi code) → build → live-verify thật → comment báo cáo trên đúng issue đó → sang issue kế.
Không tự set Done — Claude Chat soát sau.

**Lưu nguyên văn prompt này vào** `docs/claude_tasks/` **trước khi bắt đầu.**

---

### 1. AA-527 — Bổ sung khung dashboard 8-section (Phương án C)

Theo đúng 2 comment "Claude Chat review" và "Bổ sung yêu cầu kiểm tra" ở trên. KHÔNG làm lại
phần Atomize/API đã build (PR #311) — chỉ (a) xác nhận 3 điểm filter/active-inactive/thống-kê-
sử-dụng đã có hay chưa và bổ sung nếu thiếu (báo cáo rõ từng điểm, không gộp chung), (b) bọc
thêm khung dashboard: sidebar 8-mục + header chọn-tour-điểm-neo + 7 panel audit-view còn lại.
Dùng skill `frontend-design`.

### 2. AA-532 — Route versioning (thay xoá-insert)

Theo đúng mô tả issue. STEP0 bắt buộc đọc lại `route_detection.py` + schema `route`/`subject`
thật trước khi thiết kế cột versioning — tự đề xuất tên cột/kiểu dữ liệu phù hợp, không bắt
buộc theo đúng tên gợi ý trong issue. Live-verify thật trên tenant WanderLux (có Subject active
tham chiếu Route) — xác nhận không còn FAIL, không xoá Route đang được tham chiếu.

---

Báo cáo evidence đầy đủ theo đúng chuẩn (browser/API/DB thật, không `/health` suông) trên từng
issue tương ứng. AA-527 có thay đổi visual lớn — cần Nghiệp xem trực tiếp trước khi merge,
không chỉ dựa evidence số liệu.

---

## Bối cảnh liên quan (3 comment trước đó trên AA-527, cùng ngày, cần đọc để hiểu đầy đủ "bổ sung" nghĩa là gì)

### Comment "Bổ sung yêu cầu kiểm tra trước khi build tiếp" (Nghiệp, 05/09/2026)

Trước khi build khung dashboard, STEP0 xác nhận rõ (không giả định) phần Atomize đã build
(PR #311) có đủ các điểm sau chưa, nếu thiếu thì bổ sung cùng đợt:

1. **Filter/dropdown đầy đủ** — xác nhận đã có filter theo tour, distinctiveness, unreviewed —
   có thiếu chiều lọc nào khác cần không (ví dụ theo owner_scope platform/legacy, theo Segment).
2. **Tour active/inactive có atom** — trang có phân biệt được tour nào đang active (còn
   publish/rewrite) và tour nào inactive nhưng vẫn có atom tồn đọng không? Nếu chưa có, thêm
   badge/filter cho điểm này.
3. **Thống kê atom/tour đang được dùng nhiều** — khác với `recurrence` (số Segment/itinerary
   trùng nhau của 1 atom): cần thống kê thực tế atom nào đã được dùng trong bao nhiêu
   content_piece đã viết (qua provenance tag hoặc bảng liên kết thật), và tour nào có tỷ lệ
   atom-được-dùng/tổng-atom cao nhất. Nếu dữ liệu này chưa tồn tại ở bất kỳ bảng nào (cần JOIN
   với content_piece/provenance), ghi chú rõ là backlog (cần thêm truy vấn mới), không bắt
   buộc build ngay trong đợt này — nhưng phải xác nhận rõ có hay không, không bỏ qua không
   nhắc tới.

Báo cáo rõ từng điểm 1-3 ở trên: đã có/mới thêm/backlog chưa làm được — không gộp chung thành
1 câu "đủ rồi".

### Comment "Claude Chat review — CHƯA Done, sai scope so với quyết định Phương án C" (05/09/2026)

Đối chiếu evidence trên chính issue này: comment 04/09 02:29 (Nghiệp, "Quyết định bố cục —
Phương án C") đã chốt rõ AA-527 phải là **Section 01 "Atomize" của 1 trang dashboard 8-section
tổng thể** — sidebar 8 mục (Atomize/Segment/Score/Route-Hub/Slate/Write-Gate/Review/Publish),
header chọn Tour làm điểm neo lọc toàn trang, 7 section audit-view còn lại hiển thị dữ liệu đã
có.

Wireframe STEP0 và bản Done trước đó chỉ build lại đúng AA-527 gốc — 1 trang atom-curation đơn
(2 cột: tour list trái, atom panel phải). Không có sidebar 8-section.

**Cần làm lại/bổ sung đúng theo Phương án C đã chốt:**

1. Sidebar trái 8 mục theo đúng thứ tự luồng, mỗi mục hiện số lượng thật.
2. Panel phải đổi cấu trúc: hiển thị chi tiết giai đoạn đang chọn trong sidebar (không phải cố
   định atom-curation).
3. Header trên cùng: dropdown chọn Tour làm điểm neo, lọc mọi panel bên dưới.
4. Atomize (đã build) trở thành 1 section trong sidebar đó (section 01), giữ nguyên phần đã
   làm (owner_scope badge, star/xoá, recurrence).
5. 7 section còn lại: audit-view hiển thị dữ liệu đã có (Segment/Score/Route-Hub/Slate/
   Write-Gate/Review/Publish), đúng dữ liệu đã kiểm kê ở AA-525 Phần 12, đánh dấu rõ phần nào
   backlog.
6. Responsive: sidebar đổ xuống tab ngang khi màn hẹp (đã nêu ở AA-530).

Phần Atomize/API/owner_scope đã build (PR #311) giữ nguyên, không làm lại — chỉ thiếu lớp
dashboard bao ngoài. Không set Done cho AA-527.

### Comment "Quyết định bố cục — Phương án C (Dashboard, sidebar)" (Nghiệp, 04/09/2026)

Nghiệp chọn **Phương án C** (không theo khuyến nghị A của Claude Code) — sidebar bên trái liệt
kê đủ 8 giai đoạn (Atomize/Segment/Score/Route-Hub/Slate/Write-Gate/Review/Publish) kèm số
lượng, panel lớn bên phải hiển thị chi tiết giai đoạn đang chọn.

Việc cần làm khi build:
1. Sidebar trái: 8 mục theo đúng thứ tự luồng, mỗi mục hiện số lượng thật.
2. Panel phải: hiển thị chi tiết giai đoạn đang chọn, dùng đúng cấu trúc dữ liệu đã kiểm kê ở
   AA-525 Phần 12 (bảng cột thật, đánh dấu rõ phần backlog bằng badge).
3. Header trên cùng: chọn Tour làm điểm neo (dropdown "Chọn Tour khác..."), mọi panel bên dưới
   lọc theo tour đã chọn.
4. Responsive: sidebar đổ xuống tab ngang khi màn hẹp — xử lý ngay từ đầu.
5. Atomize (Section 01, thay AA-527 cũ): UI cho AA duyệt atom nào gắn tour nào + recurrence +
   ghi chú rõ 2 khối còn thiếu (evidence, model-disagreement) là backlog.
6. 7 section còn lại: AUDIT view, không phải input — trừ Atomize (section 01) là nơi duy nhất
   có hành động (star/xoá/duyệt).
7. Tham khảo skill `frontend-design` cho design token/spacing/typography — màu thương hiệu AA
   (cam #DB9628, xanh đen #1F2933, Poppins).
